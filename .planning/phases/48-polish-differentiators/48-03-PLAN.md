---
phase: 48-polish-differentiators
plan: 03
type: execute
wave: 2
depends_on:
  - 48-01
  - 48-02
files_modified: []
autonomous: false
requirements:
  - TAB-08
  - TAB-09
  - TAB-10
  - TREE-08
  - TREE-09
  - TREE-10

must_haves:
  truths:
    - "All six requirements (TAB-08, TAB-09, TAB-10, TREE-08, TREE-09, TREE-10) verified by human testing"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 48 features: tab context menu, middle-click close, duplicate detection, directory staging, count badges, and Expand All / Collapse All.

Purpose: Visual and functional verification that automated work produces correct user-facing behavior.
Output: Human-confirmed approval of all six requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-polish-differentiators/48-01-SUMMARY.md
@.planning/phases/48-polish-differentiators/48-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all Phase 48 features</name>
  <files>n/a</files>
  <read_first>.planning/phases/48-polish-differentiators/48-CONTEXT.md</read_first>
  <action>
Present the following verification checklist to the user. Start the dev server with `bun run dev` if not already running.

What was built:
- Tab context menu with Close Others, Close All, Copy Path (Plan 01)
- Middle-click to close a tab (Plan 01)
- Duplicate tab detection — opening same repo switches to existing tab (Plan 01)
- Directory count badges showing recursive file count (Plan 02)
- Directory stage/unstage action button on hover (Plan 02)
- Expand All / Collapse All buttons in staging panel header (Plan 02)

Verification steps:

TAB-08 — Tab context menu:
1. Open two repos in separate tabs
2. Right-click a tab — verify native context menu appears with "Close Others", "Close All", "Copy Path"
3. Click "Copy Path" — paste somewhere to verify the absolute repo path was copied
4. Right-click a tab and click "Close Others" — verify all other tabs close, the right-clicked tab becomes active
5. Open two tabs again, right-click and click "Close All" — verify all tabs close and a fresh empty tab appears

TAB-09 — Middle-click close:
6. Open two tabs, middle-click on one — verify it closes (same behavior as clicking X)

TAB-10 — Duplicate detection:
7. Open a repo in tab 1. Create a new tab (Cmd+T). From the welcome screen, open the SAME repo.
8. Verify: no new tab is created. Instead, tab 1 (with the repo) becomes active. The empty new tab is closed.

TREE-09 — Count badges:
9. In staging panel, switch to tree view mode
10. Verify directory nodes show a count badge like "src/lib (3)" in muted text
11. Verify the count reflects the total number of files recursively (not just direct children)
12. Verify badge is visible when directory is both collapsed and expanded

TREE-08 — Directory staging:
13. Hover over a directory in the unstaged section — verify a "+" action button appears (like on file rows)
14. Click the "+" on the directory — verify ALL files in that directory get staged
15. In staged section, hover a directory — verify "-" action button appears
16. Click the "-" — verify all files in that directory get unstaged

TREE-10 — Expand All / Collapse All:
17. Verify two icon buttons (expand/collapse) appear in the staging panel header NEXT TO the tree/list toggle
18. Verify buttons are NOT visible when in flat list mode
19. Click Expand All — verify all directory nodes in all sections open
20. Click Collapse All — verify all directory nodes in all sections collapse

Wait for user response: "approved" or description of issues.
  </action>
  <verify>
    <automated>echo "Human verification — no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - All 20 verification steps pass
    - No visual regressions in the tab bar or staging panel
    - Context menu uses native OS styling (not custom HTML)
    - Count badges use muted color and do not push layout around
  </acceptance_criteria>
  <done>Human types "approved" confirming all Phase 48 features work correctly</done>
</task>

</tasks>

<verification>
Human has confirmed all 6 requirements work correctly via manual testing.
</verification>

<success_criteria>
Human types "approved" confirming all Phase 48 features work correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/48-polish-differentiators/48-03-SUMMARY.md`
</output>
