---
phase: 76-wire-messageeditor-into-merge-continue-merge-and-revert
plan: 03
type: execute
wave: 3
depends_on: ["76-01", "76-02"]
files_modified:
  - src/components/RepoView.svelte
  - src/components/CommitGraph.svelte
  - src/components/BranchSidebar.svelte
autonomous: true
requirements: [MSG-02, MSG-03, MSG-06]
must_haves:
  truths:
    - "RepoView hosts ONE <MessageEditor bind:this> and exposes handleOpenMessageEditor(default, title) (D-04 single instance)"
    - "Per-op title is set before open(): Merge -> \"Merge commit message\", Revert -> \"Revert commit message\" (D-03)"
    - "CommitGraph revert (handleRevert:541) routes: revert_commit_begin -> editor -> revert_continue; null -> return, no commit (D-02, MSG-03/MSG-06)"
    - "CommitGraph merge (handleMergeBranch:590) routes: merge_branch_begin -> kind switch; ready -> editor -> merge_continue; fast_forwarded/conflicts -> no editor (MSG-02, criterion 5)"
    - "BranchSidebar merge (handleMergeBranch:395) routes identically — no second silent merge path remains (D-04, CONTEXT specifics)"
    - "On editor cancel (null) no *_continue invoke fires — begin already emitted repo-changed so the in-progress UI is visible (D-02, MSG-06)"
  artifacts:
    - path: "src/components/RepoView.svelte"
      provides: "MessageEditor host, handleOpenMessageEditor, onopenmessageeditor threaded to CommitGraph/BranchSidebar/StagingPanel"
      contains: "handleOpenMessageEditor"
    - path: "src/components/CommitGraph.svelte"
      provides: "revert + merge-branch routing through onopenmessageeditor"
      contains: "merge_branch_begin"
    - path: "src/components/BranchSidebar.svelte"
      provides: "merge-branch routing through onopenmessageeditor"
      contains: "merge_branch_begin"
  key_links:
    - from: "CommitGraph.handleMergeBranch"
      to: "onopenmessageeditor (RepoView handleOpenMessageEditor)"
      via: "callback prop on result.kind === ready"
      pattern: "onopenmessageeditor"
    - from: "RepoView <MessageEditor>"
      to: "editorTitle $state"
      via: "title prop set before open()"
      pattern: "editorTitle"
---

<objective>
Host a single `MessageEditor` in `RepoView.svelte` (mirroring the `onopenrebaseeditor` host+thread pattern) and route the graph/sidebar trigger sites through it: CommitGraph revert (`handleRevert:541`) and merge (`handleMergeBranch:590`), BranchSidebar merge (`handleMergeBranch:395`). RepoView also threads `onopenmessageeditor` to StagingPanel (consumed in Plan 04).

Purpose: MSG-02 (merge-branch editor, ff skip), MSG-03 (revert editor), MSG-06 (cancel → no commit, recoverable). Establishes the `onopenmessageeditor` prop contract Plan 04 consumes.
Output: RepoView host + threading; CommitGraph + BranchSidebar routed through the modal.

Depends on 76-01 (`merge_branch_begin`, `get_merge_message`, `MergeBeginResult`) and 76-02 (`revert_commit_begin`, `revert_continue`).
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-wire-messageeditor-into-merge-continue-merge-and-revert/76-RESEARCH.md
@.planning/phases/76-wire-messageeditor-into-merge-continue-merge-and-revert/76-PATTERNS.md
@.planning/phases/75-message-editor-infrastructure/75-01-SUMMARY.md
@src/components/MessageEditor.svelte

<interfaces>
<!-- Backend contract from Plans 01/02 (already implemented when this runs). -->
- `MessageEditor.open(default: string) => Promise<string | null>` — null on Cancel/Esc/backdrop/empty/whitespace (frozen Phase 75). `title` is a `$props()`, set BEFORE open().
- `merge_branch_begin(path, branch) => { kind: "fast_forwarded" } | { kind: "conflicts" } | { kind: "ready", message: string }`
- `merge_continue(path, message: string) => void`
- `revert_commit_begin(path, oid) => { graph, message: string | null }`  (Err code "conflict_state" on conflict)
- `revert_continue(path, message: string) => void`

<!-- New prop contract this plan defines (Plan 04 + this plan's children consume). Mirror onopenrebaseeditor. -->
onopenmessageeditor?: (defaultValue: string, title: string) => Promise<string | null>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Host MessageEditor in RepoView + thread onopenmessageeditor to children</name>
  <files>src/components/RepoView.svelte</files>
  <read_first>
    - src/components/RepoView.svelte (onopenrebaseeditor host+thread: handleOpenRebaseEditor:567, BranchSidebar prop :805, CommitGraph prop :888, StagingPanel render :906; imports :31-38)
    - src/components/MessageEditor.svelte (open() contract + title $props, lines 1-35)
    - .planning/phases/75-message-editor-infrastructure/75-01-SUMMARY.md (the worked Phase 76 host snippet: bind:this, await open, null check)
    - 76-PATTERNS.md §"RepoView.svelte" (lines 186-205) — the handleOpenMessageEditor + reactive-title code
    - 76-RESEARCH.md Open Question 1 (reactive title; A1) and Open Question 2 (Revert Continue reaches host via StagingPanel→OperationBanner threading)
  </read_first>
  <behavior>
    - Test: RepoView renders a MessageEditor instance (query for it via the component or its container).
    - Test: handleOpenMessageEditor("def", "Revert commit message") sets the title to "Revert commit message" before resolving via the editor's open() (mock MessageEditor.open).
    - Test: onopenmessageeditor is passed to CommitGraph, BranchSidebar, and StagingPanel (assert prop present — via spy/mock of child or rendered attribute).
  </behavior>
  <action>
    Import MessageEditor (`import MessageEditor from "./MessageEditor.svelte";`). Add `let messageEditorRef = $state<MessageEditor | null>(null);` and `let editorTitle = $state("Merge commit message");`. Add `async function handleOpenMessageEditor(defaultValue: string, title: string): Promise<string | null>` that sets `editorTitle = title` THEN returns `(await messageEditorRef?.open(defaultValue)) ?? null` (PATTERNS.md lines 196-202). Render `<MessageEditor bind:this={messageEditorRef} title={editorTitle} />` near the end of the template (alongside other modals). Thread `onopenmessageeditor={handleOpenMessageEditor}` onto `<CommitGraph>` (:888 row), `<BranchSidebar>` (:805 row), and `<StagingPanel>` (:906 row) — mirroring the existing `onopenrebaseeditor=` attribute on each. Do NOT alter the onopenrebaseeditor wiring.
  </action>
  <verify>
    <automated>npx vitest run src/components/RepoView.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onopenmessageeditor' src/components/RepoView.svelte` >= 4 (handler def + 3 child props)
    - `grep -n 'MessageEditor' src/components/RepoView.svelte` shows import + `bind:this={messageEditorRef}`
    - `editorTitle` set before `open()` in handleOpenMessageEditor (D-03)
    - RepoView.test.ts green
  </acceptance_criteria>
  <done>RepoView hosts the single MessageEditor with reactive title and threads onopenmessageeditor to all three children. Commit: `feat(76-03): host MessageEditor in RepoView, thread onopenmessageeditor`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route CommitGraph + BranchSidebar merge/revert triggers through the editor</name>
  <files>src/components/CommitGraph.svelte, src/components/BranchSidebar.svelte</files>
  <read_first>
    - src/components/CommitGraph.svelte (handleRevert:541-556 incl. safeInvoke("revert_commit"):544; handleMergeBranch:590-598 incl. safeInvoke("merge_branch"):592; onopenrebaseeditor prop decl :66 + usage; handleInteractiveRebaseBranch:610 — the fetch-then-callback analog)
    - src/components/BranchSidebar.svelte (handleMergeBranch:395-405 incl. safeInvoke("merge_branch"):397, post-step loadRefs/onrefreshed :399-400; onopenrebaseeditor prop decl :16)
    - 76-PATTERNS.md §"CommitGraph.svelte" (lines 209-227, the kind-discrimination snippet) and §"BranchSidebar.svelte" (lines 231-233)
    - 76-RESEARCH.md §"Anti-Patterns" (no editor on conflicted/ff; no --no-ff) and Code Examples (tagged begin result)
  </read_first>
  <behavior>
    - Test (CommitGraph revert): mock revert_commit_begin → {message}; assert onopenmessageeditor called with (message, "Revert commit message"); on non-null assert revert_continue invoked with edited msg; on null assert revert_continue NOT invoked.
    - Test (CommitGraph merge ready): mock merge_branch_begin → {kind:"ready", message}; assert editor opened with (message, "Merge commit message"); non-null → merge_continue invoked; null → not invoked.
    - Test (CommitGraph merge ff): mock merge_branch_begin → {kind:"fast_forwarded"}; assert onopenmessageeditor NOT called and merge_continue NOT called.
    - Test (CommitGraph merge conflicts): mock {kind:"conflicts"}; assert editor NOT opened.
    - Test (BranchSidebar merge): same merge-branch flow; assert editor opened on ready, post-step loadRefs/onrefreshed still runs.
  </behavior>
  <action>
    Add `onopenmessageeditor?: (def: string, title: string) => Promise<string | null>` to both components' Props interfaces (mirror the existing `onopenrebaseeditor` decl) and accept it in the destructured props.

    CommitGraph `handleRevert`: replace `safeInvoke("revert_commit", {path, oid})` with `safeInvoke<{message: string | null}>("revert_commit_begin", {path: repoPath, oid: commit.oid})`; then `const msg = await onopenmessageeditor?.(result.message ?? "", "Revert commit message"); if (msg == null) return;` (D-02 recoverable — begin already emitted); then `await safeInvoke("revert_continue", {path: repoPath, message: msg})`. Preserve existing error/toast handling.

    CommitGraph `handleMergeBranch`: replace `safeInvoke("merge_branch", {path, branch})` with `const result = await safeInvoke<{kind: string; message?: string}>("merge_branch_begin", {path: repoPath, branch});` then `if (result.kind === "ready") { const msg = await onopenmessageeditor?.(result.message ?? "", "Merge commit message"); if (msg == null) return; await safeInvoke("merge_continue", {path: repoPath, message: msg}); }` — the `fast_forwarded`/`conflicts` arms do nothing (the begin's repo-changed emit drives the UI; criterion 5 ff skip).

    BranchSidebar `handleMergeBranch`: identical merge_branch_begin → kind switch → editor → merge_continue. Keep its post-step `loadRefs(repoPath); onrefreshed?.()` (:399-400) on the success path.

    Both merge trigger sites must route (D-04 — no second silent path).
  </action>
  <verify>
    <automated>npx vitest run src/components/CommitGraph.test.ts src/components/BranchSidebar.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'merge_branch_begin' src/components/CommitGraph.svelte` >= 1 and same in BranchSidebar.svelte
    - `grep -n 'revert_commit_begin\|revert_continue' src/components/CommitGraph.svelte` present
    - `grep -n '"merge_branch"\|"revert_commit"' src/components/CommitGraph.svelte src/components/BranchSidebar.svelte` returns NOTHING (old direct calls gone — no silent path)
    - `result.kind === "ready"` gate present; fast_forwarded/conflicts open no editor (asserted by tests)
    - null-return-before-continue path present (D-02 / MSG-06)
    - CommitGraph.test.ts + BranchSidebar.test.ts green
  </acceptance_criteria>
  <done>Graph + sidebar merge/revert triggers route through the host editor; ff/conflict skip the editor; cancel makes no commit. Commit: `feat(76-03): route CommitGraph + BranchSidebar merge/revert through MessageEditor`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (textarea) → IPC | edited message string crosses WebView → Rust via invoke args (serialized, not shell) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-07 | Tampering | edited message passed to merge_continue/revert_continue via invoke | mitigate | Message travels as a serde-serialized IPC arg, never concatenated into a command string; the backend (Plans 01/02) passes it as a single argv element. Frontend adds no shell surface. |
| T-76-08 | Information Disclosure | default message (may contain branch/oid) shown in modal | accept | Local single-user GUI; the user is the data owner; no PII boundary crossed. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan. Audit N/A. |
</threat_model>

<verification>
- RepoView, CommitGraph, BranchSidebar vitest suites green.
- grep gates: onopenmessageeditor threaded x4 in RepoView; merge_branch_begin in both children; old "merge_branch"/"revert_commit" direct invokes gone.
</verification>

<success_criteria>
MSG-02 (merge editor, ff skip via kind switch), MSG-03 (revert editor), MSG-06 (cancel → no commit; begin-emitted recoverable state) wired for the graph + sidebar trigger sites through a single RepoView-hosted MessageEditor with per-op titles (D-03/D-04).
</success_criteria>

<output>
Create `.planning/phases/76-wire-messageeditor-into-merge-continue-merge-and-revert/76-03-SUMMARY.md` when done.
</output>
