---
phase: 76-wire-messageeditor-into-merge-continue-merge-and-revert
plan: 04
type: execute
wave: 4
depends_on: ["76-01", "76-02", "76-03"]
files_modified:
  - src/components/StagingPanel.svelte
  - src/components/OperationBanner.svelte
autonomous: false
requirements: [MSG-01, MSG-06]
must_haves:
  truths:
    - "StagingPanel inline merge form (mergeSubject/mergeBody/commitMerge + the {:else if isMerge} UI block) is REMOVED — no second message editor competes with the modal (RESEARCH finding 5)"
    - "merge --continue (after conflicts resolved) routes through the modal: get_merge_message -> onopenmessageeditor(default, \"Merge commit message\") -> merge_continue (MSG-01)"
    - "merge --continue default comes verbatim from get_merge_message (.git/MERGE_MSG), fixing the old hardcoded 'Merge branch X into Y' divergence (RESEARCH line 93)"
    - "OperationBanner renders Revert Continue + Abort buttons for a Revert state (previously zero buttons — RESEARCH finding 4); this is MSG-06's revert recovery path"
    - "OperationBanner Revert Abort calls revert_abort; Revert Continue routes through the host modal (threaded RepoView -> StagingPanel -> OperationBanner, OQ-2)"
    - "OperationBanner:33 dead merge_continue branch is NOT wired as a real editor path (RESEARCH finding 6 / anti-pattern)"
    - "Cancel (null) on merge-continue or revert makes no commit; begin/in-progress state stays visible (D-02, MSG-06)"
  artifacts:
    - path: "src/components/StagingPanel.svelte"
      provides: "modal-routed merge-continue (inline form removed)"
      contains: "get_merge_message"
    - path: "src/components/OperationBanner.svelte"
      provides: "Revert Continue + Abort affordance"
      contains: "revert_abort"
  key_links:
    - from: "StagingPanel merge-continue (allResolved)"
      to: "onopenmessageeditor (RepoView)"
      via: "get_merge_message default -> modal -> merge_continue"
      pattern: "get_merge_message"
    - from: "OperationBanner Revert Abort button"
      to: "revert_abort command"
      via: "safeInvoke"
      pattern: "revert_abort"
---

<objective>
Replace StagingPanel's inline merge-commit form with the modal-routed merge-continue flow (default from `get_merge_message`, the single live merge-continue editor per RESEARCH finding 5/6), and add the Revert Continue + Abort affordance to OperationBanner (RESEARCH finding 4 — a Revert state currently renders zero buttons, trapping a cancelled revert).

Purpose: MSG-01 (merge --continue editor, verbatim default, the conflicted MSG-01 path) and MSG-06 (revert recovery affordance + cancel leaves visible recoverable state).
Output: StagingPanel inline form removed + modal-routed; OperationBanner Revert buttons.

Depends on 76-03 for the `onopenmessageeditor` prop threaded RepoView → StagingPanel; this plan re-threads it StagingPanel → OperationBanner (OQ-2). Depends on 76-01 (`get_merge_message`, `merge_continue`) and 76-02 (`revert_continue`, `revert_abort`).
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-wire-messageeditor-into-merge-continue-merge-and-revert/76-RESEARCH.md
@.planning/phases/76-wire-messageeditor-into-merge-continue-merge-and-revert/76-PATTERNS.md

<interfaces>
<!-- Backend (Plans 01/02) + prop (Plan 03), all implemented when this runs. -->
- `get_merge_message(path) => Promise<string | null>`  (verbatim .git/MERGE_MSG)
- `merge_continue(path, message: string) => void`  (--cleanup=strip; clears MERGE_HEAD)
- `revert_continue(path, message: string) => void`  (--cleanup=strip; clears REVERT_HEAD)
- `revert_abort(path) => void`  (git revert --abort; clears REVERT_HEAD)
- prop threaded from RepoView (Plan 03): `onopenmessageeditor?: (def: string, title: string) => Promise<string | null>`
- OperationBanner is rendered by StagingPanel (StagingPanel.svelte:909) with `info`/`repoPath`/`onaction` props.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace StagingPanel inline merge form with modal-routed merge-continue; re-thread onopenmessageeditor to OperationBanner</name>
  <files>src/components/StagingPanel.svelte</files>
  <read_first>
    - src/components/StagingPanel.svelte (inline merge form to DELETE: mergeSubject/mergeBody :574-575, pre-fill $effect :578-585 hardcoding `Merge branch '<src>' into <tgt>`, commitMerge :587-607, {:else if isMerge} UI block :1302-1362; OperationBanner render :909; allResolved :134; isMerge :102)
    - 76-PATTERNS.md §"StagingPanel.svelte" (lines 237-246) — deletion targets + the modal-routed replacement
    - 76-RESEARCH.md finding 5 (StagingPanel is the ONLY live merge-continue editor), finding 6 (OperationBanner:33 dead — do NOT wire), Pitfall 1 (cleanup=strip handled at backend), Open Question 2 (thread to OperationBanner)
  </read_first>
  <behavior>
    - Test: after conflicts resolved (allResolved true), the merge-commit action calls get_merge_message then onopenmessageeditor with ("<default>", "Merge commit message"); on non-null calls merge_continue with the edited message; on null calls neither (no commit — D-02).
    - Test: the old inline subject/body inputs + "Commit and Merge" (commitMerge) UI no longer renders for a merge state.
    - Test: onopenmessageeditor is passed down to OperationBanner.
  </behavior>
  <action>
    DELETE the inline merge form: state `mergeLoading`/`mergeSubject`/`mergeBody` (:573-575), the pre-fill `$effect` (:578-585), `commitMerge` (:587-607), and the `{:else if isMerge}` UI block (:1302-1362). Accept the `onopenmessageeditor` prop (mirror how onopenrebaseeditor / other callback props are declared). Add a merge-continue handler that fires when `allResolved`: `const def = await safeInvoke<string | null>("get_merge_message", {path: repoPath}); const msg = await onopenmessageeditor?.(def ?? "", "Merge commit message"); if (msg == null) return; await safeInvoke("merge_continue", {path: repoPath, message: msg});` — render a SINGLE replacement "Commit merge" button in place of the deleted block, shown when `isMerge && allResolved` and gated `disabled={!allResolved}`, whose `onclick` calls this handler. (The old subject/body inputs + `commitMerge` button at :1347-1348 are gone; this is their one-button replacement — and it is also the D-02 clean-merge RETRY affordance: after a cancelled clean non-ff merge, `MERGE_HEAD` is set and `allResolved` is true, so this button must render so the user can re-open the editor and retry.) Thread `onopenmessageeditor={onopenmessageeditor}` onto the `<OperationBanner>` render (:909). Do NOT construct the default in the frontend (MSG-04 — comes verbatim from get_merge_message).
  </action>
  <verify>
    <automated>npx vitest run src/components/StagingPanel.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'mergeSubject\|mergeBody\|commitMerge' src/components/StagingPanel.svelte` returns NOTHING (inline form removed)
    - `grep -c 'get_merge_message' src/components/StagingPanel.svelte` >= 1
    - `grep -n 'Merge branch .* into' src/components/StagingPanel.svelte` returns NOTHING (old hardcoded default gone)
    - `grep -c 'onopenmessageeditor' src/components/StagingPanel.svelte` >= 2 (prop accept + thread to OperationBanner)
    - a single "Commit merge"-style button renders for `isMerge && allResolved` (the inline subject/body inputs are gone) and triggers the get_merge_message→modal→merge_continue handler
    - null-return-before-merge_continue present (D-02); after a cancelled clean merge the button still renders so the user can retry
    - StagingPanel.test.ts green
  </acceptance_criteria>
  <done>Inline merge form gone; merge-continue routes through the modal with verbatim get_merge_message default; prop threaded to OperationBanner. Commit: `feat(76-04): route StagingPanel merge-continue through MessageEditor, remove inline form`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Revert Continue + Abort affordance to OperationBanner</name>
  <files>src/components/OperationBanner.svelte</files>
  <read_first>
    - src/components/OperationBanner.svelte (FULL — isMerge/isRebase derived :16-17; handleContinue:30-43 (isMerge?merge_continue:rebase_continue:33); handleAbort:58-84; {#if isRebase} button block :135-180 with var(--color-success-bg)/warning/danger; label-only {:else} :131-133 where Revert currently falls)
    - 76-PATTERNS.md §"OperationBanner.svelte" (lines 250-258) — isRevert derived, button block analog, prop threading
    - 76-RESEARCH.md finding 4 (revert no recovery), finding 6 (do NOT wire :33 merge branch), Open Question 2 (Continue reaches host modal via callback prop), Pitfall 2
  </read_first>
  <behavior>
    - Test: for a Revert op_type, an Abort button and a Continue button render (previously none).
    - Test: clicking Revert Abort calls revert_abort with {path}; shows success toast; calls onaction.
    - Test: clicking Revert Continue calls get_merge_message then onopenmessageeditor("<default>", "Revert commit message"); non-null → revert_continue with edited msg; null → neither (no commit).
    - Test: the OperationBanner:33 merge branch is NOT turned into a rendered merge-continue editor button (merge-continue stays in StagingPanel; finding 6).
  </behavior>
  <action>
    Add `isRevert = $derived(info.op_type === "Revert")`. Accept an `onopenmessageeditor?: (def: string, title: string) => Promise<string | null>` prop (threaded from StagingPanel, Task 1). Add a `{#if isRevert}` button block modeled on the `{#if isRebase}` block (:135-180), reusing the existing `var(--color-*-bg)` tokens (CLAUDE.md: CSS custom properties only, NEVER inline a hex):
    - Abort button: mirror `handleAbort` (:58-84) — confirm/toast pattern, `safeInvoke("revert_abort", {path: repoPath})`, then `onaction?.()`.
    - Continue button: handler does `const def = await safeInvoke<string | null>("get_merge_message", {path: repoPath}); const msg = await onopenmessageeditor?.(def ?? "", "Revert commit message"); if (msg == null) return; await safeInvoke("revert_continue", {path: repoPath, message: msg}); onaction?.();`
    Extend `handleContinue` only if reused for revert; otherwise a dedicated revert-continue handler is cleaner. Do NOT wire the dead `isMerge ? "merge_continue"` :33 path into an editor (finding 6) — leave merge-continue to StagingPanel.
  </action>
  <verify>
    <automated>npx vitest run src/components/OperationBanner.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'isRevert' src/components/OperationBanner.svelte` >= 1
    - `grep -c 'revert_abort' src/components/OperationBanner.svelte` >= 1
    - `grep -c 'revert_continue' src/components/OperationBanner.svelte` >= 1
    - Revert state renders Continue + Abort buttons (asserted by tests); no inline hex colors added (`grep -nE '#[0-9a-fA-F]{3,6}' src/components/OperationBanner.svelte` shows no NEW hex in the revert block)
    - `:33` merge-continue branch unchanged / not turned into a rendered editor button (finding 6)
    - OperationBanner.test.ts green
  </acceptance_criteria>
  <done>Revert state has working Continue (modal-routed) + Abort (revert_abort) buttons — MSG-06 revert recovery path exists. Commit: `feat(76-04): OperationBanner revert Continue/Abort affordance`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify end-to-end commit-message UX (merge/continue, merge, revert)</name>
  <what-built>End-to-end commit-message UX for merge --continue, merge &lt;branch&gt;, and revert &lt;oid&gt;: editor pre-filled from git's own default, edited message lands in the commit, cancel/empty leaves a visible recoverable state, ff merges skip the editor.</what-built>
  <how-to-verify>
    Run `just dev`. In a scratch repo with two divergent branches:
    1. Merge a non-ff branch (from CommitGraph context menu AND from BranchSidebar) → editor opens titled "Merge commit message" pre-filled `Merge branch '<b>'`; edit, save → merge commit body matches your edit. Cancel another → no commit, OperationBanner shows the in-progress merge, Abort recovers.
    2. Merge a fast-forwardable branch → NO editor, fast-forward happens (no merge commit).
    3. Create a conflicting merge → resolve in StagingPanel → the merge-commit step opens the modal (NOT the old inline subject/body form) titled "Merge commit message" pre-filled from MERGE_MSG with NO `# Conflicts:` lines in the resulting commit body.
    4. Revert a commit → editor titled "Revert commit message" pre-filled `Revert "<subj>"\n\nThis reverts commit <full-oid>.`; save → revert commit. Cancel another → no commit, OperationBanner shows Revert with Continue + Abort; Abort clears REVERT_HEAD.
    5. Empty/whitespace message in any of the above → operation aborts, repo stays recoverable.
    Note (begin-emit coverage): scenarios above exercise the backend repo-changed emit for ALL three merge_branch_begin variants — scenario 2 = fast_forwarded (no banner, correct), scenario 3 = conflicts (conflict UI surfaces, proving the emit), scenario 1 cancel = ready/clean (in-progress banner appears). The emit-on-every-variant guarantee is verified structurally in 76-01 acceptance (single unconditional emit after an all-arm match) plus this manual UAT; it is NOT a vitest unit test (mocked IPC never runs the real wrapper).
  </how-to-verify>
  <action>Manual in-app verification only (no code change). Run `just dev` and exercise the five scenarios above; this checkpoint exists because the visible banner-after-cancel loop is best confirmed live (RESEARCH §Validation Architecture "full end-to-end UX" row = manual UAT).</action>
  <verify><human-check>Operator runs the five scenarios in `just dev` and confirms each behaves as described.</human-check></verify>
  <done>All five scenarios behave as described; operator types "approved".</done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (textarea) → IPC | edited message crosses WebView → Rust via invoke args (serialized) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-09 | Tampering | edited message → merge_continue / revert_continue via invoke | mitigate | IPC-serialized arg, never a shell string; backend (Plans 01/02) passes it as a single `commit -m` argv element. No new frontend shell surface. |
| T-76-10 | Denial of Service | repeated Abort/Continue clicks during in-flight IPC | accept | Existing OperationBanner buttons already tolerate this (handleAbort/handleContinue patterns); local single-user GUI, no amplification. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan. Audit N/A. |
</threat_model>

<verification>
- StagingPanel + OperationBanner vitest suites green.
- grep gates: inline merge form symbols gone; get_merge_message + revert_abort + revert_continue + isRevert present; no new inline hex.
- `just check` green (success criterion 6) — run after this plan as the phase gate.
- Human-verify checkpoint passed.
</verification>

<success_criteria>
MSG-01 (merge --continue via modal, verbatim default, clean conflicted body) and MSG-06 (revert recovery affordance + cancel-leaves-recoverable) complete; the only live merge-continue editor is the modal (inline form removed); a Revert state is no longer a dead end; `just check` green.
</success_criteria>

<output>
Create `.planning/phases/76-wire-messageeditor-into-merge-continue-merge-and-revert/76-04-SUMMARY.md` when done. Move `.planning/todos/pending/2026-04-14-collect-commit-messages-for-merge-revert-instead-of-bypassing-editor.md` to `.planning/todos/done/` (this phase closes it).
</output>
