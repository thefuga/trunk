---
phase: 76-wire-messageeditor-into-merge-continue-merge-and-revert
plan: 02
type: tdd
wave: 2
depends_on: ["76-01"]
files_modified:
  - src-tauri/src/commands/commit_actions.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [MSG-03, MSG-06]
must_haves:
  truths:
    - "revert <oid> on a clean revert returns the verbatim .git/MERGE_MSG default: Revert \"<subject>\" then This reverts commit <full-40-char-oid>. (MSG-03)"
    - "revert_commit_begin sets REVERT_HEAD (does not commit) and emits repo-changed so a later cancel leaves a visible, recoverable state (D-02, MSG-06)"
    - "revert that conflicts still returns Err(conflict_state) — existing handling preserved (no editor on conflict)"
    - "revert_continue commits with --cleanup=strip, clears REVERT_HEAD, body equals the edited message with no # lines (MSG-03 fidelity for conflicted reverts)"
    - "revert_abort runs git revert --abort, clears REVERT_HEAD, leaves a clean tree (MSG-06 recovery path for revert)"
    - "commit_actions.rs revert path no longer uses --no-edit (D-01, OQ-4)"
    - "editor.rs prepare()/EditorHandle is NOT imported or called by commit_actions.rs (D-01a)"
  artifacts:
    - path: "src-tauri/src/commands/commit_actions.rs"
      provides: "revert_commit_begin, revert_continue, revert_abort + #[cfg(test)] temp-repo suite"
      contains: "revert_commit_begin"
    - path: "src-tauri/src/lib.rs"
      provides: "handler registration for revert_commit_begin, revert_continue, revert_abort"
      contains: "revert_abort"
  key_links:
    - from: "revert_commit_begin async wrapper"
      to: "app.emit(\"repo-changed\")"
      via: "after REVERT_HEAD set"
      pattern: "emit.*repo-changed"
    - from: "revert_continue"
      to: "git commit -m --cleanup=strip"
      via: "subprocess args array"
      pattern: "cleanup=strip"
    - from: "revert_abort"
      to: "git revert --abort"
      via: "subprocess args array"
      pattern: "revert.*--abort"
---

<objective>
Build the revert-side backend: a two-step `revert_commit_begin` (`git revert --no-commit` → return verbatim MERGE_MSG default, emit `repo-changed`), a `revert_continue` finish (`git commit -m --cleanup=strip`, clears REVERT_HEAD), and a NEW `revert_abort` (`git revert --abort`). The abort is what makes MSG-06 satisfiable for revert (RESEARCH finding 4: today a cancelled revert traps the user — no recovery command exists).

Purpose: MSG-03 (revert default message + commit) and MSG-06 (empty → leave recoverable → abort path). All proven against a temp git repo.
Output: `commit_actions.rs` with three new commands + a `#[cfg(test)]` suite; `lib.rs` registrations.

Depends on 76-01 only for the shared `lib.rs` edit (sequential to avoid a registration-list conflict); no code dependency on Plan 01's symbols.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-wire-messageeditor-into-merge-continue-merge-and-revert/76-RESEARCH.md
@.planning/phases/76-wire-messageeditor-into-merge-continue-merge-and-revert/76-PATTERNS.md

<interfaces>
<!-- The contract Plan 03 (frontend) invokes. -->
- `revert_commit_begin(path: String, oid: String) -> Result<RevertBeginResult, String>`
   where RevertBeginResult carries the default message; on clean success REVERT_HEAD is set and repo-changed is emitted.
   Conflicted revert returns Err with code `conflict_state` (existing handling).
   Recommended shape (mirror Plan 01's pattern; a 2-field struct is simpler than a tagged enum here since there is one editor outcome):
   ```rust
   #[derive(serde::Serialize)]
   pub struct RevertBeginResult { pub graph: GraphResult, pub message: Option<String> }
   ```
- `revert_continue(path: String, message: String) -> Result<(), String>`  (MUTATION: git commit -m --cleanup=strip; clears REVERT_HEAD)
- `revert_abort(path: String) -> Result<(), String>`  (MUTATION: git revert --abort; clears REVERT_HEAD)

Existing types: `GraphResult` (git/types.rs:72), `TrunkError::new(code, msg)`, error code `conflict_state`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 0: Temp-repo test scaffold + RED tests for revert_commit_begin / revert_continue / revert_abort</name>
  <files>src-tauri/src/commands/commit_actions.rs</files>
  <read_first>
    - src-tauri/src/commands/commit_actions.rs (FULL — revert_commit_inner:143-171 is the analog; conflict_state branch :159-166 MUST be preserved)
    - src-tauri/src/commands/operation_state.rs (merge_abort_inner ~183-202 and merge_abort wrapper ~410-429 — the abort analog for revert_abort; merge_continue_inner Some(msg) arm 158-165 — the finish analog for revert_continue)
    - src-tauri/src/git/review.rs:660-700 (make_repo TempDir scaffold — commit_actions.rs has NO #[cfg(test)] yet, Wave-0 gap)
    - 76-RESEARCH.md OQ-3 table (revert + conflicted-revert MERGE_MSG, lines 90-91; full 40-char OID), §"Don't Hand-Roll" (git revert --abort), §"Validation Architecture" MSG-03/MSG-06 rows
    - 76-PATTERNS.md §"revert_commit_begin", §"revert_continue", §"revert_abort" (lines 118-167)
  </read_first>
  <behavior>
    - Test (clean revert default): commit a change, `revert_commit_begin(path, oid)` returns `message == Some(s)` where `s` starts with `Revert "<subject>"`, contains `This reverts commit <oid>.` with the FULL 40-char oid (not short); assert `REVERT_HEAD` file present (not committed yet).
    - Test (revert_continue clears REVERT_HEAD): after begin, `revert_continue(path, "Revert \"x\"\n\nbody")` → assert `REVERT_HEAD` absent, HEAD commit body == the edited message, no line starts with `#`.
    - Test (conflicted revert): construct a revert that conflicts; `revert_commit_begin` returns `Err` with code `conflict_state`; assert no editor message path taken.
    - Test (revert_abort recovers): after begin (REVERT_HEAD set), `revert_abort(path)` → assert `REVERT_HEAD` absent and `git status` clean (no staged revert changes).
    - Test (cleanup=strip on conflicted-revert body): conflicted revert resolved → revert_continue with a message that has a trailing `# Conflicts:` block → committed body has the # lines removed.
  </behavior>
  <action>
    Add a `#[cfg(test)] mod tests` block at the end of commit_actions.rs. Copy the `make_repo()` scaffold (TempDir + Repository::init, identity, commit helpers, state_map HashMap keyed by path string). Call the inner functions directly. Write the five RED tests above; they MUST fail because `revert_commit_begin`, `revert_continue`, `revert_abort`, `RevertBeginResult` do not exist. Commit RED: `test(76-02): failing tests for revert begin/continue/abort`.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib commit_actions 2>&1 | grep -qE "cannot find|error\[|test result: FAILED" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - commit_actions.rs contains `#[cfg(test)]` with a TempDir/Repository::init helper
    - Five tests present: clean default w/ full OID, revert_continue clears REVERT_HEAD, conflict→Err(conflict_state), revert_abort recovers, cleanup=strip on conflicted body
    - `cargo test --lib commit_actions` fails (RED) — symbols undefined
  </acceptance_criteria>
  <done>RED suite committed; failures are missing-symbol, not test logic.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 1: GREEN — implement revert_commit_begin / revert_continue / revert_abort; register in lib.rs</name>
  <files>src-tauri/src/commands/commit_actions.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/commands/commit_actions.rs (revert_commit_inner:143-171 — change args, preserve conflict_state branch :159-166; revert_commit wrapper ~320-340 for the spawn_blocking→cache.insert→emit shape)
    - src-tauri/src/commands/operation_state.rs (merge_abort_inner ~183-202 + wrapper ~410-429 for revert_abort; merge_continue_inner Some(msg) arm 158-165 for revert_continue)
    - src-tauri/src/lib.rs:144-165 (invoke_handler; revert_commit:145)
    - 76-PATTERNS.md §"revert_commit_begin"/"revert_continue"/"revert_abort" (lines 118-167), §"lib.rs handler registration" (lines 171-182), §"Shared Patterns" wrapper deviation (lines 262-276)
    - 76-RESEARCH.md §"Pattern: existing *_inner + wrapper" (--cleanup=strip args, lines 184-191), Pitfall 2 (revert cancel traps user), Pitfall 4 (begin must emit)
  </read_first>
  <behavior>
    - All five Task-0 tests pass (GREEN).
  </behavior>
  <action>
    1. `revert_commit_begin_inner`: change revert_commit_inner's args from `["revert", oid, "--no-edit"]` (:153) to `["revert", "--no-commit", oid]`. KEEP the `conflict_state` error branch (:159-166) verbatim — conflicted revert returns `Err(conflict_state)`, no editor. On clean success, read `std::fs::read_to_string(repo.path().join("MERGE_MSG")).ok()` (git writes the revert default there), rebuild graph, return `RevertBeginResult { graph, message }`. Preserve `.env("PATH", shell_env::system_path())`.
    2. `revert_commit_begin` async wrapper: copy revert_commit wrapper (~320-340). DEVIATION: inner returns `RevertBeginResult` not `GraphResult` — clone `.graph` for `cache.insert`, then `app.emit("repo-changed", path)` (REVERT_HEAD is now set — emit is mandatory so the banner surfaces even if the user cancels; RESEARCH Pitfall 2/4), then `Ok(result)`.
    3. `revert_continue_inner` + wrapper: mirror merge_continue's Some(msg) arm — `git commit -m <msg> --cleanup=strip` with `revert_error` code; `git commit -m` clears REVERT_HEAD (RESEARCH OQ-3 verified). Wrapper does spawn_blocking→cache.insert→emit (copy merge_continue wrapper, rename, `revert_error`).
    4. `revert_abort_inner` + wrapper: mirror merge_abort exactly — `git revert --abort`, `revert_error` code; rebuild graph; wrapper spawn_blocking→cache.insert→emit (copy merge_abort wrapper ~410-429).
    5. lib.rs: add `commands::commit_actions::revert_commit_begin,`, `commands::commit_actions::revert_continue,`, `commands::commit_actions::revert_abort,` to the invoke_handler list. DECISION: remove the old `commands::commit_actions::revert_commit,` line (:145) — frontend repoints to `revert_commit_begin` (Plan 03), so it goes unused. Before removing, grep confirms no other `invoke("revert_commit")` caller remains outside Plan 03's edit targets.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib commit_actions 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib commit_actions` passes (all revert tests GREEN)
    - `grep -n 'no-edit' src-tauri/src/commands/commit_actions.rs` returns NOTHING (D-01, OQ-4)
    - `grep -c 'cleanup=strip' src-tauri/src/commands/commit_actions.rs` >= 1 (on revert_continue)
    - `grep -n 'revert.*--abort\|"revert", "--abort"' src-tauri/src/commands/commit_actions.rs` matches (revert_abort exists)
    - `revert_commit_begin` async wrapper emits `repo-changed` after a successful (clean) begin (REVERT_HEAD now set); the emit is reached on the Ok path before returning the message (RESEARCH Pitfall 4 — cancel must leave a visible state)
    - conflict_state error branch preserved (grep `conflict_state` still present in commit_actions.rs)
    - `grep -n 'editor::prepare\|EditorHandle' src-tauri/src/commands/commit_actions.rs` returns NOTHING (D-01a)
    - lib.rs registers all three new commands; `revert_commit` line removed; `cargo build` succeeds
  </acceptance_criteria>
  <done>Revert backend GREEN; --no-edit removed; --cleanup=strip on finish; revert_abort recovery exists; emit on begin; commands registered. Commit GREEN: `feat(76-02): revert begin/continue/abort, cleanup=strip, recovery path`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend(IPC) → Rust command | oid + edited message cross from WebView into subprocess `git` args |
| Rust → `git` subprocess | argv array (not a shell string) to local git binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04 | Tampering | edited message → `git commit -m <msg>` (revert_continue) | mitigate | Single argv element via `Command::args(["commit","-m",msg,"--cleanup=strip"])`; no shell; leading `-` / metachars inert. RESEARCH Security V5. |
| T-76-05 | Tampering | oid → `git revert --no-commit <oid>` | accept | OID originates from the existing commit-graph selection (already validated); argv array, no shell. Local single-user GUI. |
| T-76-06 | Elevation | `git` subprocess PATH resolution | mitigate | Preserve `.env("PATH", shell_env::system_path())` on revert --no-commit, commit -m, revert --abort. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan. Audit N/A. |
</threat_model>

<verification>
- `cargo test --lib commit_actions` green.
- grep gates: no no-edit, cleanup=strip present, revert --abort present, conflict_state preserved, no editor::prepare.
- `cargo build` succeeds with `revert_commit` registration removed.
</verification>

<success_criteria>
MSG-03 (revert verbatim default + commit fidelity) and MSG-06 (begin leaves recoverable REVERT_HEAD + emits; revert_abort recovery) backend behavior proven against a temp git repo; commands registered; D-01a guard holds.
</success_criteria>

<output>
Create `.planning/phases/76-wire-messageeditor-into-merge-continue-merge-and-revert/76-02-SUMMARY.md` when done.
</output>
