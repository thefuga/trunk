---
phase: 66-commit-selection
plan: 03
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - src/lib/types.ts
  - src/components/CommitRow.svelte
  - src/components/CommitRow.test.ts
  - src/components/ReviewPanel.svelte
  - src/components/ReviewPanel.test.ts
autonomous: true
requirements: [SEL-03, SEL-04]
must_haves:
  truths:
    - "D-04: an in-session commit is visually marked on the HTML CommitRow via a theme CSS custom property (e.g. box-shadow inset accent or --color-review-row), driven by a new inSession prop — NEVER an inline literal color, and NEVER in the SVG lane pipeline (overlay-paths.ts / active-lanes.ts / overlay-dots)"
    - "D-01 support: CommitRow also accepts an isPendingBase prop driving a DISTINCT theme variable for the transient range-base highlight; both inSession and isPendingBase are owned here so Plan 04 only passes them at the call site (no CommitRow edit in Plan 04 — prevents a file conflict)"
    - "D-05: the ReviewPanel renders a MINIMAL commit list — short SHA + summary per row, in graph order, dedup'd (ordering already imposed server-side by list_session_commits); do not over-invest, this is the Phase 65 throwaway stub"
    - "D-07: each panel list row has an × / remove button calling remove_review_commit so the user can remove from the list they are looking at (SEL-03 from the panel)"
    - "SEL-04: the panel loads the list via list_session_commits and reloads on every session-changed for this repo's canonical path"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "SessionCommit interface mirroring the Rust struct"
      contains: "interface SessionCommit"
    - path: "src/components/CommitRow.svelte"
      provides: "inSession + isPendingBase props with theme-variable tint extending the existing selected/isSearchMatch style expression"
      contains: "inSession"
    - path: "src/components/ReviewPanel.svelte"
      provides: "Minimal session-commit list (short SHA + summary) with per-row × remove, loaded via list_session_commits and reloaded on session-changed"
      contains: "list_session_commits"
  key_links:
    - from: "ReviewPanel list row × button"
      to: "remove_review_commit command"
      via: "safeInvoke with {path, oid}"
      pattern: "remove_review_commit"
    - from: "ReviewPanel session-changed listener"
      to: "list_session_commits"
      via: "reload the list alongside reloadStatus"
      pattern: "list_session_commits"
    - from: "CommitRow inSession/isPendingBase"
      to: "theme CSS custom property"
      via: "extend the style= expression with var(--color-*)"
      pattern: "var\\(--color-"
---

<objective>
Add the D-04 in-graph membership marker (and the D-01 transient base highlight) as theme-variable tints on the plain HTML `CommitRow`, the `SessionCommit` TS interface, and the D-05 minimal commit list with D-07 per-row remove in the throwaway `ReviewPanel`. All driven by `list_session_commits` (Plan 02) and reloaded on `session-changed`.

Purpose: Make the selection VISIBLE — what is in the session (panel list + graph tint) and how to remove it (× button + graph toggle later). Own both CommitRow props here so Plan 04 (CommitGraph) only wires props at the call site and never touches CommitRow (avoids a Wave 2/3 file collision).
Output: SessionCommit interface; CommitRow inSession/isPendingBase props + tints + CommitRow.test.ts coverage; ReviewPanel list + × + reload + ReviewPanel.test.ts coverage.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-commit-selection/66-RESEARCH.md
@.planning/phases/66-commit-selection/66-PATTERNS.md
@.planning/phases/66-commit-selection/66-VALIDATION.md

<interfaces>
<!-- Rust SessionCommit (from Plan 01, Serialize-default snake_case) → TS interface: -->
  SessionCommit { oid: string; short_oid: string; summary: string }

<!-- Backend command this plan calls (from Plan 02): -->
  list_session_commits(path) -> SessionCommit[]   // Ok(list) when session active; Err code "no_session" when none
  remove_review_commit(path, oid) -> void          // emits session-changed

<!-- CommitRow existing props (CommitRow.svelte:18-43) to extend: -->
  selected?, isSearchMatch?, isCurrentMatch?, isSearchActive?  // booleans
  Row style expression at CommitRow.svelte:68 — the home for the new tints.
  NOTE: existing rgba(...) literals there are a pre-existing exception; the NEW
  marker MUST use a --color-* var (project rule, never inline a literal color).

<!-- ReviewPanel existing wiring (ReviewPanel.svelte) to extend: -->
  safeInvoke<T>(cmd, args)  from ../lib/invoke.js ; showToast from ../lib/toast.svelte.js
  reloadStatus() at :23 (mount $effect at :47) ; session-changed listener at :55-66
  status.canonical_path used to filter session-changed events
  sessionState === "active" branch (:73) — where the list + × belong (only when active)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionCommit interface + CommitRow inSession/isPendingBase tints</name>
  <files>src/lib/types.ts, src/components/CommitRow.svelte, src/components/CommitRow.test.ts</files>
  <read_first>
    - src/lib/types.ts (the file being modified; SessionStatus interface analog ~:323 and GraphCommit ~:55 for the snake_case field convention)
    - src/components/CommitRow.svelte (the file being modified; Props block :18-43 and the row style expression :61-72 — the selected/isSearchMatch/isCurrentMatch tint precedent to extend)
    - src/components/CommitRow.test.ts (the EXISTING test file to extend — it exists already despite VALIDATION.md saying otherwise; read its render/prop conventions)
    - .planning/phases/66-commit-selection/66-PATTERNS.md (§CommitRow.svelte — "Row-tint precedent (D-04 home)"; the box-shadow inset accent example; the D-04 SVG anti-pattern)
    - .planning/phases/66-commit-selection/66-RESEARCH.md (§"Pattern 4: D-04 marker on the HTML row"; Open Question 3 — isPendingBase as a second prop with a distinct theme var)
    - .claude/rules/commit-graph.md (the SVG-pipeline boundary — confirm the marker stays on the HTML row, NOT the SVG layers)
    - src/styles or the theme file where --color-* custom properties are defined (grep for --color-selected-row to find it; add --color-review-row / --color-review-pending-base there if reusing an existing var is not appropriate)
  </read_first>
  <behavior>
    - inSession tint: rendering CommitRow with inSession={true} applies a theme-variable-based marker (e.g. box-shadow: inset 3px 0 0 var(--color-accent), or background var(--color-review-row)); with inSession={false} it does not. The test asserts the style/class change, NOT a literal color value.
    - isPendingBase tint: CommitRow with isPendingBase={true} applies a DISTINCT theme-variable marker (different var from inSession); both can be off independently.
    - No inline literal color is introduced for either marker — only var(--color-*).
  </behavior>
  <action>
    Add `export interface SessionCommit { oid: string; short_oid: string; summary: string }` to src/lib/types.ts (snake_case fields, mirroring GraphCommit and the Rust struct).
    In CommitRow.svelte: add `inSession?: boolean` and `isPendingBase?: boolean` to the Props interface (:18-43) and to the destructured $props defaults (default false). Extend the existing `style="..."` expression (:68) to append, when inSession, a theme-variable marker (prefer `box-shadow: inset 3px 0 0 var(--color-accent);` so it composes with the existing background branches without fighting them) and, when isPendingBase, a DISTINCT theme variable (e.g. a different inset accent or a `--color-review-pending-base` background). If you introduce new vars (--color-review-row / --color-review-pending-base), define them in the theme CSS file alongside --color-selected-row. Do NOT touch overlay-paths.ts / active-lanes.ts / overlay-dots (D-04 anti-pattern).
    Extend CommitRow.test.ts with cases asserting the inSession and isPendingBase markers appear/disappear with the props and that no inline literal color is used for them. No fenced code blocks — see read_first for the exact style expression.
  </action>
  <verify>
    <automated>npx vitest run CommitRow</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "interface SessionCommit" src/lib/types.ts` returns the interface with oid/short_oid/summary.
    - `grep -c "inSession\|isPendingBase" src/components/CommitRow.svelte` >= 2 (props declared + used in style).
    - The D-04/D-01 markers use only `var(--color-*)` — `grep -n "inSession\|isPendingBase" src/components/CommitRow.svelte` shows no `rgb(` / `#` literal tied to these props.
    - No membership styling added to overlay-paths.ts / active-lanes.ts / the overlay-dots block (`git diff --name-only` shows none of those files touched).
    - `npx vitest run CommitRow` passes including the new inSession/isPendingBase cases.
  </acceptance_criteria>
  <done>CommitRow shows in-session and pending-base markers via theme variables on the HTML row (never the SVG pipeline, never an inline literal), with both props owned here so Plan 04 only passes them.</done>
</task>

<task type="auto">
  <name>Task 2: ReviewPanel minimal commit list + per-row remove (D-05, D-07, SEL-04)</name>
  <files>src/components/ReviewPanel.svelte, src/components/ReviewPanel.test.ts</files>
  <read_first>
    - src/components/ReviewPanel.svelte (the file being modified; reloadStatus :23, the mount $effect :47, the session-changed listener :55-66, the sessionState==="active" branch :73 where the list belongs, the End-Review button style :84-96 as the × button style precedent)
    - src/components/ReviewPanel.test.ts (the EXISTING test file to extend — read its safeInvoke mock + session-changed dispatch conventions, e.g. the listener test pattern)
    - src/lib/types.ts (the SessionCommit interface added in Task 1)
    - .planning/phases/66-commit-selection/66-PATTERNS.md (§ReviewPanel.svelte — "session-changed listener (reuse verbatim, swap reload fn)"; "Reload-on-mount + invoke + toast-on-error"; "Per-row × remove (D-07)")
    - .planning/phases/66-commit-selection/66-CONTEXT.md (D-05 minimal list / do not over-invest; D-07 × button)
  </read_first>
  <action>
    In ReviewPanel.svelte: add `let sessionCommits = $state<SessionCommit[]>([])` and an async `reloadCommits()` that calls `safeInvoke<SessionCommit[]>("list_session_commits", { path: repoPath })` and assigns the result (catch → set []; do NOT toast on no_session, since an inactive session is normal). Call reloadCommits from the existing mount $effect (alongside reloadStatus) and from the existing session-changed listener (alongside reloadStatus) so the list stays live. Render the list ONLY inside the `sessionState === "active"` branch (:73): one minimal row per SessionCommit showing `{short_oid}` + `{summary}` (graph order already imposed server-side, dedup'd) with an × button per row calling `safeInvoke("remove_review_commit", { path: repoPath, oid: c.oid })` then awaiting reload (or rely on the session-changed emit to reload). Style the × button with theme vars mirroring the End-Review button (:84-96); never inline a literal color. Keep markup minimal — this is the throwaway stub (D-05).
    Import SessionCommit from ../lib/types.js.
    Extend ReviewPanel.test.ts: (a) with a mocked list_session_commits returning two commits, the panel renders both short SHAs + summaries; (b) clicking a row × invokes remove_review_commit with the right oid; (c) dispatching a session-changed event for this repo's canonical_path triggers a list reload.
  </action>
  <verify>
    <automated>npx vitest run ReviewPanel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "list_session_commits\|remove_review_commit" src/components/ReviewPanel.svelte` >= 2.
    - reloadCommits is called from both the mount effect and the session-changed listener (verify by reading; both call sites present).
    - The list renders only under sessionState === "active".
    - `npx vitest run ReviewPanel` passes including: list renders short SHA + summary, × invokes remove_review_commit with the correct oid, session-changed triggers reload.
    - No inline literal color in the new list/× markup (`grep` for the × button shows var(--color-*) only).
  </acceptance_criteria>
  <done>The review panel shows the minimal in-session commit list in graph order with a working per-row × remove, loaded via list_session_commits and kept live by the session-changed listener (D-05, D-07, SEL-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReviewPanel → Rust commands | The panel calls `list_session_commits` / `remove_review_commit` over the already-validated IPC implemented in Plan 02; the panel passes only `repoPath` (the open repo) and OIDs it received from the backend list. No new untrusted input originates here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-03 | Information disclosure / Tampering | CommitRow marker + panel list rendering | accept | Pure presentation of data the backend already returns; no new trust boundary. The D-04 marker stays on the HTML row (never the SVG pipeline) per `.claude/rules/commit-graph.md`, which is a correctness rule, not a security one. |
| T-66-SC | Tampering (supply chain) | npm installs | accept | N/A — no packages installed this phase. |

No network, no secrets, no auth introduced by this plan.
</threat_model>

<verification>
- `npx vitest run CommitRow ReviewPanel` — all green.
- `npx svelte-check` — no new type errors (SessionCommit imported and used correctly).
</verification>

<success_criteria>
- In-session commits are visibly marked in the graph via a theme variable (D-04); the transient base highlight prop exists (D-01 support).
- The panel shows the minimal graph-ordered commit list with working per-row remove (D-05, D-07, SEL-04 visibility + SEL-03 from the panel).
- CommitRow props are owned here so Plan 04 only passes them — no shared-file conflict.
</success_criteria>

<output>
Create `.planning/phases/66-commit-selection/66-03-SUMMARY.md` when done.
</output>
