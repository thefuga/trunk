---
phase: 66-commit-selection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands/review.rs
autonomous: true
requirements: [SEL-01, SEL-02, SEL-03, SEL-04]
must_haves:
  truths:
    - "D-02: range is inclusive [base..tip] via revwalk push(tip) + hide(base.parent_id(0)); root-commit base hides nothing (interactive_rebase.rs:53 fallback) so base is always in the set"
    - "D-03: seeding a range UNIONS into the existing set (never replaces); hand-picked commits survive a later range seed; dedup keeps the set clean (success criterion #4)"
    - "D-08: a merge commit is selectable like any other commit — no is_merge gate in the range walk or add path; the diff-source-on-merge restriction is deferred to Phase 67"
    - "Invalid-range handling: base==tip is valid (set {base}); base-not-ancestor-of-tip and unrelated-history both reject with a TrunkError and leave the session unmutated"
    - "SEL-04 ordering is server-side: intersect the session set with the FULL cached GraphResult order; OIDs absent from the cache resolve via repo.find_commit fallback and are never silently dropped"
  artifacts:
    - path: "src-tauri/src/commands/review.rs"
      provides: "Pure testable selection helpers (validate_range, compute_range_oids, apply_add, apply_remove, union_dedup, intersect_graph_order) + SessionCommit struct + #[cfg(test)] unit tests with an in-process test-repo helper"
      contains: "fn validate_range"
  key_links:
    - from: "compute_range_oids"
      to: "git2 revwalk push/hide"
      via: "interactive_rebase.rs:40-56 pattern adapted (push tip, hide base.parent_id(0), root fallback)"
      pattern: "revwalk\\.(push|hide)"
    - from: "intersect_graph_order"
      to: "CommitCache GraphResult.commits"
      via: "filter session set against full cached graph order, find_commit fallback"
      pattern: "find_commit"
---

<objective>
Build the PURE, testable Rust selection core for the review-session commit set: range computation (git2 revwalk), invalid-range validation, set union/add/remove/dedup, and graph-ordered intersection for the SEL-04 list. These are plain functions with no Tauri state, unit-tested against an in-process git2 test repo, so the hard logic is proven before any IPC wiring (Plan 02) consumes it.

Purpose: De-risk the only genuinely new logic in Phase 66 (revwalk range + validation + set semantics) under TDD, isolated from mutex/IO orchestration. This fulfills the Wave 0 Rust test-repo requirement from 66-VALIDATION.md.
Output: New pure helpers + `SessionCommit` Serialize struct + a `#[cfg(test)]` test-repo helper and unit tests, all in `src-tauri/src/commands/review.rs`.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-commit-selection/66-CONTEXT.md
@.planning/phases/66-commit-selection/66-RESEARCH.md
@.planning/phases/66-commit-selection/66-PATTERNS.md
@.planning/phases/66-commit-selection/66-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted live from the codebase this session. -->

From src-tauri/src/git/types.rs (ReviewSession — Phase 65 keystone, UNCHANGED this phase):
  ReviewSession { schema_version, commits: Vec<String>, comments, draft_comment }
  GraphCommit { oid: String, short_oid: String, summary: String, is_merge: bool, ... }  (types.rs:55)
  GraphResult.commits: Vec<GraphCommit>  (the FULL graph-ordered list, built once at open)

From src-tauri/src/commands/review.rs (existing imports already present, lines 14-21):
  use crate::error::TrunkError;          // TrunkError::new(code, msg); impl From<git2::Error>
  use crate::git::types::ReviewSession;
  #[cfg(test)] mod tests at review.rs:276 (pure-fn tests, no Tauri state) — EXTEND this.

SessionCommit (NEW — Serialize-default snake_case to match GraphCommit/SessionStatus):
  SessionCommit { oid: String, short_oid: String, summary: String }
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Range walk + validation (RED → GREEN → REFACTOR)</name>
  <files>src-tauri/src/commands/review.rs</files>
  <read_first>
    - src-tauri/src/commands/review.rs (the file being modified; the existing #[cfg(test)] mod tests at :276, the TrunkError/import conventions at :14-21)
    - src-tauri/src/commands/interactive_rebase.rs (analog at :40-56 — the VERIFIED push/hide range walk WITH the root-commit "don't hide anything" branch at :53)
    - .planning/phases/66-commit-selection/66-RESEARCH.md (§"Code Examples / Detecting an invalid range" — the full validate_range helper; §"Pattern 1: Range walk"; Pitfall 1)
    - .planning/phases/66-commit-selection/66-PATTERNS.md (§review.rs "Range walk — inclusive [base..tip]")
    - src-tauri/src/git/review_store.rs (header note at :14 — tests inject a tempfile::TempDir; the established Rust test idiom)
  </read_first>
  <behavior>
    First build the in-process test-repo helper (Wave 0): tempfile::TempDir + git2::Repository::init creating a known topology — a linear chain AND a merge commit AND two unrelated roots (for the unrelated-history case). tempfile = "3" is already in dev-dependencies. No existing test-repo helper was found in src-tauri tests, so create one in the #[cfg(test)] module.
    - seed_range_inclusive: compute_range_oids(repo, base, tip) for a linear chain returns BOTH base and tip OIDs (inclusive [base..tip])
    - seed_range_root_base: when base is the ROOT commit (parent_count()==0), the walk hides nothing and the full ancestry through tip is returned (no panic on parent_id(0))
    - seed_range_base_eq_tip: validate_range(repo, x, x) is Ok and compute_range_oids yields exactly {x}
    - seed_range_rejects_non_ancestor: validate_range where base is NOT an ancestor of tip returns Err (graph_descendant_of(tip, base) == false), no mutation
    - seed_range_rejects_unrelated: validate_range across two unrelated roots returns Err (merge_base errors NotFound)
    - merge_commit_selectable: a merge commit can be passed as base or tip and appears in the computed range (D-08 — no is_merge gate)
  </behavior>
  <action>
    Follow strict RED → GREEN → REFACTOR per @/Users/joaofnds/code/trunk/.claude/get-shit-done/references/tdd.md.
    RED: write the test-repo helper + the six failing tests above; run, confirm they fail to compile/assert; commit `test(66-01): add failing range-walk + validation tests`.
    GREEN: implement two pure fns in review.rs: `validate_range(repo: &git2::Repository, base: git2::Oid, tip: git2::Oid) -> Result<(), TrunkError>` (order: base==tip → Ok early; merge_base(base,tip) Err → TrunkError code "unrelated_history"; graph_descendant_of(tip, base) false → TrunkError code "bad_range"; note graph_descendant_of(x,x)==false so the base==tip early return is REQUIRED) and `compute_range_oids(repo: &git2::Repository, base: git2::Oid, tip: git2::Oid) -> Result<Vec<String>, TrunkError>` (revwalk set_sorting TOPOLOGICAL|TIME, push(tip), find_commit(base); if parent_count()>0 hide(parent_id(0)) else hide nothing — D-02 inclusive + root fallback). Do NOT add any `is_merge` guard (D-08). Run tests green; commit `feat(66-01): implement range walk + validation`.
    REFACTOR (if needed): dedup error-mapping, keep helpers pure (no AppHandle/State params); run `cd src-tauri && cargo test --lib review`; commit `refactor(66-01): tidy range helpers` only if changes made.
    No fenced code blocks here — implementation detail lives in the read_first files (interactive_rebase.rs:40-56 and RESEARCH §Code Examples).
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib seed_range_inclusive seed_range_root_base seed_range_base_eq_tip seed_range_rejects_non_ancestor seed_range_rejects_unrelated merge_commit_selectable</automated>
  </verify>
  <acceptance_criteria>
    - All six named tests pass via the command above.
    - `grep -n "fn validate_range\|fn compute_range_oids" src-tauri/src/commands/review.rs` returns both signatures, neither taking a Tauri `State`/`AppHandle` param (pure).
    - No `is_merge` token appears in compute_range_oids/validate_range bodies (D-08 — `grep -v '^//' | grep -c is_merge` over the new helper region == 0).
    - At least one RED commit and one GREEN commit exist in `git log --oneline` for this task.
  </acceptance_criteria>
  <done>Range computation and validation are pure, unit-tested against a real in-process repo (linear + merge + unrelated roots), with inclusive [base..tip], root-commit fallback, base==tip, non-ancestor, and unrelated-history all proven; merge commits selectable.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Set union / add / remove / dedup (RED → GREEN → REFACTOR)</name>
  <files>src-tauri/src/commands/review.rs</files>
  <read_first>
    - src-tauri/src/commands/review.rs (the file being modified; the test-repo helper from Task 1 is reusable but these tests need only Vec<String> set logic, no repo)
    - .planning/phases/66-commit-selection/66-RESEARCH.md (§"Pattern 2: Set union + dedup"; §"Don't Hand-Roll")
    - .planning/phases/66-commit-selection/66-PATTERNS.md (§review.rs "Set union + dedup (D-03)")
    - .planning/phases/66-commit-selection/66-CONTEXT.md (D-03 union semantics; D-06 single toggle = add when absent / remove when present)
  </read_first>
  <behavior>
    - add_commit_idempotent: apply_add(commits, oid) appends when absent; a second apply_add of the same oid is a no-op (no duplicate) — SEL-02
    - remove_commit: apply_remove(commits, oid) deletes exactly the one matching oid; removing an oid not in the set is a safe no-op — SEL-03
    - seed_range_unions_dedups: union_dedup(existing_commits, range_oids) preserves pre-existing hand-picked commits, adds the range, and contains no duplicates — D-03 + success criterion #4
  </behavior>
  <action>
    RED: write the three failing tests on pure Vec<String> helpers; commit `test(66-01): add failing set-op tests`.
    GREEN: implement `apply_add(&mut Vec<String>, &str)` (push if !contains), `apply_remove(&mut Vec<String>, &str)` (retain != oid), and `union_dedup(existing: &[String], incoming: Vec<String>) -> Vec<String>` (HashSet union; store order is irrelevant — list_session_commits re-imposes graph order on read per Task 3). These operate on the bare `Vec<String>` (or `&mut ReviewSession.commits`); keep them pure. Commit `feat(66-01): implement set union/add/remove helpers`.
    REFACTOR if warranted; commit only if changed.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib add_commit_idempotent remove_commit seed_range_unions_dedups</automated>
  </verify>
  <acceptance_criteria>
    - The three named tests pass.
    - `grep -n "fn apply_add\|fn apply_remove\|fn union_dedup" src-tauri/src/commands/review.rs` returns all three; none take a Tauri State/AppHandle.
    - RED and GREEN commits present for this task in `git log --oneline`.
  </acceptance_criteria>
  <done>Add/remove/union set semantics are pure and unit-tested: add is idempotent, remove is exact + safe-on-missing, range seeding unions without replacing and dedups (D-03).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Graph-ordered intersection for SEL-04 (RED → GREEN → REFACTOR)</name>
  <files>src-tauri/src/commands/review.rs</files>
  <read_first>
    - src-tauri/src/commands/review.rs (the file being modified; reuse the Task 1 test-repo helper)
    - src-tauri/src/git/types.rs (GraphCommit {oid, short_oid, summary} at :55; GraphResult.commits — the 1:1 map to SessionCommit)
    - src-tauri/src/git/graph.rs (walk_commits — the graph-order authority; useful for building the GraphResult test fixture)
    - src-tauri/src/commands/repo.rs (lines 32,45 — CommitCache populated with walk_commits(.., 0, usize::MAX), the FULL graph order)
    - .planning/phases/66-commit-selection/66-RESEARCH.md (§"Pattern 3: Graph-ordered read"; Open Question 1 RESOLVED — not a pagination problem; orphan fallback recommendation)
    - .planning/phases/66-commit-selection/66-PATTERNS.md (§review.rs "list_session_commits — graph-ordered read")
  </read_first>
  <behavior>
    - list_session_commits_graph_order: intersect_graph_order(session_commits, &GraphResult, &repo) returns the selected commits in the SAME order as the cached graph, deduplicated, as Vec<SessionCommit>{oid, short_oid, summary}
    - list_session_commits_orphan_fallback: a selected OID NOT present in the GraphResult is resolved via repo.find_commit and APPENDED (never silently dropped); if even find_commit fails, it is included with a placeholder summary (lean: "(unavailable)") rather than omitted
  </behavior>
  <action>
    RED: build a GraphResult fixture from the test repo (or reuse walk output) plus a session set that includes one in-graph OID and one orphan OID; write the two failing tests; commit `test(66-01): add failing graph-order intersect tests`.
    GREEN: implement `intersect_graph_order(commits: &[String], graph: &GraphResult, repo: &git2::Repository) -> Vec<SessionCommit>`: build a HashSet of the wanted OIDs, iterate graph.commits in order filtering by membership and mapping GraphCommit→SessionCommit (clone oid/short_oid/summary), then append any session OIDs not seen via git2::Oid::from_str + repo.find_commit (short_oid = first 7 chars, summary from commit.summary() or "(unavailable)"). Also add the `SessionCommit` Serialize struct (snake_case default, derives Debug, Serialize, Clone) at module top. Commit `feat(66-01): implement graph-ordered session-commit intersection`.
    REFACTOR if warranted.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib list_session_commits_graph_order list_session_commits_orphan_fallback</automated>
  </verify>
  <acceptance_criteria>
    - Both named tests pass.
    - `grep -n "fn intersect_graph_order\|struct SessionCommit" src-tauri/src/commands/review.rs` returns both.
    - SessionCommit has exactly fields oid, short_oid, summary with Serialize-default (no rename_all) — `grep -A4 "struct SessionCommit" src-tauri/src/commands/review.rs` shows snake_case fields.
    - The orphan test proves a session OID absent from the graph still appears in the output (never dropped).
    - RED and GREEN commits present.
  </acceptance_criteria>
  <done>SEL-04 ordering is proven server-side: the session set, intersected with the full cached graph order, yields a graph-ordered dedup'd Vec<SessionCommit>, with a find_commit fallback that never drops a selected commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri IPC → these helpers | Plan 02 wraps these pure fns in #[tauri::command]s; the OID/path strings cross the IPC boundary there. This plan's helpers receive already-parsed git2::Oid / &[String] from the wrapper — the boundary check lives in Plan 02, but the validation LOGIC (validate_range) is implemented and proven here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering / Denial of Service | malformed or hostile base/tip OID in `seed_review_range` | mitigate | `validate_range` rejects base-not-ancestor (`bad_range`) and unrelated-history (`unrelated_history`) with a TrunkError before any walk; `compute_range_oids` uses `parent_count()` to avoid panicking on `parent_id(0)` for root commits. Proven by `seed_range_rejects_non_ancestor` / `seed_range_rejects_unrelated` / `seed_range_root_base`. |
| T-66-SC | Tampering (supply chain) | npm/cargo installs | accept | N/A — Phase 66 installs no packages (RESEARCH §Package Legitimacy Audit). All crates already vendored. |

No secrets, no network, no auth in this plan — pure git2 set computation on the user's own repo.
</threat_model>

<verification>
- `cd src-tauri && cargo test --lib review` — all selection unit tests green.
- `cd src-tauri && cargo clippy` — no new warnings on the added helpers.
</verification>

<success_criteria>
- All eleven Rust unit tests named in 66-VALIDATION.md that belong to the pure core pass (range: inclusive/root/eq/non-ancestor/unrelated/merge-selectable; set: add-idempotent/remove/union-dedup; list: graph-order/orphan-fallback).
- Helpers are pure (no Tauri state) and reusable by Plan 02's thin commands.
- `SessionCommit` struct exists with the frozen field shape for the frontend interface (Plan 03).
</success_criteria>

<output>
Create `.planning/phases/66-commit-selection/66-01-SUMMARY.md` when done.
</output>
