---
phase: 66-commit-selection
plan: 04
type: execute
wave: 3
depends_on: ["66-02", "66-03"]
files_modified:
  - src/components/CommitGraph.svelte
autonomous: false
requirements: [SEL-01, SEL-02, SEL-03]
must_haves:
  truths:
    - "D-01: range is seeded by TWO right-clicks on the graph — 'Set as review base' sets a transient pendingBase; a second right-click shows 'Add range to review' (only when pendingBase is set) which calls seed_review_range and clears pendingBase. No modal. The pending base is shown via the CommitRow isPendingBase marker (from Plan 03) and cleared after the range is added or cancelled."
    - "D-06: the context menu carries a SINGLE toggle item — 'Add to review' when the commit is not in the session, 'Remove from review' when it is — covering SEL-02 and SEL-03 from the graph. The item is injected ONLY when a session is active."
    - "D-08: the Add/Remove toggle and the range items carry NO `enabled: !commit.is_merge` gate — merges are selectable (contrast Cherry-pick/Revert at CommitGraph.svelte:680-693 which DO gate). The diff-source-on-merge restriction is deferred to Phase 67."
    - "sessionActive gating: CommitGraph derives session-active from get_review_session_status (state === 'active'), NOT from reviewPanelOpen — VERIFIED A1: reviewPanelOpen (App.svelte:58) tracks panel VISIBILITY only, toggled by the review-toggle event, not session existence. Membership (sessionOids) loads from list_session_commits."
    - "sessionOids is event-driven $state<Set<string>> reloaded from list_session_commits on mount and on every session-changed for this repo; the Set is reassigned (new Set(...)) so Svelte 5 reactivity fires (Pitfall 5). inSession and isPendingBase are passed to CommitRow at the existing call site — CommitRow itself is NOT edited here (owned by Plan 03)."
  artifacts:
    - path: "src/components/CommitGraph.svelte"
      provides: "Two-right-click range gesture (D-01) + single Add/Remove toggle (D-06) in showCommitContextMenu, session-active gating, sessionOids Set, pendingBase state, and inSession/isPendingBase props passed to CommitRow"
      contains: "seed_review_range"
  key_links:
    - from: "context menu review items"
      to: "seed_review_range / add_review_commit / remove_review_commit"
      via: "safeInvoke with {path, baseOid, tipOid} or {path, oid}"
      pattern: "(seed_review_range|add_review_commit|remove_review_commit)"
    - from: "sessionOids / sessionActive"
      to: "list_session_commits + get_review_session_status"
      via: "reload on mount and on session-changed; reassign Set"
      pattern: "list_session_commits"
    - from: "CommitRow call site"
      to: "inSession / isPendingBase props"
      via: "sessionOids.has(commit.oid) and pendingBase === commit.oid"
      pattern: "inSession=\\{"
---

<objective>
Wire the graph side of selection in `CommitGraph.svelte`: the D-01 two-right-click range gesture with a transient base highlight, the D-06 single Add/Remove toggle, session-active gating (correctly sourced from session status, NOT the panel-open flag), the event-driven `sessionOids` Set, and passing `inSession`/`isPendingBase` to `CommitRow` at the existing call site.

Purpose: Let the user define the review set FROM the graph where they browse — seed a range by two right-clicks, hand-pick/remove via the toggle, and see what is in via the marker. This closes SEL-01/02/03 from the graph and feeds the SEL-04 marker.
Output: CommitGraph context-menu review items + state + prop wiring. A human-verify checkpoint covers the native-menu gestures vitest cannot drive.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-commit-selection/66-RESEARCH.md
@.planning/phases/66-commit-selection/66-PATTERNS.md
@.planning/phases/66-commit-selection/66-02-SUMMARY.md
@.planning/phases/66-commit-selection/66-03-SUMMARY.md

<interfaces>
<!-- Backend commands (Plan 02): -->
  seed_review_range(path, baseOid, tipOid) -> void   // validates + walks + unions + emits
  add_review_commit(path, oid) -> void  /  remove_review_commit(path, oid) -> void
  list_session_commits(path) -> SessionCommit[]      // Err code "no_session" when inactive
  get_review_session_status(path) -> SessionStatus { state: "active"|"resume-available"|"none", canonical_path, file_exists }  // Phase 65, review.rs

<!-- CommitRow props (Plan 03, do NOT edit CommitRow here — only pass these): -->
  inSession?: boolean ; isPendingBase?: boolean

<!-- CommitGraph existing wiring (VERIFIED this session): -->
  repoPath prop (:57,69) ; safeInvoke (:27) ; import MenuItem/PredefinedMenuItem/Submenu/Menu (:8)
  showCommitContextMenu(e, commit) at :567 — builds conditional item arrays
    (mergeRebaseItems / fastForwardItems / interactiveRebaseItems spread into Menu.new items at :643)
  Cherry-pick/Revert with `enabled: !commit.is_merge` at :680-693 — the D-08 ANTI-PATTERN (do NOT copy onto review items)
  searchMatchOids = $derived(new Set(...)) at :293 — the Set-construction idiom (but sessionOids is event-driven $state, NOT $derived)
  <CommitRow {commit} ... isSearchMatch={searchMatchOids.has(commit.oid)} ... /> at ~:1806 — the prop-passing call site

<!-- A1 RESOLVED: reviewPanelOpen lives in App.svelte:58, toggled by the "review-toggle" event (App.svelte:553-557). It is PANEL VISIBILITY, not session-active. Do NOT use it for D-06 gating. Use get_review_session_status state === "active". -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sessionOids/sessionActive state + pendingBase + CommitRow prop wiring</name>
  <files>src/components/CommitGraph.svelte</files>
  <read_first>
    - src/components/CommitGraph.svelte (the file being modified; the searchMatchOids $derived at :293 for the Set idiom; the repoPath prop :57,69; the CommitRow call site ~:1806; the existing safeInvoke usage :307-308)
    - src/components/ReviewPanel.svelte (the session-changed listener pattern at :55-66 to mirror for sessionOids reload; the status.canonical_path filter)
    - .planning/phases/66-commit-selection/66-RESEARCH.md (§"Frontend: refresh session Set on change"; Pitfall 5 stale Set; Pitfall 4 + A1 — gate on session-active, not panel-open)
    - .planning/phases/66-commit-selection/66-PATTERNS.md (§CommitGraph.svelte — "Membership Set"; "Prop wiring to CommitRow")
  </read_first>
  <action>
    Add three pieces of reactive state to CommitGraph: `let sessionOids = $state<Set<string>>(new Set())`, `let sessionActive = $state(false)`, and `let pendingBase = $state<string | null>(null)` (the transient D-01 range base). Add an async `reloadSession()` that (a) calls `get_review_session_status({path: repoPath})` and sets `sessionActive = status.state === "active"`, and (b) if active, calls `list_session_commits({path: repoPath})` and sets `sessionOids = new Set(list.map(c => c.oid))` (REASSIGN so reactivity fires — Pitfall 5); on error/inactive set sessionActive=false and sessionOids=new Set(). Call reloadSession from a mount $effect and from a session-changed listener filtered by canonical path (mirror ReviewPanel.svelte:55-66). Clear pendingBase when sessionActive becomes false.
    At the CommitRow call site (~:1806) add `inSession={sessionOids.has(commit.oid)}` and `isPendingBase={pendingBase === commit.oid}`. Do NOT edit CommitRow.svelte (props owned by Plan 03).
    No fenced code blocks — see read_first for the exact idioms.
  </action>
  <verify>
    <automated>npx svelte-check 2>&1 | grep -i "CommitGraph" | grep -ic error; grep -c "sessionOids\|sessionActive\|pendingBase" src/components/CommitGraph.svelte</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sessionOids\|sessionActive\|pendingBase" src/components/CommitGraph.svelte` >= 6 (declarations + uses).
    - sessionActive is derived from get_review_session_status state === "active" — `grep -n "get_review_session_status" src/components/CommitGraph.svelte` returns at least one call; reviewPanelOpen is NOT referenced for gating (`grep -c reviewPanelOpen src/components/CommitGraph.svelte` == 0).
    - sessionOids is reassigned as `new Set(...)` (not mutated in place) — verify by reading reloadSession.
    - CommitRow call site passes inSession and isPendingBase — `grep -c "inSession={\|isPendingBase={" src/components/CommitGraph.svelte` == 2.
    - svelte-check reports no new CommitGraph errors (the first grep, error count, == 0).
  </acceptance_criteria>
  <done>CommitGraph holds live session membership (sessionOids) and session-active state (from status, not panel-open) plus the transient pendingBase, and passes the marker props to CommitRow without editing CommitRow.</done>
</task>

<task type="auto">
  <name>Task 2: Range gesture (D-01) + Add/Remove toggle (D-06) in the context menu</name>
  <files>src/components/CommitGraph.svelte</files>
  <read_first>
    - src/components/CommitGraph.svelte (the file being modified; showCommitContextMenu :567; the conditional item-array pattern — mergeRebaseItems/fastForwardItems spread at :643; the MenuItem build style at :648-659; the Copy SHA item; the D-08 ANTI-PATTERN Cherry-pick/Revert `enabled: !commit.is_merge` at :680-693)
    - .planning/phases/66-commit-selection/66-RESEARCH.md (§"Pattern 5: D-06 toggle in the native context menu"; Open Question 3 transient base; the D-08 anti-pattern note)
    - .planning/phases/66-commit-selection/66-PATTERNS.md (§CommitGraph.svelte — "Context menu Add/Remove toggle (D-06)"; "DO NOT COPY — merge gating")
    - .planning/phases/66-commit-selection/66-CONTEXT.md (D-01 two-right-click flow + transient highlight cleared on completion/cancel; D-06 single toggle; D-08 merges selectable)
    - .planning/phases/66-commit-selection/66-VALIDATION.md (§Manual-Only Verifications — the two-right-click gesture + toggle label flip are human-verified)
  </read_first>
  <action>
    In showCommitContextMenu, build a conditional `reviewItems` array (mirroring the mergeRebaseItems spread pattern), injected into Menu.new items ONLY when sessionActive is true. Contents:
    - The D-06 toggle: a single MenuItem whose text is `sessionOids.has(commit.oid) ? "Remove from review" : "Add to review"` and whose action calls safeInvoke(remove_review_commit | add_review_commit, {path: repoPath, oid: commit.oid}) inside an explicit try/catch that on error calls showToast(err.message ?? "...", "error") — NOTE: safeInvoke does NOT auto-toast (it parses+rethrows TrunkError); CommitGraph already imports showToast and uses this exact pattern at :540/:550 for merge/rebase. A bare `.catch(()=>{})` would silently swallow errors. NO `enabled: !commit.is_merge` (D-08).
    - The D-01 range gesture as TWO context-dependent items: when pendingBase is null, a "Set as review base" item that sets `pendingBase = commit.oid` (the CommitRow marker from Task 1 shows it); when pendingBase is non-null, an "Add range to review" item that calls safeInvoke("seed_review_range", {path: repoPath, baseOid: pendingBase, tipOid: commit.oid}) inside an explicit try/catch — on success and on error set pendingBase = null (the gesture is done either way); on error call showToast(err.message ?? "Failed to seed range", "error") so an invalid range (bad_range / unrelated_history from Plan 01) is user-visible (human-verify step 7 depends on this). Also offer a "Clear review base" / cancel affordance when pendingBase is set so the user can cancel (clears pendingBase). NO is_merge gate on any of these (D-08). Place a Separator after the review items, matching the existing menu structure.
    Keep the items minimal and consistent with the existing MenuItem build style. All three review invocations (seed/add/remove) use the explicit try/catch + showToast pattern above — never a bare .catch(()=>{}) — because safeInvoke rethrows rather than toasts.
    No fenced code blocks — RESEARCH §Pattern 5 and PATTERNS §"Context menu Add/Remove toggle" carry the exact item shape.
  </action>
  <verify>
    <automated>npx svelte-check 2>&1 | grep -i "CommitGraph" | grep -ic error; grep -c "seed_review_range\|add_review_commit\|remove_review_commit\|Set as review base\|Add range to review" src/components/CommitGraph.svelte</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Add to review\|Remove from review" src/components/CommitGraph.svelte` >= 2 (toggle both labels present).
    - `grep -c "Set as review base\|Add range to review" src/components/CommitGraph.svelte` == 2 (the two-step range gesture).
    - The review items carry NO `enabled: !commit.is_merge` — read the reviewItems block and confirm; `grep` shows the is_merge gate only on the pre-existing Cherry-pick/Revert items.
    - The reviewItems array is gated on sessionActive (spread only when active) — verify by reading.
    - pendingBase is set by "Set as review base" and cleared after "Add range to review" — verify by reading both actions.
    - Review-command errors are user-visible: each review safeInvoke is wrapped in try/catch → showToast (not a bare .catch) — `grep -c "showToast" src/components/CommitGraph.svelte` increases by at least the number of review commands wired here; invalid-range seed shows a toast (human-verify step 7).
    - svelte-check reports no new CommitGraph errors.
  </acceptance_criteria>
  <done>The graph context menu offers the two-right-click range gesture (with transient base highlight via the CommitRow marker) and the single Add/Remove toggle, only when a session is active, with no merge gating (SEL-01/02/03 from the graph, D-01/D-06/D-08).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the native-menu selection gestures (D-01/D-04/D-06/D-08)</name>
  <files>src/components/CommitGraph.svelte</files>
  <action>Run `just check` (must be green) then `just dev`, and walk the human through the verification steps in how-to-verify below. This checkpoint exists because the native Tauri Menu.popup() gestures and the transient pending-base highlight cannot be driven by vitest (66-VALIDATION.md §Manual-Only Verifications). Do not mark the phase done until the human confirms.</action>
  <what-built>The graph-side selection UX: two-right-click range seeding with a transient base highlight (D-01), the single Add/Remove context-menu toggle (D-06), merge commits selectable (D-08), the in-session row marker (D-04, from Plan 03), and the panel list staying in sync. These rely on the native Tauri Menu.popup() and transient frontend state that vitest cannot drive (per 66-VALIDATION.md §Manual-Only Verifications).</what-built>
  <how-to-verify>
    1. `just dev`, open a repo with a linear stretch AND at least one merge commit, and start a code review session (View menu → Start Code Review; the panel opens).
    2. Right-click a commit → "Set as review base". Confirm the base row shows the transient pending-base highlight.
    3. Right-click a LATER (descendant) commit → "Add range to review". Confirm: the panel list now shows the inclusive [base..tip] commits (both endpoints included) in graph order with no duplicates, and the pending-base highlight clears.
    4. Right-click a commit NOT in the session → confirm the toggle reads "Add to review"; click it; confirm the row gets the in-session marker and it appears in the panel list.
    5. Right-click a commit ALREADY in the session → confirm the toggle reads "Remove from review"; click it; confirm the marker and the panel row both disappear.
    6. Right-click a MERGE commit → confirm "Add to review" is ENABLED (not greyed), add it, confirm it lands in the set (D-08). Also confirm it can be a range base/tip.
    7. Seed a range where base is NOT an ancestor of tip (pick two unrelated/sibling commits) → confirm a toast appears (e.g. "Base is not an ancestor of tip" / "share no history") and the session set is unchanged.
    8. In the panel, click a row's × → confirm the commit is removed from both the list and the graph marker.
    9. (Optional) Open the same repo in a second tab and confirm add/remove in one reflects in the other (session-changed sync).
  </how-to-verify>
  <verify>
    <human-check>All nine steps above behave as described — range is inclusive [base..tip] and dedup'd, toggle label flips on membership, merges are selectable, invalid range toasts without mutating, panel × removes from list and graph.</human-check>
  </verify>
  <done>Human has confirmed (typed "approved") that the two-right-click range gesture, the Add/Remove toggle, merge selectability, invalid-range rejection, the in-session marker, and panel-× removal all work in the running app.</done>
  <resume-signal>Type "approved" or describe any mismatch (wrong range, merge greyed out, missing marker, toast not shown, lost write on rapid clicks).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CommitGraph context menu → Rust commands | The menu calls seed_review_range / add_review_commit / remove_review_commit over the validated IPC from Plan 02. Args are repoPath (open repo) and OIDs taken from the rendered graph commits — no new free-form untrusted input originates in this UI layer. Range validation and RMW serialization are enforced backend-side (Plans 01/02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering / DoS | invalid range seeded from the two-click gesture | mitigate | Validation is enforced backend-side (validate_range, Plan 01); the UI surfaces the TrunkError toast via safeInvoke and clears pendingBase so a bad gesture leaves the set unchanged. |
| T-66-02 | Tampering (integrity) | rapid add/remove clicks racing | mitigate | Serialized backend-side under the ReviewSessionsState mutex (Plan 02); the UI relies on session-changed to resync sessionOids (Pitfall 5 reassign). No client-side write path that could lose state. |
| T-66-04 | Elevation / confused-deputy | toggle shown with no active session | mitigate | reviewItems are injected ONLY when sessionActive (from get_review_session_status state === "active"), NOT the panel-open flag (A1) — prevents invoking selection commands with no session. |
| T-66-SC | Tampering (supply chain) | npm installs | accept | N/A — no packages installed this phase. |

No network, no secrets, no auth introduced by this plan.
</threat_model>

<verification>
- `npx svelte-check` — no new errors.
- `just check` — full suite green (fmt, biome, svelte-check, clippy, cargo-test, vitest) before the human-verify checkpoint resolves.
- Human-verify checkpoint covers the native-menu gestures and transient highlight.
</verification>

<success_criteria>
- User can seed a range by two right-clicks (D-01), inclusive [base..tip], with a transient base highlight (SEL-01).
- User can add/remove a commit via the single context-menu toggle (D-06), merges included (D-08) (SEL-02/SEL-03).
- The toggle only appears with an active session, gated on session status not panel visibility (A1).
- In-session commits are marked in the graph and stay in sync via session-changed.
</success_criteria>

<output>
Create `.planning/phases/66-commit-selection/66-04-SUMMARY.md` when done.
</output>
