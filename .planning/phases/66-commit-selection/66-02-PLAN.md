---
phase: 66-commit-selection
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - src-tauri/src/commands/review.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [SEL-01, SEL-02, SEL-03, SEL-04]
must_haves:
  truths:
    - "D-03: seed_review_range UNIONS the computed range into the existing session.commits (never replaces) and dedups, via the Plan 01 union_dedup helper; one walk, one save, one emit per gesture (never decomposed into N adds)"
    - "RMW concurrency: add/remove/seed are read-modify-write — the ENTIRE read→mutate→save_session→map-write sequence is serialized under the ReviewSessionsState mutex so two rapid clicks cannot lose a write (NOT the Phase 65 create/delete _inner shape, which reads no prior state)"
    - "Dual path-keying: each command uses the CANONICAL path (canonical_repo_path) for review_store/ReviewSessionsState and the RAW path arg for CommitCache.get() — both derived from the same incoming path"
    - "list_session_commits distinguishes 'no active session' (TrunkError code no_session) from 'repo not open' (not_open) so the frontend (Plan 04) can branch on session-active vs error"
    - "every mutation emits a single session-changed event with the canonical path string, mirroring the Phase 65 lifecycle commands"
  artifacts:
    - path: "src-tauri/src/commands/review.rs"
      provides: "Four #[tauri::command] wrappers (seed_review_range, add_review_commit, remove_review_commit, list_session_commits) orchestrating the Plan 01 pure helpers under the sessions mutex + a selection_rmw_serialized integration test"
      exports: ["seed_review_range", "add_review_commit", "remove_review_commit", "list_session_commits"]
    - path: "src-tauri/src/lib.rs"
      provides: "Registration of the four new commands in the invoke_handler generate_handler! block"
      contains: "commands::review::seed_review_range"
  key_links:
    - from: "add_review_commit / remove_review_commit / seed_review_range"
      to: "ReviewSessionsState mutex"
      via: "lock held across read→mutate→save_session→map-write (Pitfall 2)"
      pattern: "sessions\\.0\\.lock"
    - from: "list_session_commits"
      to: "CommitCache (raw path) + review_store session (canonical path)"
      via: "intersect_graph_order over the cached GraphResult"
      pattern: "intersect_graph_order"
    - from: "lib.rs invoke_handler"
      to: "the four commands"
      via: "generate_handler! registration after line 127"
      pattern: "commands::review::(seed|add|remove|list)_"
---

<objective>
Wrap the Plan 01 pure selection helpers in four `#[tauri::command]`s that mutate the persisted review session, serialize the read-modify-write under the `ReviewSessionsState` mutex, persist via the Phase 65 atomic store, and emit `session-changed`; then register them in `lib.rs`. This is the IPC surface SEL-01..04 ride on.

Purpose: Expose the proven set logic to the frontend with correct concurrency (no lost writes on rapid clicks) and correct dual path-keying. Add the one integration test that requires real Tauri-state orchestration (`selection_rmw_serialized`).
Output: Four commands + `SessionCommit` already defined in Plan 01; registration in `lib.rs`; RMW serialization test.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-commit-selection/66-RESEARCH.md
@.planning/phases/66-commit-selection/66-PATTERNS.md
@.planning/phases/66-commit-selection/66-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (this plan depends on it): -->
  fn validate_range(repo, base, tip) -> Result<(), TrunkError>
  fn compute_range_oids(repo, base, tip) -> Result<Vec<String>, TrunkError>
  fn apply_add(&mut Vec<String>, &str)  /  fn apply_remove(&mut Vec<String>, &str)
  fn union_dedup(existing: &[String], incoming: Vec<String>) -> Vec<String>
  fn intersect_graph_order(commits, &GraphResult, &git2::Repository) -> Vec<SessionCommit>
  struct SessionCommit { oid, short_oid, summary }   // Serialize-default snake_case

<!-- Existing Phase 65 plumbing to mirror/reuse (VERIFIED in review.rs): -->
  fn canonical_repo_path(path: &str, state_map: &HashMap<String,PathBuf>) -> Result<PathBuf, TrunkError>  // review.rs:50
  fn resolve_data_dir(app: &AppHandle) -> Result<PathBuf, String>  // review.rs:143
  review_store::save_session(&data_dir, &canonical, &ReviewSession) -> Result<_, TrunkError>  // atomic tmp+rename (D-10)
  State<'_, RepoState>  (state.0: Mutex<HashMap<String, PathBuf>>)  // raw-path keyed
  State<'_, ReviewSessionsState>  (sessions.0: Mutex<HashMap<PathBuf, ReviewSession>>)  // canonical-path keyed
  CommitCache  (HashMap<String, GraphResult>, RAW-path keyed, repo.rs:32,45)
  app.emit("session-changed", canonical.to_string_lossy().into_owned())  // review.rs:171
  lib.rs:124-127  existing commands::review::* registration block (inside generate_handler! at lib.rs:79)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four thin commands with mutex-serialized RMW</name>
  <files>src-tauri/src/commands/review.rs</files>
  <read_first>
    - src-tauri/src/commands/review.rs (the file being modified; the CREATE-only thin command at :149-173 — copy the SHAPE, not the read-no-prior-state assumption; canonical_repo_path :50; resolve_data_dir :143; the emit at :171; the existing #[cfg(test)] mod at :276)
    - src-tauri/src/state.rs (RepoState :8,32 raw key; CommitCache :32; ReviewSessionsState — confirm the inner Mutex/Map types and how CommitCache is reached)
    - src-tauri/src/commands/repo.rs (lines 32,45 — how CommitCache is read elsewhere with the RAW path)
    - .planning/phases/66-commit-selection/66-RESEARCH.md (Pitfall 2 RMW serialization steps; Pitfall 3 dual path-keying; §"Thin command shape (mirrors review.rs, adapted for RMW)"; §"Code Examples / Thin command shape")
    - .planning/phases/66-commit-selection/66-PATTERNS.md (§"Read-modify-write serialization (NEW)"; §"Single emit per gesture"; §"Async command shape")
  </read_first>
  <behavior>
    - selection_rmw_serialized: spawning multiple concurrent add (and add+remove) operations against the same session results in NO lost write — the final set equals the expected union under serialization (Pitfall 2). This is the one test requiring real Tauri-state-like orchestration; build it against the in-memory map + save_session with a tempfile data_dir, exercising the serialized RMW path.
  </behavior>
  <action>
    Add four `#[tauri::command] pub async fn`s to review.rs, each taking `path: String` plus its args, `state: State<'_, RepoState>`, `sessions: State<'_, ReviewSessionsState>`, the CommitCache state, and `app: AppHandle`, returning `Result<_, String>` (JSON-stringified TrunkError on Err, matching :156-163).
    - seed_review_range(path, baseOid, tipOid): Tauri receives camelCase params as base_oid/tip_oid (no wrapper struct — like get_rebase_todo). Resolve canonical + data_dir + raw repo from RepoState; open/borrow the git2 Repository; parse Oids; call validate_range then compute_range_oids; then under the sessions mutex: read current session for the canonical key, set session.commits = union_dedup(&session.commits, range_oids), save_session WHILE HOLDING THE LOCK, write back into the map, release; emit once. SEL-01/D-03.
    - add_review_commit(path, oid): RMW under the mutex calling apply_add; save; map-write; release; emit. SEL-02.
    - remove_review_commit(path, oid): RMW under the mutex calling apply_remove; save; map-write; release; emit. SEL-03.
    - list_session_commits(path) -> Vec<SessionCommit>: read the session by CANONICAL key from the in-memory map — if absent return TrunkError code "no_session" (distinct from canonical_repo_path's "not_open"); read the GraphResult from CommitCache by RAW path; open the repo via git2::Repository::open(&path) inside spawn_blocking (do NOT hold the RepoState lock across git2 work); return intersect_graph_order(&session.commits, &graph, &repo). SEL-04. NO mutation, NO emit.
    CRITICAL constraints to honor (do NOT regress): hold the sessions mutex across the entire read→mutate→save_session→map-write (never across an .await — do the blocking save inside the lock, which is fine for small writes per RESEARCH); use canonical key for session store and RAW path for CommitCache (Pitfall 3); seed is ONE walk / ONE save / ONE emit (never N adds). No is_merge gate anywhere (D-08).
    Then add the selection_rmw_serialized test in the #[cfg(test)] module. No fenced code blocks in this action — the read_first files carry the exact shapes.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib selection_rmw_serialized && cargo build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub async fn seed_review_range\|pub async fn add_review_commit\|pub async fn remove_review_commit\|pub async fn list_session_commits" src-tauri/src/commands/review.rs` returns all four.
    - Each mutating command holds the sessions lock across save_session: `grep -n "sessions.0.lock" src-tauri/src/commands/review.rs` count >= 3, and save_session calls appear within those locked blocks (verify by reading the bodies, not just grep).
    - list_session_commits returns a "no_session" TrunkError when no in-memory session — `grep -c "no_session" src-tauri/src/commands/review.rs` >= 1.
    - selection_rmw_serialized passes (no lost write under concurrency).
    - No `is_merge` in any of the four command bodies (D-08).
    - `cd src-tauri && cargo build` succeeds.
  </acceptance_criteria>
  <done>Four commands expose the selection core with serialized RMW (no lost writes), correct dual path-keying, single-emit-per-gesture, and a no_session/not_open error distinction the frontend can branch on.</done>
</task>

<task type="auto">
  <name>Task 2: Register the four commands in lib.rs</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/lib.rs (the file being modified; the generate_handler! block at :79 and the existing commands::review::* registration at :124-127)
    - .planning/phases/66-commit-selection/66-PATTERNS.md (§lib.rs — "Add the four new commands ... after line 127. One line per command; no other change")
  </read_first>
  <action>
    Inside `tauri::generate_handler![` (lib.rs:79), immediately after the existing `commands::review::get_review_session_status,` line (:127), add four lines: `commands::review::seed_review_range,`, `commands::review::add_review_commit,`, `commands::review::remove_review_commit,`, `commands::review::list_session_commits,`. No other change to lib.rs.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | grep -v warning | grep -ic error; grep -c "commands::review::seed_review_range\|commands::review::add_review_commit\|commands::review::remove_review_commit\|commands::review::list_session_commits" src-tauri/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - The grep count for the four registrations == 4.
    - `cd src-tauri && cargo build` succeeds (the first grep, error count, == 0).
  </acceptance_criteria>
  <done>The four selection commands are invokable from the frontend via Tauri's invoke handler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend (JS) → Rust #[tauri::command] | Untrusted `path`, `baseOid`, `tipOid`, `oid` strings cross here. The repo path must be a currently-open repo; OIDs must parse and resolve. |
| Command → ReviewSessionsState (shared in-memory map) | Concurrent commands mutate the same session entry — an integrity boundary, not a privilege one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering / Denial of Service | malformed path / OID args to the four commands | mitigate | `canonical_repo_path` rejects unknown repos with `not_open`; `git2::Oid::from_str` + `find_commit` reject bad OIDs; `validate_range` (Plan 01) rejects bad ranges before any walk. Path-traversal on the persisted file is already mitigated by Phase 65's canonical-path keying + FNV-1a filename hash (D-11) reused unchanged. |
| T-66-02 | Tampering (integrity) | concurrent read-modify-write on session.commits losing a write | mitigate | The entire read→mutate→`save_session`→map-write runs under the `ReviewSessionsState` mutex; the lock is never held across an `.await`. Proven by `selection_rmw_serialized`. |
| T-66-SC | Tampering (supply chain) | npm/cargo installs | accept | N/A — no packages installed this phase (RESEARCH §Package Legitimacy Audit). |

No network, no secrets, no auth — local desktop app on the user's own repo; disposition for those STRIDE categories is "not applicable".
</threat_model>

<verification>
- `cd src-tauri && cargo test --lib review` — selection unit + integration tests green.
- `cd src-tauri && cargo build && cargo clippy` — clean.
</verification>

<success_criteria>
- SEL-01..04 are reachable over IPC: seed/add/remove mutate and persist, list returns graph-ordered SessionCommits.
- Rapid concurrent mutations never lose a write (T-66-02 mitigated and tested).
- Frontend can distinguish no-session from repo-not-open via error codes.
</success_criteria>

<output>
Create `.planning/phases/66-commit-selection/66-02-SUMMARY.md` when done.
</output>
