---
phase: 69-comment-management-ui
plan: 04
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - src/lib/types.ts
  - src/lib/review-session.svelte.ts
autonomous: true
requirements: [CMT-01, CMT-04]
must_haves:
  truths:
    - "D-07: the review-session rune module owns Review-mode right-pane state (panel vs diff) and exposes a jumpTo(comment) action; jump reveals the diff/full-file per Source while the session stays active and a persistent toggle returns to the panel"
    - "the TS Comment DTO mirrors the v2 Rust wire shape: id: string and commit_oid?: string | null added; OrphanReason and CommentResolution types mirror the Rust DTOs (PascalCase enum strings, snake_case fields, | null for Option)"
    - "the rune module is a .svelte.ts file exporting a createXxxState() factory returning { state, ...methods } with $state declared inside (Svelte 5 runes only compile in .svelte / .svelte.ts)"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Comment.id + Comment.commit_oid; OrphanReason + CommentResolution TS types"
      contains: "CommentResolution"
    - path: "src/lib/review-session.svelte.ts"
      provides: "rune factory owning rightPaneMode (panel | diff) + reviewActive + jumpTo action"
      contains: "createReviewSession"
  key_links:
    - from: "review-session.svelte.ts"
      to: "RepoView selection/scroll machinery (Plan 05 wires the concrete callbacks)"
      via: "jumpTo composes injected select-commit / select-file / scroll callbacks"
      pattern: "jumpTo"
---

<objective>
Create the frontend contracts the panel and wiring (Plan 05) consume: the v2 TS DTO mirrors (`Comment.id`, `Comment.commit_oid`, `OrphanReason`, `CommentResolution`) and the new `review-session.svelte.ts` rune module that owns Review-mode right-pane state (`rightPaneMode: "panel" | "diff"`, `reviewActive`) and the `jumpTo(comment)` action (D-07).

Purpose: This is the interface-definition plan. It runs in parallel with the backend write commands (Plan 02) because it depends only on the v2 schema SHAPE from Plan 01, not on any command implementation. Plan 05 implements the panel and wires the rune's jump callbacks into RepoView's concrete selection/scroll machinery.
Output: A TS type layer string-for-string with the Rust wire shape, and a rune factory modeled on the existing `createUndoRedoState` shape.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-comment-management-ui/69-CONTEXT.md
@.planning/phases/69-comment-management-ui/69-RESEARCH.md
@.planning/phases/69-comment-management-ui/69-PATTERNS.md
@.planning/phases/69-comment-management-ui/69-UI-SPEC.md

<interfaces>
Current TS Comment (src/lib/types.ts:300-304): { text: string; anchor: Anchor | null; cached_excerpt: string | null }.
TS enum-string convention (types.ts:288-289): PascalCase union strings mirroring Rust enums (e.g. Source = "Diff" | "FullFile", Side = "Old" | "New").
Rune factory analog (src/lib/undo-redo.svelte.ts:17-35): export function createUndoRedoState(): UndoRedoManager { const state = $state({...}); return { state, push(...) {...} }; }.
Pane-gating analog (RepoView.svelte:122-131): showDiff = $derived(...) two-state gate; rightPaneMode adds a third logical state (panel) to the same center pane.
UI-SPEC pane lock (69-UI-SPEC.md:133): the panel claims the CENTER pane; jump is a single-pane swap (panel → diff → back via the "Review" toggle). Do NOT relocate to the right pane.
Rust DTOs being mirrored (from Plan 03): OrphanReason { CommitGone, FileGone, LineOutOfRange }; CommentResolution { id, resolvable, reason }.
File extension MUST be .svelte.ts (siblings: toast.svelte.ts, undo-redo.svelte.ts, remote-state.svelte.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend TS DTOs to v2 + add resolution types</name>
  <files>src/lib/types.ts</files>
  <read_first>
    - src/lib/types.ts:288-353 (Comment/Anchor/SessionCommit DTOs + the PascalCase enum-string convention)
    - 69-PATTERNS.md § src/lib/types.ts assignment (exact additions)
    - 69-RESEARCH.md § Recommended Project Structure (Comment gets id + commit_oid?; ResolvedComment/orphan-reason types)
  </read_first>
  <action>
    Add `id: string;` and `commit_oid?: string | null;` to the Comment interface (types.ts:300-304), matching the Rust wire shape (snake_case field name commit_oid; | null for Option). Add `export type OrphanReason = "CommitGone" | "FileGone" | "LineOutOfRange";` (PascalCase strings, matching the Rust enum serialization, following the existing Source/Side convention at types.ts:288-289). Add `export interface CommentResolution { id: string; resolvable: boolean; reason: OrphanReason | null; }` (snake_case-irrelevant here; fields match the Rust struct). Do NOT change Anchor, Source, Side, or any other DTO.
  </action>
  <verify>
    <automated>npx svelte-check --threshold error 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/types.ts Comment interface contains `id: string` and `commit_oid?: string | null`
    - src/lib/types.ts contains `export type OrphanReason` with the three PascalCase members and `export interface CommentResolution`
    - svelte-check reports no new type errors introduced by these changes
  </acceptance_criteria>
  <done>The TS DTO layer mirrors the v2 Rust wire shape incl. resolution types.</done>
</task>

<task type="auto">
  <name>Task 2: Create review-session.svelte.ts rune module</name>
  <files>src/lib/review-session.svelte.ts</files>
  <read_first>
    - src/lib/undo-redo.svelte.ts:17-35 (createXxxState factory shape — $state inside, return { state, ...methods })
    - src/components/RepoView.svelte:122-131 (showDiff derived pane-gating pattern; rightPaneMode generalizes it)
    - src/components/RepoView.svelte:285-370 (handleCommitSelect / handleCommitFileSelect — the callbacks Plan 05 injects into jumpTo)
    - 69-UI-SPEC.md:121-133 (the locked interaction contract: jump per Source, center-pane swap, Review toggle returns)
    - 69-PATTERNS.md § review-session.svelte.ts assignment
  </read_first>
  <action>
    Create src/lib/review-session.svelte.ts (extension MUST be .svelte.ts). Export a `createReviewSession()` factory returning `{ state, ...methods }` with `const state = $state({ reviewActive: false, rightPaneMode: "panel" as "panel" | "diff" })` declared inside (mirror createUndoRedoState). Methods: `setReviewActive(active: boolean)`, `showPanel()` (rightPaneMode = "panel"), `showDiff()` (rightPaneMode = "diff"), and `async jumpTo(comment, deps)` where `deps` carries the injected callbacks Plan 05 supplies — selectCommit(oid), selectFile(path), scrollToRange(start, end) — so the rune stays decoupled from RepoView internals. jumpTo: if comment.anchor is null (commit-level OR orphaned), return early without navigating (D-08 — orphaned/commit-level have no jump target); otherwise await selectCommit(anchor.commit_oid), await selectFile(anchor.file_path) (the file's Source decides diff vs full-file view downstream), set rightPaneMode = "diff", then scrollToRange(anchor.start_line, anchor.end_line). Type the deps and comment params against src/lib/types.ts (import Comment). Do NOT import RepoView directly — keep the module a pure state+action factory; Plan 05 binds the concrete callbacks. Add a short JSDoc noting the module owns center-pane Review-mode state (panel vs diff) per UI-SPEC:133.
  </action>
  <verify>
    <automated>npx svelte-check --threshold error 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/review-session.svelte.ts exists with the `.svelte.ts` extension
    - It exports `createReviewSession` returning an object whose `state` has `reviewActive` and `rightPaneMode` ("panel" | "diff")
    - It exports a `jumpTo` method that returns early when comment.anchor is null and otherwise sets rightPaneMode to "diff"
    - It does NOT import from RepoView.svelte (decoupled — callbacks injected via deps)
    - svelte-check reports no errors in the new file
  </acceptance_criteria>
  <done>The rune module owns Review-mode pane state and exposes a decoupled jumpTo action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds pure frontend type definitions and transient UI state; it opens no new IPC command surface and reads no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-13 | Tampering | TS DTO drift from the Rust wire shape causing silent misparse | mitigate | DTO fields are mirrored string-for-string from the Rust types (id, commit_oid, OrphanReason variants, CommentResolution); svelte-check guards usage; Plan 05 component tests exercise the real shapes |
| T-69-14 | (n/a) | rune module transient UI state | accept | pure in-memory UI state (rightPaneMode, reviewActive); no persistence, no IPC, no untrusted input — negligible risk |
</threat_model>

<verification>
- `npx svelte-check --threshold error` reports no new errors
- review-session.svelte.ts exists and exports createReviewSession with the jumpTo contract
</verification>

<success_criteria>
- TS DTOs mirror the v2 wire shape incl. OrphanReason / CommentResolution
- The rune module owns center-pane Review-mode state and a decoupled jumpTo action (D-07), ready for Plan 05 to bind callbacks
</success_criteria>

<output>
Create `.planning/phases/69-comment-management-ui/69-04-SUMMARY.md` when done
</output>
