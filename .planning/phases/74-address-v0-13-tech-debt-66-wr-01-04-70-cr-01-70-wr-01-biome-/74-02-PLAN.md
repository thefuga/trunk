---
phase: 74-address-v0-13-tech-debt-66-wr-01-04-70-cr-01-70-wr-01-biome
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/diff/CommentComposer.svelte
autonomous: true
requirements: []
findings:
  - biome-noNonNullAssertion
must_haves:
  truths:
    - "bunx biome ci src/components/diff/CommentComposer.svelte reports zero noNonNullAssertion warnings"
    - "Both call contracts (diff-path and full-file-path) continue to work — existing CommentComposer.test.ts passes unchanged"
    - "No // biome-ignore directives introduced (project has zero precedent)"
  artifacts:
    - path: src/components/diff/CommentComposer.svelte
      provides: "deriveDiffCapture runtime guard helper replacing 3× non-null assertions"
      contains: "function deriveDiffCapture"
  key_links:
    - from: "$derived capturedResult"
      to: "deriveDiffCapture()"
      via: "?? fallback"
      pattern: "captured \\?\\? deriveDiffCapture\\(\\)"
---

<objective>
Eliminate the 3× `noNonNullAssertion` biome warnings at `src/components/diff/CommentComposer.svelte:43` by extracting the diff-path derivation into an explicit runtime guard helper (RESEARCH §7 Option B, locked decision from /gsd:discuss-phase).

Purpose: the non-null assertions silently document a caller contract — `DiffPanel.svelte:625` guards `composerOpen && composerFile && composerHunkIdx !== null` before passing props, so on the diff path the three optional props are always defined; on the full-file path `captured` is supplied and the three are undefined. The `??` operator picks the right contract at runtime, but biome can't see that. Replacing the bang operators with an explicit `if (file === undefined || ...) throw` guard makes the contract explicit, deletes the suppression precedent (project has zero `biome-ignore`), and aligns with the project tone in `coding_style.md` ("don't defend against your own code" — but when you DO need to guard, make it an explicit branch, not an invisible assertion).

Output: zero biome warnings on this file; existing tests still green.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/74-address-v0-13-tech-debt-66-wr-01-04-70-cr-01-70-wr-01-biome-/74-RESEARCH.md

<interfaces>
Current shape (verified — src/components/diff/CommentComposer.svelte:36-44):

```ts
// The capture-time adapter is the single source of truth for both the persisted
// range (start_line..end_line) and the excerpt. When the host injects a captured
// result (full-file path) use it directly; otherwise derive it from the diff-path
// props. The diff-path caller always supplies file/hunkIdx/selectedLineIndices,
// so the non-null assertions document that contract rather than guard it.
const capturedResult = $derived(
    captured ?? buildDiffAnchor(commitOid, file!, hunkIdx!, selectedLineIndices!),
);
```

`buildDiffAnchor` lives in `src/lib/diff-anchor.ts`; the caller at `src/components/diff/DiffPanel.svelte:625` guards before mounting CommentComposer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace non-null assertions with runtime guard helper</name>
  <files>src/components/diff/CommentComposer.svelte</files>
  <read_first>
    - src/components/diff/CommentComposer.svelte (full file — small; need to see Props interface around `let { ... } = $props()` and the existing comment block at lines 36-44)
    - src/components/diff/DiffPanel.svelte:620-650 (the call site that establishes the diff-path contract; do not modify, just confirm the guard there)
    - .planning/phases/74-address-v0-13-tech-debt-66-wr-01-04-70-cr-01-70-wr-01-biome-/74-RESEARCH.md §7 (Option B sketch)
    - src/components/diff/CommentComposer.test.ts (existing tests — confirm both contracts have coverage; the new throw branch is dead in practice but the helper signature should not change observable behavior)
  </read_first>
  <behavior>
    - On the diff path (DiffPanel call): `capturedResult` resolves to the same value as today — `buildDiffAnchor(commitOid, file, hunkIdx, selectedLineIndices)` where the three are guaranteed defined by the caller.
    - On the full-file path: `capturedResult` resolves to the injected `captured` — `deriveDiffCapture()` is never called.
    - Dead branch (defensive): if the helper is invoked with any of `file`, `hunkIdx`, or `selectedLineIndices` undefined, it throws `Error("CommentComposer: diff-path props missing — caller contract violated")`. This is not a user-facing path; it is documentation-by-throw of the contract.
  </behavior>
  <action>
    In `src/components/diff/CommentComposer.svelte`:
    1. Replace the two-line `const capturedResult = $derived(...)` block (lines 42-44) and the preceding comment (lines 36-41) with the Option B shape from RESEARCH §7:
       - Define `function deriveDiffCapture()` returning the same type that `buildDiffAnchor` returns. The function starts with the `if (file === undefined || hunkIdx === undefined || selectedLineIndices === undefined) throw new Error("CommentComposer: diff-path props missing — caller contract violated");` guard, then returns `buildDiffAnchor(commitOid, file, hunkIdx, selectedLineIndices)`.
       - Update `capturedResult` to `$derived(captured ?? deriveDiffCapture())`.
       - Keep the explanatory comment (the WHY — about the two contracts) above the function; trim the now-misleading "non-null assertions document that contract" phrasing.
    2. Do not change the Props interface, do not change anything else in the file, do not introduce `biome-ignore`.
    3. Run `bunx biome ci src/components/diff/CommentComposer.svelte` and confirm zero `noNonNullAssertion` warnings.
    4. Run `bunx vitest run src/components/diff/CommentComposer.test.ts` and confirm no regressions.
    Commit message: `refactor(74-02): replace non-null assertions with explicit deriveDiffCapture guard (closes biome noNonNullAssertion debt)`.
  </action>
  <verify>
    <automated>bunx biome ci src/components/diff/CommentComposer.svelte 2>&1 | grep -v '^#' | grep -c noNonNullAssertion | xargs -I {} test {} -eq 0 && bunx vitest run src/components/diff/CommentComposer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx biome ci src/components/diff/CommentComposer.svelte 2>&1 | grep -c noNonNullAssertion` returns 0.
    - `bunx vitest run src/components/diff/CommentComposer.test.ts` exits 0 with the same test count as before.
    - `grep -c "function deriveDiffCapture" src/components/diff/CommentComposer.svelte` returns 1.
    - `grep -c "biome-ignore" src/components/diff/CommentComposer.svelte` returns 0.
    - `just check` exits 0.
  </acceptance_criteria>
  <done>Three biome warnings removed via explicit guard; no precedent for `biome-ignore` set; tests green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

No new attack surface. Tech-debt cleanup; no input parsing changes, no auth changes, no IPC contract changes. The added runtime throw is in a dead branch (caller contract is upheld by DiffPanel.svelte:625 guard) and acts as documentation-by-throw — it does not change reachable behavior.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | n/a | No applicable threats. |
</threat_model>

<verification>
- `bunx biome ci src/components/diff/CommentComposer.svelte` exits 0 with zero `noNonNullAssertion` warnings.
- `bunx vitest run src/components/diff/CommentComposer.test.ts` exits 0; same test count.
- `just check` exits 0.
</verification>

<success_criteria>
- Three noNonNullAssertion warnings removed; no biome-ignore precedent set.
- Caller contract documented by an explicit guard branch.
- Tests still pass.
</success_criteria>

<output>
Create `.planning/phases/74-address-v0-13-tech-debt-66-wr-01-04-70-cr-01-70-wr-01-biome-/74-02-SUMMARY.md` when done. Note the warning count delta (3 → 0) and confirm no `biome-ignore` directives were introduced project-wide.
</output>
