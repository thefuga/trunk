---
phase: 74-address-v0-13-tech-debt-66-wr-01-04-70-cr-01-70-wr-01-biome
plan: 05
type: execute
wave: 2
depends_on:
  - 74-04
files_modified:
  - src-tauri/src/commands/review.rs
autonomous: true
requirements: []
findings:
  - 66/WR-04
must_haves:
  truths:
    - "All 10 call sites of `app.emit(\"session-changed\", ...)` log a warning when emit fails instead of discarding the error"
    - "Zero `let _ = app.emit(...)` patterns remain in src-tauri/src/commands/review.rs"
    - "Happy path (emit succeeds) is behaviorally unchanged"
  artifacts:
    - path: src-tauri/src/commands/review.rs
      provides: "emit_session_changed helper used at all 10 call sites"
      contains: "fn emit_session_changed"
  key_links:
    - from: "10 session-changed call sites"
      to: "emit_session_changed helper"
      via: "single function call replacing let _ = app.emit"
      pattern: "emit_session_changed\\(&app, &canonical\\)"
---

<objective>
Fix audit finding 66/WR-04: 10 call sites in `src-tauri/src/commands/review.rs` use `let _ = app.emit("session-changed", canonical.to_string_lossy().into_owned());` — silently discarding any emit failure. Phase 65's stated stance is "never silently destroy"; this aligns the file to that.

Purpose: replace all 10 sites with an `emit_session_changed(&app, &canonical)` helper that logs (via `eprintln!`, since the codebase has neither `log` nor `tracing` as a dependency — verified by grep). Mechanical refactor with zero behavior change on the happy path; on the never-seen-in-practice failure path a diagnostic line appears in stderr.

Output: helper exists, all 10 sites use it, grep gate confirms zero `let _ = app.emit` patterns remain in this file.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/74-address-v0-13-tech-debt-66-wr-01-04-70-cr-01-70-wr-01-biome-/74-RESEARCH.md

<interfaces>
Verified call sites (10 lines — grep on commands/review.rs at planning time): 661, 681, 701, 735, 789, 815, 839, 1065, 1120, 1142. Line 661 sits inside `seed_review_range`'s body, which Plan 74-04 modifies first — that is why Plan 74-05 depends on Plan 74-04. After Plan 74-04 lands, the line number for that emit may shift but the call shape `let _ = app.emit("session-changed", canonical.to_string_lossy().into_owned());` is preserved verbatim per Plan 74-04 instructions.

Logging convention: `grep -rn "log::\|tracing::\|eprintln!" src-tauri/src/` returns no matches — the codebase has no logger configured. Use `eprintln!("session-changed emit failed for {}: {}", canonical.display(), e);` per RESEARCH §4 (the assumption A4 about `log::warn!` is FALSIFIED — verified at plan time; `eprintln!` is the project-consistent path).

Type signature: `AppHandle` (Tauri 2) is the type already imported at the top of `commands/review.rs`. `canonical: &Path` works because all 10 sites currently call `canonical.to_string_lossy().into_owned()` on a value that is either `&PathBuf` or `&Path` in scope (verify per site during execution; if some sites pass `PathBuf` by value, adjust the helper to take `&Path` and call `&canonical` at the site — references are zero-cost).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add emit_session_changed helper</name>
  <files>src-tauri/src/commands/review.rs</files>
  <read_first>
    - src-tauri/src/commands/review.rs top of file (imports — confirm `tauri::AppHandle` and `std::path::Path` are already in scope, add if missing)
    - src-tauri/src/commands/review.rs (grep for `let _ = app.emit` to enumerate current line numbers post-Plan-74-04)
    - .planning/phases/74-address-v0-13-tech-debt-66-wr-01-04-70-cr-01-70-wr-01-biome-/74-RESEARCH.md §4
  </read_first>
  <action>
    Add a private module-scope helper near the top of `src-tauri/src/commands/review.rs` (after imports, before the first `#[tauri::command]`):
    - Function name: `emit_session_changed`
    - Signature: takes `&AppHandle` and `&Path`, returns `()`.
    - Body: calls `app.emit("session-changed", canonical.to_string_lossy().into_owned())`. On `Err(e)`, writes `eprintln!("session-changed emit failed for {}: {}", canonical.display(), e);`. On `Ok`, nothing.
    - Visibility: `fn` (private to the module) — siblings in the same file are the only callers.
    Add the `std::path::Path` import if not present. Compile: `cd src-tauri && cargo build --lib`. Do not yet replace any call sites. Commit message: `refactor(74-05): add emit_session_changed helper for logged emit failures`.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build --lib 2>&1 | tail -5 | grep -E "(error|warning).*emit_session_changed" | wc -l | xargs -I {} test {} -le 1</automated>
  </verify>
  <acceptance_criteria>
    - `cd src-tauri && cargo build --lib` exits 0.
    - `grep -c "fn emit_session_changed" src-tauri/src/commands/review.rs` returns 1.
    - Existing call sites still use `let _ = app.emit(...)` (this task only adds the helper; replacement is Task 2).
    - At most one warning (the `dead_code` warning for the unused helper, which Task 2 closes).
  </acceptance_criteria>
  <done>Helper compiles; replacements still pending.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 10 call sites with the helper</name>
  <files>src-tauri/src/commands/review.rs</files>
  <read_first>
    - src-tauri/src/commands/review.rs (use `grep -n "let _ = app.emit" src-tauri/src/commands/review.rs` to enumerate current call sites — should be exactly 10)
  </read_first>
  <action>
    Replace every `let _ = app.emit("session-changed", canonical.to_string_lossy().into_owned());` line in `src-tauri/src/commands/review.rs` with `emit_session_changed(&app, &canonical);`. Use exact-match find-and-replace across the whole file. If any site has a slightly different shape (different variable name than `canonical`, or `canonical.to_string_lossy()` without `.into_owned()`), inspect and adapt — the helper signature takes `&Path`, so pass `&that_variable` and let the helper handle the conversion. If a site emits a *different* event name (not `"session-changed"`), DO NOT replace it — `emit_session_changed` is for `session-changed` only; surface the discrepancy. (Per the verified grep, all 10 sites use `"session-changed"`.) Run `just check` after replacement. Commit message: `refactor(74-05): replace 10 silent let _ = app.emit sites with emit_session_changed helper (closes 66/WR-04)`.
  </action>
  <verify>
    <automated>test "$(grep -c 'let _ = app.emit' src-tauri/src/commands/review.rs)" -eq 0 && test "$(grep -c 'emit_session_changed(&app' src-tauri/src/commands/review.rs)" -ge 10 && just check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "let _ = app.emit" src-tauri/src/commands/review.rs` returns 0.
    - `grep -c "emit_session_changed(&app" src-tauri/src/commands/review.rs` returns at least 10.
    - `just check` exits 0 (clippy + biome + svelte-check + cargo test + vitest + fmt).
    - `cd src-tauri && cargo build --lib 2>&1 | grep -c dead_code` returns 0 (helper is now used).
  </acceptance_criteria>
  <done>All 10 sites use the helper; grep gate is clean; full project gate green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

No new attack surface. Refactor + diagnostic logging.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-05-01 | Information Disclosure | eprintln! payload | accept | The log line includes `canonical.display()` — an absolute filesystem path. This is local stderr in a desktop app; no remote sink. The user already sees this path in their own UI. Severity: none. |
</threat_model>

<verification>
- `grep -c "let _ = app.emit" src-tauri/src/commands/review.rs` returns 0.
- `just check` exits 0.
- Manual smoke (not testable without a Tauri integration harness — RESEARCH §4): emit failures are essentially unobservable in normal operation; the eprintln line will only appear if the Tauri event bus is dead, which is a non-recoverable runtime fault.
</verification>

<success_criteria>
- 66/WR-04 closed; no silent emit failures remain in this file.
- Single helper avoids 10× copies of identical logging code.
- Project gate green.
</success_criteria>

<output>
Create `.planning/phases/74-address-v0-13-tech-debt-66-wr-01-04-70-cr-01-70-wr-01-biome-/74-05-SUMMARY.md` when done. Include the grep output before and after (the `let _ = app.emit` count going from 10 → 0).
</output>
