---
phase: 54-frontend-unit-tests
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - src/components/Toast.test.ts
  - src/components/RefPill.test.ts
  - src/components/BranchRow.test.ts
  - src/components/BranchSection.test.ts
  - src/components/RemoteGroup.test.ts
  - src/components/DirectoryRow.test.ts
  - src/components/FileRow.test.ts
  - src/components/CommitRow.test.ts
  - src/components/SearchBar.test.ts
  - src/components/InputDialog.test.ts
  - src/components/OperationBanner.test.ts
  - src/components/PullDropdown.test.ts
  - src/components/CommitForm.test.ts
autonomous: true
requirements:
  - UNIT-03

must_haves:
  truths:
    - "Every simple Svelte component renders with correct props and fires events"
    - "Every medium Svelte component handles user interactions, conditional rendering, and form state"
    - "All 13 component test files import the shared Tauri mock and use @testing-library/svelte"
  artifacts:
    - path: "src/components/Toast.test.ts"
      provides: "Toast component tests"
      contains: "describe"
    - path: "src/components/RefPill.test.ts"
      provides: "RefPill component tests"
      contains: "describe"
    - path: "src/components/BranchRow.test.ts"
      provides: "BranchRow component tests"
      contains: "describe"
    - path: "src/components/BranchSection.test.ts"
      provides: "BranchSection component tests"
      contains: "describe"
    - path: "src/components/RemoteGroup.test.ts"
      provides: "RemoteGroup component tests"
      contains: "describe"
    - path: "src/components/DirectoryRow.test.ts"
      provides: "DirectoryRow component tests"
      contains: "describe"
    - path: "src/components/FileRow.test.ts"
      provides: "FileRow component tests"
      contains: "describe"
    - path: "src/components/CommitRow.test.ts"
      provides: "CommitRow component tests"
      contains: "describe"
    - path: "src/components/SearchBar.test.ts"
      provides: "SearchBar component tests"
      contains: "describe"
    - path: "src/components/InputDialog.test.ts"
      provides: "InputDialog component tests"
      contains: "describe"
    - path: "src/components/OperationBanner.test.ts"
      provides: "OperationBanner component tests"
      contains: "describe"
    - path: "src/components/PullDropdown.test.ts"
      provides: "PullDropdown component tests"
      contains: "describe"
    - path: "src/components/CommitForm.test.ts"
      provides: "CommitForm component tests"
      contains: "describe"
  key_links:
    - from: "src/components/*.test.ts"
      to: "src/__tests__/helpers/tauri-mock.ts"
      via: "import side-effect"
      pattern: 'import.*tauri-mock'
    - from: "src/components/*.test.ts"
      to: "@testing-library/svelte"
      via: "render/screen imports"
      pattern: "import.*from.*@testing-library/svelte"
---

<objective>
Create unit tests for 13 simple and medium-complexity Svelte components using @testing-library/svelte. Each component gets collocated test files covering rendering, prop display, click handlers, event emissions, and conditional rendering.

Purpose: Per D-02 and D-03, ALL components get tests — not just logic-heavy ones. Simple components get render + props + events testing depth. This plan covers the 8 simpler components (Toast through CommitRow) and 5 medium components (SearchBar through CommitForm).

Output: 13 component test files, all passing via `bun run test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-frontend-unit-tests/54-CONTEXT.md
@.planning/phases/54-frontend-unit-tests/54-RESEARCH.md
@.planning/phases/54-frontend-unit-tests/54-01-SUMMARY.md

<interfaces>
<!-- Component Props interfaces the executor needs -->

Toast.svelte — no props; reads from `toasts.items` store (imported from $lib/toast.svelte.js)
  Renders: div[role="status"] for each toast with toast.message text
  Uses: transition:fly

RefPill.svelte:
```typescript
interface Props {
  refs: RefLabel[];
  showAll?: boolean;
  expanded?: boolean;
  maxWidth?: number;
}
```
  Key behavior: showAll renders all refs in column, otherwise renders first ref only. is_head adds "font-bold". isRemoteOnly gets opacity:0.5. Tags get "◆" prefix, stashes get "⚑" prefix.

BranchRow.svelte:
```typescript
interface Props {
  name: string;
  kind?: "local" | "remote" | "tag";
  isHead?: boolean;
  isLoading?: boolean;
  isError?: boolean;
  errorText?: string;
  ahead?: number;
  behind?: number;
  onclick?: () => void;
  ondblclick?: () => void;
  oncontextmenu?: (e: MouseEvent) => void;
}
```
  Key behavior: role="button", shows error div when isError=true, shows ahead/behind counts with ArrowUp/ArrowDown icons.

BranchSection.svelte:
```typescript
interface Props {
  label: string;
  count: number;
  expanded: boolean;
  ontoggle: () => void;
  showCreateButton?: boolean;
  oncreate?: () => void;
  children: Snippet;
}
```
  Key behavior: Shows "label (count)", expands/collapses children, optional Plus button for create.

RemoteGroup.svelte:
```typescript
interface Props {
  remoteName: string;
  branches: string[];
  checkingOut: string | null;
  errorBranch: string | null;
  errorText: string;
  oncheckout: (fullName: string) => void;
  oncontextmenu?: (e: MouseEvent, fullName: string) => void;
}
```
  Key behavior: Renders remoteName header, renders BranchRow for each branch.

DirectoryRow.svelte:
```typescript
interface Props {
  node: DirectoryNode;
  depth: number;
  expanded: boolean;
  focused: boolean;
  ontoggle: () => void;
  actionLabel?: string;
  onaction?: () => void;
  oncontextmenu?: (e: MouseEvent) => void;
}
```
  Key behavior: role="treeitem", aria-expanded, shows node.name + file count, chevron icon changes on expand.

FileRow.svelte:
```typescript
interface Props {
  file: FileStatus;
  isLoading?: boolean;
  actionLabel: string;
  onaction: () => void;
  onclick?: () => void;
  oncontextmenu?: (e: MouseEvent) => void;
  depth?: number;
  displayName?: string;
  focused?: boolean;
}
```
  Key behavior: Shows file.path (or displayName), status icon per file.status, hover action button.

CommitRow.svelte:
```typescript
interface Props {
  commit: GraphCommit;
  rowIndex: number;
  onselect?: (oid: string) => void;
  oncontextmenu?: (e: MouseEvent, commit: GraphCommit) => void;
  maxColumns?: number;
  columnWidths: ColumnWidths;
  columnVisibility: ColumnVisibility;
  selected?: boolean;
  rowHeight?: number;
  isSearchMatch?: boolean;
  isCurrentMatch?: boolean;
  isSearchActive?: boolean;
}
```
  Key behavior: Shows commit summary, author, relative date, short_oid. WIP/stash rows italic+muted. Hides columns based on columnVisibility.

SearchBar.svelte:
```typescript
interface Props {
  query: string;
  currentIndex: number;
  totalMatches: number;
  onquerychange: (query: string) => void;
  onnext: () => void;
  onprev: () => void;
  onclose: () => void;
}
```
  Key behavior: Input with autofocus, shows "N of M" match count, prev/next/close buttons, Escape calls onclose, Enter calls onnext, Shift+Enter calls onprev.

InputDialog.svelte:
```typescript
interface Props {
  title: string;
  fields: Field[];
  onsubmit: (values: Record<string, string>) => void;
  oncancel: () => void;
  confirmLabel?: string;
  cancelLabel?: string;
}
interface Field {
  key: string;
  label: string;
  placeholder?: string;
  multiline?: boolean;
  required?: boolean;
  defaultValue?: string;
}
```
  Key behavior: Modal with title, fields, submit/cancel. Submit disabled when required fields empty. Escape cancels. Enter submits (unless in textarea). Backdrop click cancels.

OperationBanner.svelte:
```typescript
interface Props {
  info: OperationInfo;
  repoPath: string;
  onaction?: () => void;
}
```
  Key behavior: Shows "Merging source into target" or "Rebasing source onto target". Rebase shows Continue/Skip/Abort buttons. CherryPick/Revert show label text.

PullDropdown.svelte:
```typescript
interface Props {
  repoPath: string;
  disabled: boolean;
  remoteState: RemoteState;
}
```
  Key behavior: Dropdown with Fetch, FF options, Pull rebase. Opens/closes on click.

CommitForm.svelte:
```typescript
interface Props {
  repoPath: string;
  stagedCount: number;
  onsubjectchange?: (value: string) => void;
  clearRedoStack: () => void;
}
```
  Key behavior: Mode switching (commit/amend/stash), subject + body inputs, submit button label changes per mode, disabled when subject empty or no staged files.

<!-- Shared helpers from Plan 01 -->
From src/__tests__/helpers/factories.ts:
  export function makeCommit(...): GraphCommit
  export function makeEdge(...): GraphEdge
  export function makeFile(...): FileStatus
  export function makeRef(...): RefLabel

From src/__tests__/helpers/tauri-mock.ts:
  Side-effect import that mocks all @tauri-apps modules
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests for 8 simple components (Toast, RefPill, BranchRow, BranchSection, RemoteGroup, DirectoryRow, FileRow, CommitRow)</name>
  <files>src/components/Toast.test.ts, src/components/RefPill.test.ts, src/components/BranchRow.test.ts, src/components/BranchSection.test.ts, src/components/RemoteGroup.test.ts, src/components/DirectoryRow.test.ts, src/components/FileRow.test.ts, src/components/CommitRow.test.ts</files>
  <read_first>
    - src/components/Toast.svelte (full source — understand toasts store import, role="status", fly transition)
    - src/components/RefPill.svelte (full source — props interface, pillClasses, isRemoteOnly, pillPrefix logic)
    - src/components/BranchRow.svelte (full source — Props interface, event handlers, error state)
    - src/components/BranchSection.svelte (full source — Props with Snippet children, toggle, create button)
    - src/components/RemoteGroup.svelte (full source — renders BranchRow children)
    - src/components/DirectoryRow.svelte (full source — role="treeitem", file count, action button)
    - src/components/FileRow.svelte (full source — status icons, action button, displayName)
    - src/components/CommitRow.svelte (full source — column visibility, WIP/stash, relativeDate)
    - src/lib/toast.svelte.ts (toast store — showToast, toasts.items, _resetToasts)
    - src/lib/types.ts (GraphCommit, FileStatus, RefLabel types for factory use)
    - src/lib/graph-constants.ts (ROW_HEIGHT, LANE_WIDTH, COLUMN_PADDING_X values for CommitRow)
    - src/__tests__/helpers/factories.ts (makeCommit, makeFile, makeRef)
    - src/__tests__/helpers/tauri-mock.ts (shared mock — import pattern)
  </read_first>
  <action>
Per D-02 and D-03: all components get render + props + events testing depth.

All test files follow this pattern:
```typescript
import { render, screen, fireEvent } from "@testing-library/svelte";
import { describe, expect, it, vi } from "vitest";
import ComponentName from "./ComponentName.svelte";
import "../__tests__/helpers/tauri-mock";
// import factories as needed
```

Test file naming: collocated `.test.ts` files (Claude's discretion per CONTEXT.md).

**Toast.test.ts** — Tests:
- "renders nothing when no toasts" — render Toast, no role="status" in document
- "renders toast message with role=status" — import `showToast`, `_resetToasts` from `$lib/toast.svelte.js`, call `showToast("Hello", "success")`, verify `screen.getByRole("status")` has text "Hello"
- "renders error toast with error styling" — call `showToast("Fail", "error")`, verify role="status" contains "Fail"
- Note: Use `_resetToasts()` in `beforeEach` for clean state. Use `vi.useFakeTimers()` so toast doesn't auto-dismiss.

**RefPill.test.ts** — Tests:
- "renders primary ref short name" — render with refs=[{short_name:"main", ref_type:"LocalBranch", is_head:true, ...}], verify "main" in document
- "applies font-bold class to HEAD ref" — render with is_head:true, check class contains "font-bold"
- "renders tag prefix" — ref_type:"Tag", verify "◆" prefix rendered
- "renders stash prefix" — ref_type:"Stash", verify "⚑" prefix rendered
- "renders all refs when showAll=true" — 3 refs, showAll=true, all 3 short_names in document
- "renders only first ref when showAll=false" — 3 refs, showAll=false, only first rendered

**BranchRow.test.ts** — Tests:
- "renders branch name" — name="feature/login", verify text in document
- "calls onclick when clicked" — vi.fn(), click role="button", verify called
- "shows error message when isError=true" — isError=true, errorText="Checkout failed", verify error text rendered
- "shows default error when isError=true but no errorText" — verify default "Cannot checkout" text
- "shows ahead count with arrow" — ahead=3, verify "3" in document
- "shows behind count with arrow" — behind=2, verify "2" in document
- "does not show ahead/behind when both zero" — ahead=0, behind=0, no counts shown
- "applies accent color when isHead" — isHead=true, verify style contains "var(--color-accent)"

**BranchSection.test.ts** — Tests:
- "renders label with count" — label="Branches", count=5, verify "Branches (5)" text
- "calls ontoggle when header clicked" — vi.fn(), click header, verify called
- "shows create button when showCreateButton=true" — verify aria-label="Create new branch" present
- "hides create button by default" — verify no element with aria-label="Create new branch"
- Note: BranchSection uses Snippet children. Use a wrapper component or pass `children` via the testing library's rendering approach. If Svelte 5 Snippets don't render in tests, test the toggle behavior and button rendering only.

**RemoteGroup.test.ts** — Tests:
- "renders remote name header" — remoteName="origin", verify "origin" text (uppercase)
- "renders branch rows for each branch" — branches=["main","dev"], verify both names rendered
- "calls oncheckout with full name when branch clicked" — click a branch row, verify oncheckout called with "origin/main"

**DirectoryRow.test.ts** — Tests:
- "renders directory name" — node={name:"src", ...}, verify "src" in document
- "renders file count" — node with children, verify count displayed
- "has treeitem role and aria-expanded" — verify role="treeitem" exists, aria-expanded matches prop
- "calls ontoggle when clicked" — vi.fn(), click row, verify called

**FileRow.test.ts** — Tests:
- "renders file path" — file={path:"README.md", status:"Modified", is_binary:false}, verify "README.md"
- "renders displayName when provided" — displayName="short.ts", verify "short.ts" in document
- "has correct role based on depth" — depth=0: role="listitem", depth>0: role="treeitem"
- "shows stage button label on hover" — actionLabel="+", mouseenter, verify aria-label="Stage file"

**CommitRow.test.ts** — Tests:
- "renders commit summary" — commit with summary="fix: bug", verify text
- "renders author name when column visible" — columnVisibility with author:true, verify author name
- "hides author column when not visible" — author:false, author name not in document
- "renders short OID when column visible" — sha:true, verify short_oid text
- "renders WIP row in italic style" — oid="__wip__", verify italic class
- "calls onselect with oid when clicked" — vi.fn(), click, verify called with oid
- Use `makeCommit` factory. Provide required columnWidths and columnVisibility objects:
  ```typescript
  const defaultWidths = { ref: 120, graph: 24, author: 60, date: 40, sha: 50 };
  const allVisible = { ref: true, graph: true, message: true, author: true, date: true, sha: true };
  ```

Run `bun run test` after all 8 files created.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Toast.test.ts exists and contains `describe("Toast"`
    - src/components/RefPill.test.ts exists and contains `describe("RefPill"`
    - src/components/BranchRow.test.ts exists and contains `describe("BranchRow"` and `role="button"` or `getByRole("button")`
    - src/components/BranchSection.test.ts exists and contains `describe("BranchSection"`
    - src/components/RemoteGroup.test.ts exists and contains `describe("RemoteGroup"`
    - src/components/DirectoryRow.test.ts exists and contains `describe("DirectoryRow"` and `treeitem`
    - src/components/FileRow.test.ts exists and contains `describe("FileRow"`
    - src/components/CommitRow.test.ts exists and contains `describe("CommitRow"` and `makeCommit`
    - All 8 test files import from `@testing-library/svelte`
    - All 8 test files import `../__ tests__/helpers/tauri-mock`
    - `bun run test` exits 0
  </acceptance_criteria>
  <done>8 simple component test files created, each covering render, props, events, and conditional rendering. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests for 5 medium components (SearchBar, InputDialog, OperationBanner, PullDropdown, CommitForm)</name>
  <files>src/components/SearchBar.test.ts, src/components/InputDialog.test.ts, src/components/OperationBanner.test.ts, src/components/PullDropdown.test.ts, src/components/CommitForm.test.ts</files>
  <read_first>
    - src/components/SearchBar.svelte (full source — keyboard handling, match counter, nav buttons)
    - src/components/InputDialog.svelte (full source — field rendering, validation, keyboard, backdrop)
    - src/components/OperationBanner.svelte (full source — merge vs rebase rendering, action buttons)
    - src/components/PullDropdown.svelte (full source — dropdown state, option list, remote operations)
    - src/components/CommitForm.svelte (full source — mode switching, validation, submit)
    - src/lib/invoke.ts (safeInvoke — used by OperationBanner, PullDropdown, CommitForm)
    - src/lib/toast.svelte.ts (showToast — used by OperationBanner, PullDropdown, CommitForm)
    - src/lib/remote-state.svelte.ts (RemoteState type — used by PullDropdown)
    - src/__tests__/helpers/tauri-mock.ts (verify mock covers all Tauri plugins these components use)
  </read_first>
  <action>
Per D-02 and D-03: even medium-complexity components get full testing depth.

**SearchBar.test.ts** — Tests:
- "renders input with placeholder 'Search commits...'" — verify placeholder text
- "shows match count when query has matches" — query="fix", totalMatches=5, currentIndex=2, verify "3 of 5" text
- "shows '0 matches' when query has no results" — query="xyz", totalMatches=0, verify "0 matches" text
- "hides match count when query empty" — query="", verify no match counter
- "calls onquerychange when typing" — type in input, verify callback fired with input value
- "calls onnext on Enter" — fire keydown Enter on input, verify onnext called
- "calls onprev on Shift+Enter" — fire keydown Shift+Enter, verify onprev called
- "calls onclose on Escape" — fire keydown Escape, verify onclose called
- "disables nav buttons when totalMatches=0" — verify prev/next buttons disabled

**InputDialog.test.ts** — Tests:
- "renders title" — title="Create Branch", verify text
- "renders field labels" — fields with label "Branch name", verify label text
- "renders required field marker" — field with required:true, verify "*" rendered
- "disables confirm when required field empty" — required field, confirm button disabled
- "enables confirm when required field filled" — type in required field, button enabled
- "calls onsubmit with field values" — fill fields, click confirm, verify onsubmit called with correct Record<string,string>
- "calls oncancel on Cancel click" — click cancel button, verify oncancel called
- "calls oncancel on Escape" — keydown Escape, verify oncancel called
- "calls onsubmit on Enter in text input" — keydown Enter in input, verify onsubmit called
- "renders custom confirmLabel and cancelLabel" — confirmLabel="Create", cancelLabel="Dismiss", verify text

**OperationBanner.test.ts** — Tests:
- "shows 'Merging' for merge operations" — info.op_type="Merge", verify "Merging" text
- "shows source and target branch names" — source_branch="feature", target_branch="main", verify both
- "shows 'Rebasing' for rebase operations" — op_type="Rebase", verify "Rebasing" text
- "shows 'onto' for rebase instead of 'into'" — op_type="Rebase", verify "onto" not "into"
- "shows Continue/Skip/Abort buttons for rebase" — op_type="Rebase", verify 3 buttons present
- "does not show Continue/Skip/Abort for merge" — op_type="Merge", verify buttons absent
- "shows progress for rebase" — progress="2/5", verify "(2/5)" text
- "shows cherry-pick label" — op_type="CherryPick", verify "Cherry-pick in progress" text
- Use mock invoke (from tauri-mock) — buttons call safeInvoke which is already mocked.

**PullDropdown.test.ts** — Tests:
- "renders pull/fetch button" — verify button exists
- "shows dropdown options when clicked" — click button, verify "Fetch", "Fast-forward if possible", "Fast-forward only", "Pull (rebase)" text
- "closes dropdown on second click" — click open, click again, options hidden
- Note: PullDropdown uses safeInvoke for remote operations. Since invoke is mocked, test the UI interactions (open/close, option display) not the actual remote calls.

**CommitForm.test.ts** — Tests:
- "renders Commit button in commit mode" — verify "Commit" text on button
- "renders Amend button when mode switched" — click Amend tab/button, verify "Amend" text
- "renders Stash button when mode switched" — click Stash tab/button, verify "Stash" text
- "disables submit when subject empty" — no text entered, verify button disabled
- "enables submit when subject filled and staged files exist" — stagedCount=1, type subject, verify button enabled
- Note: CommitForm uses safeInvoke for get_head_commit_message in amend mode — already mocked.

Run `bun run test` after all 5 files created.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SearchBar.test.ts exists and contains `describe("SearchBar"` and at least 5 `it(` blocks
    - src/components/InputDialog.test.ts exists and contains `describe("InputDialog"` and at least 5 `it(` blocks
    - src/components/OperationBanner.test.ts exists and contains `describe("OperationBanner"` and tests for both Merge and Rebase
    - src/components/PullDropdown.test.ts exists and contains `describe("PullDropdown"`
    - src/components/CommitForm.test.ts exists and contains `describe("CommitForm"`
    - All 5 test files import from `@testing-library/svelte`
    - All 5 test files import `../__ tests__/helpers/tauri-mock`
    - `bun run test` exits 0
  </acceptance_criteria>
  <done>5 medium component test files created, each covering rendering, user interactions, conditional logic, and form validation. All tests pass.</done>
</task>

</tasks>

<verification>
1. `bun run test` passes with 0 failures
2. `bun run check` passes
3. All 13 component test files exist: `ls src/components/*.test.ts | wc -l` returns 13
4. Every test file imports @testing-library/svelte and the shared tauri-mock
</verification>

<success_criteria>
- 13 component test files created (Toast, RefPill, BranchRow, BranchSection, RemoteGroup, DirectoryRow, FileRow, CommitRow, SearchBar, InputDialog, OperationBanner, PullDropdown, CommitForm)
- Each component has at least 3 test cases covering render, props, and events
- All tests use @testing-library/svelte render + screen queries (not manual DOM manipulation)
- All tests import shared tauri-mock for consistent Tauri module mocking
- `bun run test` green, `bun run check` green
</success_criteria>

<output>
After completion, create `.planning/phases/54-frontend-unit-tests/54-02-SUMMARY.md`
</output>
