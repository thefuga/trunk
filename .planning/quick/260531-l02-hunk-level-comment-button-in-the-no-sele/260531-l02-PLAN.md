---
quick_id: 260531-l02
phase: 260531-l02-hunk-level-comment
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
files_modified:
  - src/components/diff/HunkView.svelte
  - src/components/diff/SplitView.svelte
  - src/components/DiffPanel.svelte
  - src/lib/diff-anchor.ts
requirements: [HUNK-COMMENT-01]

must_haves:
  truths:
    - "A 'Comment' button appears in the NO-SELECTION hunk toolbar (beside Stage Hunk) for unstaged diffs, and in the commit-diff no-selection toolbar (disabled on merge commits)."
    - "Clicking it comments on the whole hunk's New-side range — no line selection required — reusing the existing composer + snapshot path."
    - "A pure-deletion hunk yields a no-op + toast (New-side scope), via the SAME resolveSide guard as line-level."
    - "The existing line-selection Comment button and commit-mode behavior are unchanged."
    - "No 'session'/'snapshot' wording is shown to the user."
  artifacts:
    - path: "src/lib/diff-anchor.ts"
      provides: "hunkSelectableIndices(hunk) pure helper — set of non-context line indices in a hunk"
      contains: "export function hunkSelectableIndices"
    - path: "src/components/DiffPanel.svelte"
      provides: "handleCommentHunk(filePath, hunkIndex) that synthesizes a full-hunk selection then delegates to handleCommentLines"
      contains: "handleCommentHunk"
---

<objective>
Let the user comment on a WHOLE HUNK without first selecting lines, by adding a "Comment" button to the NO-SELECTION state of the hunk toolbar. Builds on quick task 260531-k4j (in-diff line-level Comment). Reuse everything — same composer, same get-or-create snapshot (`ensure_working_tree_snapshot`), same New-side scope guard. The only new logic is synthesizing a full-hunk selection and surfacing a button in the no-selection toolbar branch.

DESIGN IS LOCKED (decided with user). Do NOT re-litigate. New-side only; unstaged + commit; staged untouched; FullFileView untouched.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces_verified>
DiffPanel.svelte (CONFIRMED current):
- `handleCommentLines(filePath, hunkIndex)` (line 181): if isMerge return; for diffKind==='unstaged' runs the Old-side guard via `resolveSide(fd.status, selected)` over the host `selectedLineIndices`; then ensureActiveSession(); then (unstaged) `workingTreeSnapshotOid = await safeInvoke("ensure_working_tree_snapshot", {path: repoPath})`; then sets composerFilePath/composerHunkIdx/composerOpen. The composer reads the host `selectedLineIndices` + `commitOid` ($derived: unstaged→workingTreeSnapshotOid, else commitDetail.oid) to call buildDiffAnchor.
- Host selection state: `selectedHunkKey` and `selectedLineIndices: Set<number>` (indices into a hunk's `lines` array). `closeComposer` clears workingTreeSnapshotOid.
- The composer's anchor is built from `selectedLineIndices`, so synthesizing a full-hunk selection then delegating to handleCommentLines reuses the guard + snapshot + composer with ZERO duplication.

HunkView.svelte hunk toolbar (CONFIRMED ~156-346): per diffKind, a `hasSelection` branch and an `{:else}` no-selection branch.
- unstaged `{:else}`: Discard Hunk / Stage Hunk buttons. ADD the Comment button here.
- commit: `{#if hasSelection}` Comment (selectedCount) with isMerge guard; NO `{:else}`. ADD an `{:else}` (or widen) with a Comment-Hunk button carrying the SAME isMerge disable guard.
- The selection-state Comment button markup (the accent button) is the template to reuse — NO new colors.
SplitView.svelte mirrors this — apply the same two additions.

diff-anchor.ts: `resolveSide(status, selected)` exported; line selection convention = non-context lines (origin !== 'Context').
</interfaces_verified>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hunk-level Comment button + handler (unstaged + commit), reusing the line-level path</name>
  <files>src/lib/diff-anchor.ts, src/components/DiffPanel.svelte, src/components/diff/HunkView.svelte, src/components/diff/SplitView.svelte</files>
  <behavior>
    - hunkSelectableIndices(hunk) returns the set of indices i where hunk.lines[i].origin !== 'Context' (the same lines a user could select). Unit-test: a mixed Add+Delete+Context hunk returns only the Add/Delete indices; a pure-context hunk returns empty.
    - Clicking "Comment" in the unstaged no-selection toolbar comments the whole hunk: the composer opens with an anchor spanning the hunk's New-side range, via the EXISTING snapshot + composer path (handleCommentLines delegated).
    - A pure-deletion hunk → no-op + toast (same resolveSide guard as line-level), NOT an Old-side comment.
    - Commit-diff no-selection toolbar shows a Comment button; disabled on merge commits (isMerge), enabled otherwise; comments the whole hunk via commitDetail.oid.
    - Existing line-selection Comment button + commit-mode behavior unchanged; staged + FullFileView untouched.
  </behavior>
  <action>
    1. diff-anchor.ts: add `export function hunkSelectableIndices(hunk: Hunk): Set<number>` returning indices of non-context lines (mirror the `isSelectable = origin !== 'Context'` convention). Pure, unit-testable. Add a vitest covering mixed / pure-add / pure-delete / pure-context hunks.
    2. DiffPanel.svelte: add `async function handleCommentHunk(filePath: string, hunkIndex: number)` that: finds the FileDiff + hunk; builds the full-hunk selection via hunkSelectableIndices; sets `selectedHunkKey = `${filePath}-${hunkIndex}`` and `selectedLineIndices = <that set>`; then `await handleCommentLines(filePath, hunkIndex)`. This reuses the isMerge guard, the unstaged Old-side guard (pure-delete hunk → toast+return), ensureActiveSession, the snapshot fetch, and the composer verbatim. (If the synthesized set is empty — e.g. a degenerate hunk — return without opening.) Wire a new `oncommenthunk` prop to the diff views (mirror how `oncommentlines` is threaded at ~line 685).
    3. HunkView.svelte + SplitView.svelte: accept an `oncommenthunk` prop (sibling of oncommentlines). In the unstaged `{:else}` (no-selection) branch, add a Comment button (reuse the accent Comment-button markup verbatim, label "Comment") beside Stage Hunk, calling `oncommenthunk(fd.path, hunkIdx)`. In the commit branch, add an `{:else}` no-selection arm with the same accent Comment button, carrying the SAME `disabled={isMerge}` + merge tooltip the selection-state commit Comment button uses, calling `oncommenthunk(fd.path, hunkIdx)`. Do NOT touch the staged branch or FullFileView. NO inline colors.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && just check</automated>
  </verify>
  <done>hunkSelectableIndices exported + unit-tested; handleCommentHunk delegates to handleCommentLines; Comment button present in unstaged no-selection (beside Stage Hunk) and commit no-selection (merge-disabled) toolbars in BOTH HunkView and SplitView; staged + FullFileView untouched; line-level + commit-mode behavior unchanged; `just check` green (UNFILTERED — never a filtered cargo test).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Consolidated in-app UAT (k4j line-level + l02 hunk-level)</name>
  <action>Human-verification checkpoint. `just check` CANNOT verify these GUI flows. The implementer pauses; the user runs the UAT below via `just dev`.</action>
  <what-built>A "Comment" button in the no-selection hunk toolbar (unstaged beside Stage Hunk; commit, merge-disabled) that comments the whole hunk's New-side range — plus (from k4j, still unverified) the line-selection Comment for unstaged.</what-built>
  <how-to-verify>
    Run `just dev`, open a repo with uncommitted changes:
    1. UNSTAGED hunk, NO line selection → a "Comment" button appears beside Stage Hunk. Click → composer opens (no "session"/"snapshot" wording) → submit → the comment appears in the generated review doc, anchored to the hunk's new-side range.
    2. Same in the SPLIT view layout.
    3. (k4j) Select specific New-side lines → the line-level Comment still works.
    4. COMMIT diff (select a commit, view a file), NO selection → a "Comment" button appears; on a MERGE commit it is disabled.
    5. (Negative) A pure-deletion hunk (only `-` lines) → clicking Comment is a no-op with a toast ("removed lines isn't supported yet").
    6. Edit a file mid-review and comment again → BOTH comments survive in the review (earlier not orphaned).
  </how-to-verify>
  <verify><human-check>All six pass in-app.</human-check></verify>
  <done>User confirms all six (or files issues).</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `just check` after Task 1 (fmt, biome, svelte-check, clippy, cargo-test UNFILTERED, vitest) + the hunkSelectableIndices unit test.
- The blocking human-verify checkpoint is the ONLY verification of the GUI wiring; `just check` cannot cover it.
</verification>

<success_criteria>
- Hunk-level Comment button in the no-selection toolbar (unstaged + commit, both HunkView & SplitView); commit-mode merge-disabled.
- Whole-hunk comment reuses the line-level path (snapshot + composer + Old-side guard) via a synthesized full-hunk selection — no duplicated logic.
- Pure-deletion hunk → no-op + toast. Staged + FullFileView untouched. Line-level + commit-mode unchanged. No inline colors.
</success_criteria>

<output>
Create `.planning/quick/260531-l02-hunk-level-comment-button-in-the-no-sele/260531-l02-SUMMARY.md` when done.
</output>
