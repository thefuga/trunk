---
phase: 38-merge-editor
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MergeEditor.svelte
autonomous: true
gap_closure: true
requirements: [CONF-03]

must_haves:
  truths:
    - "Scrolling any of the three panels (Current, Incoming, Output) scrolls the other two to the same scrollTop position"
  artifacts:
    - path: "src/components/MergeEditor.svelte"
      provides: "Three-way scroll sync including output textarea"
      contains: "panelRefs[2]"
  key_links:
    - from: "output textarea"
      to: "panelRefs[2]"
      via: "bind:this and onscroll handler"
      pattern: "bind:this=.panelRefs\\[2\\]"
---

<objective>
Fix output panel scroll sync gap: the output textarea is not wired into the three-way synchronized scroll system. Currently only panelRefs[0] (Current) and panelRefs[1] (Incoming) participate in scroll sync -- panelRefs[2] is never bound.

Purpose: Close CONF-03 verification gap so all three panels scroll in sync as required.
Output: Updated MergeEditor.svelte with output textarea participating in scroll sync.
</objective>

<execution_context>
@/Users/joaofnds/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-merge-editor/38-03-SUMMARY.md
@.planning/phases/38-merge-editor/38-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire output textarea into scroll sync system</name>
  <files>src/components/MergeEditor.svelte</files>
  <read_first>src/components/MergeEditor.svelte</read_first>
  <action>
In MergeEditor.svelte, make these specific changes:

1. Change the panelRefs type declaration (line 36) from:
   `let panelRefs: HTMLDivElement[] = [];`
   to:
   `let panelRefs: HTMLElement[] = [];`
   This allows the array to hold both HTMLDivElement (top panels) and HTMLTextAreaElement (output).

2. Add `bind:this={panelRefs[2]}` and `onscroll={() => handleScroll(2)}` to the output textarea element (currently at line 634). The textarea currently has:
   ```
   <textarea
     value={outputText}
     oninput={handleOutputEdit}
     style="..."
   ></textarea>
   ```
   Change it to:
   ```
   <textarea
     bind:this={panelRefs[2]}
     value={outputText}
     oninput={handleOutputEdit}
     onscroll={() => handleScroll(2)}
     style="..."
   ></textarea>
   ```

No other changes needed. The existing handleScroll function (lines 74-84) already handles any number of panelRefs entries -- it iterates with forEach and skips the source index, so adding panelRefs[2] will automatically participate in the sync loop.
  </action>
  <verify>
    <automated>grep -n 'panelRefs\[2\]' src/components/MergeEditor.svelte | grep -c 'bind:this'</automated>
  </verify>
  <acceptance_criteria>
    - MergeEditor.svelte contains `let panelRefs: HTMLElement[] = [];` (not HTMLDivElement)
    - The output textarea element contains `bind:this={panelRefs[2]}`
    - The output textarea element contains `onscroll={() => handleScroll(2)}`
    - panelRefs[0] still bound to Current panel div, panelRefs[1] still bound to Incoming panel div
    - No other changes to handleScroll function or any other part of the component
  </acceptance_criteria>
  <done>Output textarea participates in three-way scroll sync -- scrolling any panel scrolls all three</done>
</task>

</tasks>

<verification>
- `grep -n 'panelRefs\[2\]' src/components/MergeEditor.svelte` shows bind:this on output textarea
- `grep -n 'handleScroll(2)' src/components/MergeEditor.svelte` shows onscroll handler on output textarea
- `grep -n 'HTMLElement\[\]' src/components/MergeEditor.svelte` shows updated type
- `grep -c 'panelRefs\[' src/components/MergeEditor.svelte` returns at least 3 (indices 0, 1, 2 in bind:this)
- `cd src-tauri && cargo test && cd .. && npx vitest run` -- all existing tests still pass
</verification>

<success_criteria>
All three merge editor panels (Current, Incoming, Output) participate in synchronized scroll via the panelRefs array and handleScroll guard-flag pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/38-merge-editor/38-05-SUMMARY.md`
</output>
