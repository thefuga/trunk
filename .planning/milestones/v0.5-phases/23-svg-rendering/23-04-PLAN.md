---
phase: 23-svg-rendering
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/graph-constants.test.ts
  - src/lib/overlay-paths.test.ts
autonomous: true
gap_closure: true
requirements: [OVRL-04, CURV-03]

must_haves:
  truths:
    - "Full test suite passes with OVERLAY_ROW_HEIGHT=26 (0 failures)"
    - "graph-constants.test.ts asserts OVERLAY_ROW_HEIGHT toBe(26)"
    - "overlay-paths.test.ts uses ROW=26 for all Y-coordinate assertions"
  artifacts:
    - path: "src/lib/graph-constants.test.ts"
      provides: "Updated assertion for OVERLAY_ROW_HEIGHT"
      contains: "toBe(26)"
    - path: "src/lib/overlay-paths.test.ts"
      provides: "Updated ROW constant for test helpers"
      contains: "const ROW = 26"
  key_links:
    - from: "src/lib/graph-constants.test.ts"
      to: "src/lib/graph-constants.ts"
      via: "import and assertion"
      pattern: "OVERLAY_ROW_HEIGHT.*toBe\\(26\\)"
    - from: "src/lib/overlay-paths.test.ts"
      to: "src/lib/graph-constants.ts"
      via: "mirrored constant value"
      pattern: "const ROW = 26"
---

<objective>
Fix 11 broken tests caused by stale OVERLAY_ROW_HEIGHT=36 in test files after plan 23-03 changed the production constant to 26.

Purpose: Plan 23-03 correctly fixed OVERLAY_ROW_HEIGHT from 36→26 in production code but did not update two test files that mirror this value. This is a test regression blocker — CI fails with 11 broken tests.
Output: All 51 tests in both files pass (11 currently failing → 0 failures).
</objective>

<execution_context>
@/Users/joaofnds/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-svg-rendering/23-VERIFICATION.md
@.planning/phases/23-svg-rendering/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stale OVERLAY_ROW_HEIGHT values in test files</name>
  <files>src/lib/graph-constants.test.ts, src/lib/overlay-paths.test.ts</files>
  <action>
Two test files hardcode the old OVERLAY_ROW_HEIGHT=36 value. Update both to match the production value of 26:

1. **src/lib/graph-constants.test.ts** line 19:
   Change `toBe(36)` → `toBe(26)` in the `'OVERLAY_ROW_HEIGHT is 36'` test.
   Also update the test description string from `'OVERLAY_ROW_HEIGHT is 36'` → `'OVERLAY_ROW_HEIGHT is 26'`.

2. **src/lib/overlay-paths.test.ts** line 7:
   Change `const ROW = 36` → `const ROW = 26`.
   This single change fixes all 10 failing tests in this file because `cy()`, `rowTop()`, and `rowBottom()` helper functions all derive from `ROW`.
   Also update the comment on line 226 from `cy(1)=54` → `cy(1)=39` (since 1*26+26/2=39, not 1*36+36/2=54).

No other changes needed — all test logic and assertions are correct; only the constant value is stale.
  </action>
  <verify>
    <automated>npx vitest run src/lib/graph-constants.test.ts src/lib/overlay-paths.test.ts</automated>
  </verify>
  <done>All 51 tests pass (0 failures). Both test files assert against OVERLAY_ROW_HEIGHT=26.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/graph-constants.test.ts src/lib/overlay-paths.test.ts` — 51/51 pass, 0 failures
2. `npx vitest run` — full suite passes (no regressions introduced)
</verification>

<success_criteria>
- 11 previously-failing tests now pass
- No test regressions in full suite
- graph-constants.test.ts asserts OVERLAY_ROW_HEIGHT is 26
- overlay-paths.test.ts uses ROW=26 for all coordinate computation
</success_criteria>

<output>
After completion, create `.planning/phases/23-svg-rendering/23-04-SUMMARY.md`
</output>
