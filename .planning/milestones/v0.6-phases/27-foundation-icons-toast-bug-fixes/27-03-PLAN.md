---
phase: 27-foundation-icons-toast-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/Toolbar.svelte
  - src/components/PullDropdown.svelte
  - src/components/FileRow.svelte
  - src/components/StagingPanel.svelte
  - src/components/BranchSection.svelte
  - src/components/BranchRow.svelte
  - src/components/TabBar.svelte
autonomous: false
requirements:
  - ICON-01
must_haves:
  truths:
    - "No Unicode HTML entities (&#8617;, &#9095;, etc.) or Unicode symbol literals (✎, ×, ▼, ▶, ↓, ↑) remain in any of the 7 component files"
    - "Each replaced symbol renders as a Lucide SVG icon component with the correct size"
    - "`@lucide/svelte` is listed in package.json dependencies and `node_modules/@lucide/svelte` exists"
  artifacts:
    - path: "package.json"
      provides: "@lucide/svelte dependency"
      contains: "@lucide/svelte"
    - path: "src/components/Toolbar.svelte"
      provides: "SVG icons for undo, redo, pull, push, branch, stash, pop"
      contains: "@lucide/svelte"
    - path: "src/components/FileRow.svelte"
      provides: "SVG icons for file status (New/Modified/Deleted/Renamed/Typechange/Conflicted) and stage/unstage actions"
      contains: "@lucide/svelte"
    - path: "src/components/StagingPanel.svelte"
      provides: "SVG chevron icons for section expand/collapse"
      contains: "@lucide/svelte"
    - path: "src/components/BranchSection.svelte"
      provides: "SVG chevron and Plus icons"
      contains: "@lucide/svelte"
    - path: "src/components/BranchRow.svelte"
      provides: "SVG ArrowDown/ArrowUp for behind/ahead tracking counts"
      contains: "@lucide/svelte"
    - path: "src/components/TabBar.svelte"
      provides: "SVG X icon for tab close"
      contains: "@lucide/svelte"
    - path: "src/components/PullDropdown.svelte"
      provides: "SVG ChevronDown icon for dropdown trigger"
      contains: "@lucide/svelte"
  key_links:
    - from: "src/components/Toolbar.svelte"
      to: "@lucide/svelte"
      via: "named import"
      pattern: "from '@lucide/svelte'"
    - from: "src/components/FileRow.svelte"
      to: "@lucide/svelte"
      via: "named import of FilePlus, FilePen, FileMinus, FileSymlink, FileType2, FileWarning, Plus, Minus"
      pattern: "from '@lucide/svelte'"
---

<objective>
Install `@lucide/svelte` and replace all Unicode symbols across the 7 affected UI components with the corresponding Lucide SVG icon components.

Purpose: ICON-01 — consistent visual vocabulary; SVG icons render crisply at all DPIs, unlike Unicode glyphs that vary by platform font.
Output: 7 updated components with zero Unicode symbol literals; package.json updated.
</objective>

<execution_context>
@/Users/joaofnds/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-foundation-icons-toast-bug-fixes/27-RESEARCH.md
@src/components/Toolbar.svelte
@src/components/PullDropdown.svelte
@src/components/FileRow.svelte
@src/components/StagingPanel.svelte
@src/components/BranchSection.svelte
@src/components/BranchRow.svelte
@src/components/TabBar.svelte

<interfaces>
<!-- Complete icon mapping from 27-RESEARCH.md — use this as the authoritative mapping -->

| Component | Current Symbol | Lucide Icon | Size |
|-----------|---------------|-------------|------|
| Toolbar | &#8617; Undo | `Undo2` | 14 |
| Toolbar | &#8618; Redo | `Redo2` | 14 |
| Toolbar | &#8595; Pull | `ArrowDown` | 14 |
| Toolbar | &#8593; Push | `ArrowUp` | 14 |
| Toolbar | &#9095; Branch | `GitBranch` | 14 |
| Toolbar | &#128230; Stash | `Archive` | 14 |
| Toolbar | &#128229; Pop | `PackageOpen` | 14 |
| PullDropdown | &#9662; chevron | `ChevronDown` | 12 |
| FileRow | + (New status) | `FilePlus` | 12 |
| FileRow | ✎ (Modified) | `FilePen` | 12 |
| FileRow | − (Deleted) | `FileMinus` | 12 |
| FileRow | → (Renamed) | `FileSymlink` | 12 |
| FileRow | ⇄ (Typechange) | `FileType2` | 12 |
| FileRow | ! (Conflicted) | `FileWarning` | 12 |
| FileRow action | + (stage btn) | `Plus` | 11 |
| FileRow action | − (unstage btn) | `Minus` | 11 |
| StagingPanel | ▼ expand | `ChevronDown` | 12 |
| StagingPanel | ▶ collapse | `ChevronRight` | 12 |
| BranchSection | ▼ expand | `ChevronDown` | 12 |
| BranchSection | ▶ collapse | `ChevronRight` | 12 |
| BranchSection | + create | `Plus` | 12 |
| BranchRow | \u2193 behind | `ArrowDown` | 11 |
| BranchRow | \u2191 ahead | `ArrowUp` | 11 |
| TabBar | × close | `X` | 12 |

<!-- FileRow currently uses a STATUS_ICONS record mapping FileStatusType → { symbol, color }.
     Replace with a STATUS_ICON_COMPONENTS record mapping FileStatusType → Lucide component.
     Keep the color system — pass color as a prop or CSS variable to the icon. -->

<!-- Lucide usage syntax for Svelte 5:
import { Undo2, ArrowDown, GitBranch } from '@lucide/svelte';
<Undo2 size={14} />                   // inherits currentColor
<FilePlus size={12} color="#22c55e" /> // explicit color
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @lucide/svelte and update Toolbar + PullDropdown</name>
  <files>package.json, src/components/Toolbar.svelte, src/components/PullDropdown.svelte</files>
  <action>
**Step 1: Install the package**
```bash
npm install @lucide/svelte
```
Verify `"@lucide/svelte"` appears in `package.json` `dependencies` (not devDependencies).

**CRITICAL: Use `@lucide/svelte` (Svelte 5 package), NOT `lucide-svelte` (Svelte 4 package).** The project uses Svelte 5 (`"svelte": "^5.0.0"` in package.json). Using the wrong package will cause `SvelteComponent` type errors.

**Step 2: Update Toolbar.svelte**
At top of `<script lang="ts">`:
```svelte
import { Undo2, Redo2, ArrowDown, ArrowUp, GitBranch, Archive, PackageOpen } from '@lucide/svelte';
```

Replace each button's text/entity with the corresponding icon component at `size={14}`:
- `&#8617; Undo` → `<Undo2 size={14} /> Undo`
- `&#8618; Redo` → `<Redo2 size={14} /> Redo`
- `&#8595; Pull` → `<ArrowDown size={14} /> Pull`
- `&#8593; Push` → `<ArrowUp size={14} /> Push`
- `&#9095; Branch` → `<GitBranch size={14} /> Branch`
- `&#128230; Stash` → `<Archive size={14} /> Stash`
- `&#128229; Pop` → `<PackageOpen size={14} /> Pop`

Icons use `currentColor` by default — they will inherit the button's text color automatically. Do not set an explicit `color` prop on Toolbar icons.

**Step 3: Update PullDropdown.svelte**
Replace `&#9662;` with `<ChevronDown size={12} />` (import from `@lucide/svelte`).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>`@lucide/svelte` installed. Toolbar and PullDropdown compile. No HTML entities (`&#...;`) remain in those two files. Full Vitest suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Unicode symbols in FileRow, StagingPanel, BranchSection, BranchRow, TabBar</name>
  <files>src/components/FileRow.svelte, src/components/StagingPanel.svelte, src/components/BranchSection.svelte, src/components/BranchRow.svelte, src/components/TabBar.svelte</files>
  <action>
**FileRow.svelte**

Current code uses `STATUS_ICONS: Record<FileStatusType, StatusIcon>` with `{ symbol: string, color: string }`. Replace the symbol string with a Lucide component reference.

New approach — replace `STATUS_ICONS` with a component map:
```typescript
import { FilePlus, FilePen, FileMinus, FileSymlink, FileType2, FileWarning, Plus, Minus } from '@lucide/svelte';
import type { Component } from 'svelte';

type StatusIconConfig = { component: Component<any>; color: string };
const STATUS_ICON_COMPONENTS: Record<FileStatusType, StatusIconConfig> = {
  New:        { component: FilePlus,     color: '#22c55e' },
  Modified:   { component: FilePen,      color: '#fb923c' },
  Deleted:    { component: FileMinus,    color: '#f87171' },
  Renamed:    { component: FileSymlink,  color: '#60a5fa' },
  Typechange: { component: FileType2,    color: '#a78bfa' },
  Conflicted: { component: FileWarning,  color: '#facc15' },
};
let iconConfig = $derived(STATUS_ICON_COMPONENTS[file.status] ?? { component: FilePen, color: 'var(--color-text-muted)' });
```

In the template, replace the text span with:
```svelte
<svelte:component this={iconConfig.component} size={12} color={iconConfig.color} />
```

For the action buttons (stage `+` / unstage `−`): replace the literal `+`/`−` text with:
```svelte
{#if actionLabel === '+'}
  <Plus size={11} />
{:else}
  <Minus size={11} />
{/if}
```
(import `Plus` and `Minus` from `@lucide/svelte`)

**StagingPanel.svelte**

Import `ChevronDown, ChevronRight` from `@lucide/svelte`.

Replace both section toggle literals:
- `{unstaged_expanded ? '▼' : '▶'}` → `{#if unstaged_expanded}<ChevronDown size={12} />{:else}<ChevronRight size={12} />{/if}`
- `{staged_expanded ? '▼' : '▶'}` → `{#if staged_expanded}<ChevronDown size={12} />{:else}<ChevronRight size={12} />{/if}`

**BranchSection.svelte**

Import `ChevronDown, ChevronRight, Plus` from `@lucide/svelte`.

- Section toggle `{expanded ? '▼' : '▶'}` → `{#if expanded}<ChevronDown size={12} />{:else}<ChevronRight size={12} />{/if}`
- Create button `+` text → `<Plus size={12} />`

**BranchRow.svelte**

Import `ArrowDown, ArrowUp` from `@lucide/svelte`.

Replace the tracking indicators:
- `{'\u2193'}{behind}` → `<ArrowDown size={11} />{behind}`
- `{'\u2191'}{ahead}` → `<ArrowUp size={11} />{ahead}`

**TabBar.svelte**

Import `X` from `@lucide/svelte`.

Replace the close button `×` text → `<X size={12} />`.

---

**Anti-patterns:**
- Do NOT use `import * from '@lucide/svelte'` — always use named imports to keep bundle size minimal
- Do NOT set `strokeWidth` prop unless explicitly needed — the default of 2 matches the design system
- Preserve all existing `onclick`, `class`, `aria-label`, and `disabled` attributes on buttons — only replace the inner content symbol
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>All 5 components updated. No Unicode symbol literals (▼, ▶, ✎, ×, ↓, ↑, +, −) remain in these files as raw text (Lucide components replace them all). Full Vitest suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All 7 components updated with `@lucide/svelte` SVG icons replacing Unicode symbols. Package installed. Full test suite green.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` (or `cargo tauri dev`) to start the app
2. Toolbar: confirm all 7 buttons show SVG icons (Undo, Redo, Pull, Push, Branch, Stash, Pop) — no `↵`, `↺`, `↓`, `↑`, `⎇`, `📦`, `📥`
3. File list: create/modify/delete a file — confirm file status icons are SVG (FilePlus green, FilePen orange, FileMinus red)
4. File row hover: confirm stage/unstage buttons show Plus/Minus SVG icons
5. Staging panel section headers: confirm `▼`/`▶` are replaced with Chevron SVG icons
6. Branch sidebar: confirm section expand/collapse uses ChevronDown/Right; branch with upstream tracking shows ArrowDown/Up
7. Tab bar: confirm the × close button is now an X SVG icon
8. PullDropdown: click the pull button's dropdown arrow — confirm ChevronDown SVG
  </how-to-verify>
  <resume-signal>Type "approved" to confirm icons look correct, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
- `npm install @lucide/svelte` succeeded; package.json updated
- `npx vitest run` passes (no regressions from icon changes)
- Zero HTML entities (`&#[0-9]+;`) in Toolbar.svelte and PullDropdown.svelte
- Zero Unicode symbol literals (▼, ▶, ✎, ×, \u2193, \u2191) in FileRow, StagingPanel, BranchSection, BranchRow, TabBar
- Human checkpoint confirms icons are visually correct
</verification>

<success_criteria>
ICON-01 satisfied: all toolbar buttons, file rows, sidebar sections, and tab bar items display Lucide SVG icons. No Unicode symbols remain in the 7 component files.
</success_criteria>

<output>
After completion, create `.planning/phases/27-foundation-icons-toast-bug-fixes/27-03-SUMMARY.md`
</output>
