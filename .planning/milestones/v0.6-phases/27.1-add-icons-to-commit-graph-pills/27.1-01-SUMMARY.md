---
phase: 27.1-add-icons-to-commit-graph-pills
plan: 01
subsystem: ui
tags: [svelte, lucide, commit-graph, icons, svg]

# Dependency graph
requires: []
provides:
  - Lucide icons (Laptop, Globe, Tag, Archive) in all four commit graph pill types
  - PILL_ICONS lookup map matching sidebar BranchRow icon vocabulary
  - Uniform ICON_WIDTH inclusion for all ref types in pill width calculation
affects: [commit-graph, ref-pills, ui-polish]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "PILL_ICONS Record<string, typeof Laptop> lookup map for dynamic icon selection"
    - "foreignObject flex layout combining icon + label for crisp SVG text rendering"

key-files:
  created: []
  modified:
    - src/lib/ref-pill-data.ts
    - src/lib/ref-pill-data.test.ts
    - src/components/CommitGraph.svelte

key-decisions:
  - "All ref types unconditionally use ICON_WIDTH — removes hasIcon() branching and makes pill widths uniform"
  - "foreignObject flex layout replaces separate SVG <path> icons — flexbox distributes icon+text space automatically"
  - "PILL_ICONS map mirrors BranchRow.svelte pattern — consistent icon vocabulary across sidebar and graph"

patterns-established:
  - "PILL_ICONS map: use Record<string, typeof Laptop> for Svelte 5 compatible icon lookup"
  - "Dynamic icon in template: use {@const PillIcon = PILL_ICONS[type]} pattern for type-safe rendering"

requirements-completed: [PILL-ICON-01]

# Metrics
duration: 2min
completed: 2026-03-15
---

# Phase 27.1 Plan 01: Add Icons to Commit Graph Pills Summary

**Lucide icons (Laptop, Globe, Tag, Archive) added to all four commit graph pill types, replacing hand-drawn SVG path icons and adding icons to branch pills that previously had none**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-15T18:49:31Z
- **Completed:** 2026-03-15T18:52:13Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- All four ref pill types (LocalBranch, RemoteBranch, Tag, Stash) now display Lucide icons left of the label
- Removed hand-drawn diamond SVG path for Tag pills and flag SVG path for Stash pills
- Added Laptop/Globe icons to LocalBranch/RemoteBranch pills that previously had no icon
- Icon vocabulary now matches BranchRow.svelte sidebar (same Laptop, Globe, Tag icons)
- Hover expansion tooltips (both multi-ref and truncated single-ref) show icons alongside ref names
- Eliminated all hardcoded `pill.refType === 'Tag' || pill.refType === 'Stash'` conditionals

## Task Commits

Each task was committed atomically:

1. **Task 1: Expand icon width to all ref types and update tests** - `8de19a2` (feat)
2. **Task 2: Replace hand-drawn SVG icons with Lucide components in pills and hover tooltips** - `311b032` (feat)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified
- `src/lib/ref-pill-data.ts` - Removed `hasIcon()` function; all ref types now unconditionally use `ICON_WIDTH`
- `src/lib/ref-pill-data.test.ts` - Updated 2 tests to expect `ICON_WIDTH` included for branch pill widths
- `src/components/CommitGraph.svelte` - Added Lucide imports, PILL_ICONS map, replaced hand-drawn paths with icon+text flex layout

## Decisions Made
- Used `const PILL_ICONS: Record<string, typeof Laptop>` map pattern — mirrors BranchRow.svelte's `kindIcon` approach for consistent icon vocabulary
- All ref types unconditionally include `ICON_WIDTH` — removes conditional branching and simplifies pill width calculation
- foreignObject with flex layout replaces position-calculated SVG paths — flexbox handles icon+text spacing naturally without coordinate math

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 27.1 complete with all pill icon changes implemented and tested
- Visual consistency between commit graph pills and sidebar BranchRow achieved
- Ready for transition to next planned phase

---
*Phase: 27.1-add-icons-to-commit-graph-pills*
*Completed: 2026-03-15*
