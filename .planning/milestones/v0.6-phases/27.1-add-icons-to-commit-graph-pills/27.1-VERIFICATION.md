---
phase: 27.1-add-icons-to-commit-graph-pills
verified: 2026-03-15T15:56:00Z
status: human_needed
score: 5/5 must-haves verified
re_verification: false
human_verification:
  - test: "Open any repository and inspect the commit graph ref pills"
    expected: "Local branch pills show Laptop icon, remote branch pills show Globe icon, tag pills show Lucide Tag icon (not a hand-drawn diamond), stash pills show Archive icon (not a hand-drawn flag) — all left of the label text"
    why_human: "Visual appearance of SVG foreignObject+Lucide components inside the graph overlay cannot be verified programmatically; icon rendering at small sizes (10px) needs real render validation"
  - test: "Hover over a multi-ref pill (one with +N badge) and inspect the tooltip"
    expected: "Each ref in the expanded list shows its appropriate icon (Laptop/Globe/Tag/Archive) to the left of the ref name"
    why_human: "Hover expansion tooltip behavior and icon rendering at runtime requires visual inspection"
  - test: "Hover over a truncated pill (label ending in '…') and inspect the tooltip"
    expected: "The full expanded pill shows the ref icon (matching the ref type) left of the full label"
    why_human: "Single-ref truncated hover tooltip icon rendering requires runtime visual inspection"
---

# Phase 27.1: Add Icons to Commit Graph Pills — Verification Report

**Phase Goal:** All four ref pill types (local branch, remote branch, tag, stash) in the commit graph SVG overlay display Lucide icons matching their sidebar/toolbar counterparts, replacing hand-drawn SVG path icons
**Verified:** 2026-03-15T15:56:00Z
**Status:** ✅ human_needed (all automated checks passed — visual render needs human confirmation)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                            | Status     | Evidence                                                                 |
| --- | ---------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------ |
| 1   | Local branch pills display a Laptop icon                         | ✓ VERIFIED | `PILL_ICONS.LocalBranch = Laptop` in CommitGraph.svelte line 40; `{#if PILL_ICONS[pill.refType]}` renders icon for all types line 668 |
| 2   | Remote branch pills display a Globe icon                         | ✓ VERIFIED | `PILL_ICONS.RemoteBranch = Globe` in CommitGraph.svelte line 41          |
| 3   | Tag pills display a Lucide Tag icon (not hand-drawn diamond)     | ✓ VERIFIED | `PILL_ICONS.Tag = Tag` at line 42; no `<path d="M...l 4 -4">` diamond paths remain |
| 4   | Stash pills display an Archive icon (not hand-drawn flag)        | ✓ VERIFIED | `PILL_ICONS.Stash = Archive` at line 43; no `<path d="M...v -8 h 5">` flag paths remain |
| 5   | Hover expansion tooltips show icons alongside ref names          | ✓ VERIFIED | Multi-ref hover: `{#if PILL_ICONS[ref.ref_type]}` at line 737; single-ref hover: `{#if PILL_ICONS[hoveredPill.refType]}` at line 766 |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/lib/ref-pill-data.ts` | All ref types include `ICON_WIDTH` in pill sizing; `hasIcon()` removed | ✓ VERIFIED | Line 94: `const iconWidth = ICON_WIDTH;` — unconditional for all types. `hasIcon()` function not present. |
| `src/components/CommitGraph.svelte` | Lucide icon rendering for all pill types | ✓ VERIFIED | Import `{ Laptop, Globe, Tag, Archive }` at line 21; `PILL_ICONS` map at lines 39–44; icon rendering via `{#if PILL_ICONS[pill.refType]}` pattern |

---

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `src/lib/ref-pill-data.ts` | `src/components/CommitGraph.svelte` | `ICON_WIDTH` used in pill width for all types; CommitGraph imports `Laptop, Globe, Tag, Archive` and renders via `PILL_ICONS[pill.refType]` | ✓ WIRED | `ref-pill-data.ts` unconditionally sets `iconWidth = ICON_WIDTH` (line 94) for all ref types. `CommitGraph.svelte` PILL_ICONS map covers all 4 types. `{#if PILL_ICONS[pill.refType]}` renders icons. Pattern verified in all three pill-rendering locations (main pill, multi-ref hover, single-ref hover). |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| PILL-ICON-01 | 27.1-01-PLAN.md | Add Lucide icons to all four ref pill types in commit graph, replacing hand-drawn SVG path icons | ✓ SATISFIED | All four icons implemented (Laptop/Globe/Tag/Archive); hand-drawn `<path>` icons absent from pill rendering section; `PILL_ICONS` map fully covers `LocalBranch`, `RemoteBranch`, `Tag`, `Stash` |

**Note:** `PILL-ICON-01` is referenced in ROADMAP.md under Phase 27.1 but does **not** appear as a formally defined requirement ID in `REQUIREMENTS.md` (the file defines `ICON-01`, `GITOP-*`, `STAGE-*`, etc. — not `PILL-ICON-01`). The traceability table in REQUIREMENTS.md also does not map PILL-ICON-01 to Phase 27.1. This is an informational gap: the plan references a requirement ID that exists only in the ROADMAP.md `**Requirements**:` field, not in the canonical REQUIREMENTS.md document. The implementation itself is fully correct; the documentation inconsistency should be noted for record-keeping.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `CommitGraph.svelte` | 135 | `placeholder?` in InputDialog `fields` interface — pre-existing unrelated field | ℹ️ Info | None — this is a legitimate optional interface field, not a stub |

No blocker or warning-level anti-patterns found in the three modified files (`ref-pill-data.ts`, `ref-pill-data.test.ts`, `CommitGraph.svelte`).

---

### Test Results

All 17 tests in `src/lib/ref-pill-data.test.ts` pass:

- Line 122: `pill.width === 28 + PILL_PADDING_X * 2 + ICON_WIDTH` — LocalBranch pill now includes `ICON_WIDTH` ✓
- Line 218: `pill.width === 21 + PILL_PADDING_X * 2 + ICON_WIDTH` — branch pill width formula updated ✓
- Line 227: Tag pill still includes `ICON_WIDTH` ✓
- Line 238: Stash pill still includes `ICON_WIDTH` ✓

---

### Human Verification Required

#### 1. All Four Pill Types Render Icons

**Test:** Open any repository in the app with local branches, remote-only branches, tags, and stash entries visible in the commit graph
**Expected:** Each pill type shows its icon (Laptop for local branch, Globe for remote, Tag icon for tag, Archive for stash) to the left of the label text inside the colored pill
**Why human:** Lucide components inside SVG `foreignObject` elements render as HTML; their actual pixel appearance at 10px size requires a running browser

#### 2. Multi-Ref Hover Expansion Shows Icons

**Test:** Find a commit with multiple refs (shown as a pill with a `+N` badge), hover over it
**Expected:** The expanded tooltip shows each ref on its own row with the appropriate icon before the ref name
**Why human:** Hover tooltip DOM insertion and icon rendering require runtime interaction

#### 3. Single-Ref Truncated Hover Shows Icon

**Test:** Find a commit whose ref name is truncated (label ends with `…`), hover over the pill
**Expected:** The expanded pill shows the full ref name with the appropriate icon to its left
**Why human:** Truncation depends on rendered text width and requires a running browser to trigger

---

### Implementation Quality Notes

- **Icon vocabulary consistency:** `PILL_ICONS` map uses identical icons to `BranchRow.svelte` (`Laptop`, `Globe`, `Tag`) — stash uses `Archive` matching the toolbar Stash button, which is correct since `BranchRow` doesn't handle stash
- **No hardcoded `Tag || Stash` conditionals remain** — confirmed by `grep -n "refType.*Tag\|refType.*Stash"` returning no results from pill-rendering code
- **`foreignObject` flex layout** eliminates coordinate math — all icon+text spacing handled by CSS flexbox with `gap: 2px`
- **`hasIcon()` fully removed** — `ref-pill-data.ts` now uses `const iconWidth = ICON_WIDTH;` unconditionally (line 94, confirmed by direct code read)
- **Commits verified:** `8de19a2` (expand icon width to all ref types) and `311b032` (replace hand-drawn SVG icons) both exist in git history

---

### Gaps Summary

No gaps found. All automated checks passed:
- ✓ `hasIcon()` removed from `ref-pill-data.ts`
- ✓ `ICON_WIDTH` unconditional for all 4 ref types
- ✓ `PILL_ICONS` map covers all 4 ref types in `CommitGraph.svelte`
- ✓ Lucide imports (`Laptop`, `Globe`, `Tag`, `Archive`) present
- ✓ Icon rendering in main pills, multi-ref hover, single-ref hover
- ✓ No hand-drawn `<path>` icons in pill rendering section
- ✓ No hardcoded `refType === 'Tag' || refType === 'Stash'` conditionals
- ✓ All 17 tests pass
- ✓ PILL-ICON-01 requirement satisfied by implementation

3 items require human visual confirmation (all runtime/visual, not logic gaps).

---

_Verified: 2026-03-15T15:56:00Z_
_Verifier: Claude (gsd-verifier)_
