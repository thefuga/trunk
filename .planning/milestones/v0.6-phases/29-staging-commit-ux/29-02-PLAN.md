---
phase: 29-staging-commit-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/StagingPanel.svelte
  - src/components/FileRow.svelte
autonomous: true
requirements: [STAGE-03, STAGE-04, STAGE-05]

must_haves:
  truths:
    - "Stage All Changes button has green (#22c55e) filled background with white text"
    - "Unstage All button has red (#f87171) filled background with white text"
    - "Discard All button has red (#f87171) filled background with white text"
    - "Individual file row Plus (+) icon is green (#22c55e) when hovered"
    - "Individual file row Minus (−) icon is red (#f87171) when hovered"
    - "When both unstaged and staged sections are expanded, they each take exactly 50% of available space"
    - "When one section is collapsed, the other expands to 100%"
    - "Each file list section has its own independent scroll container"
    - "Section headers always visible even when section has 0 files"
  artifacts:
    - path: "src/components/StagingPanel.svelte"
      provides: "Colored buttons and 50/50 flex layout"
      contains: "background: #22c55e"
    - path: "src/components/FileRow.svelte"
      provides: "Green/red icon tinting"
      contains: "#22c55e"
  key_links:
    - from: "src/components/StagingPanel.svelte"
      to: "CSS flex layout"
      via: "flex: 1 on both section containers"
      pattern: "flex: 1.*overflow-y: auto"
    - from: "src/components/FileRow.svelte"
      to: "icon color"
      via: "conditional color based on actionLabel"
      pattern: "actionLabel === '\\+' \\? '#22c55e' : '#f87171'"
---

<objective>
Style staging buttons with green/red fills, tint file row icons, and implement 50/50 equal-height file list layout.

Purpose: Users can distinguish stage/unstage actions at a glance through color coding, and both file lists share space equally for balanced visibility.
Output: StagingPanel.svelte with colored buttons and split layout; FileRow.svelte with green/red action icons.
</objective>

<execution_context>
@/Users/joaofnds/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-staging-commit-ux/29-CONTEXT.md
@.planning/phases/29-staging-commit-ux/29-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/StagingPanel.svelte (current state):
- Line 181: Single scroll wrapper `<div style="flex: 1; overflow-y: auto; min-height: 0;">` contains both unstaged and staged sections
- Lines 206-220: Discard All button — currently `background: none; border: none; color: var(--color-text-muted)`
- Lines 221-235: Stage All Changes button — currently `background: none; border: none; color: var(--color-text-muted)`
- Lines 289-303: Unstage All button — currently `background: none; border: none; color: var(--color-text-muted)`
- Lines 25-26: `let unstaged_expanded = $state(true)` and `let staged_expanded = $state(true)`

From src/components/FileRow.svelte (current state):
- Lines 81-101: Action button with `color: var(--color-accent)` — same blue for both Plus and Minus
- `actionLabel` prop is `'+'` for stage, `'−'` (unicode minus) for unstage

From src/app.css (color tokens):
```css
--color-accent: #388bfd;
--color-text: #c9d1d9;
--color-text-muted: #8b949e;
--color-border: #30363d;
--color-surface: #21262d;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Color stage/unstage/discard buttons and tint file row icons</name>
  <files>src/components/StagingPanel.svelte, src/components/FileRow.svelte</files>
  <read_first>
    - src/components/StagingPanel.svelte (current button styles at lines 206-235 and 289-303)
    - src/components/FileRow.svelte (current action button at lines 81-101)
  </read_first>
  <action>
**A. StagingPanel.svelte — Color the three action buttons:**

**1. Discard All button (lines 206-220):** Replace the button's inline style:
Replace:
```
style="
  color: var(--color-text-muted);
  font-size: 11px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0 4px;
  white-space: nowrap;
"
```
With:
```
style="
  background: #f87171;
  color: white;
  font-size: 11px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  padding: 2px 8px;
  white-space: nowrap;
"
```

**2. Stage All Changes button (lines 221-235):** Replace the button's inline style:
Replace:
```
style="
  color: var(--color-text-muted);
  font-size: 11px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0 4px;
  white-space: nowrap;
"
```
With:
```
style="
  background: #22c55e;
  color: white;
  font-size: 11px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  padding: 2px 8px;
  white-space: nowrap;
"
```

**3. Unstage All button (lines 289-303):** Replace the button's inline style:
Replace:
```
style="
  color: var(--color-text-muted);
  font-size: 11px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0 4px;
  white-space: nowrap;
"
```
With:
```
style="
  background: #f87171;
  color: white;
  font-size: 11px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  padding: 2px 8px;
  white-space: nowrap;
"
```

**B. FileRow.svelte — Tint Plus/Minus action icons green/red:**

Replace the action button's inline style (lines 85-94):
Replace:
```
style="
  background: none;
  border: none;
  cursor: pointer;
  color: var(--color-accent);
  display: flex;
  align-items: center;
  padding: 0 4px;
  line-height: 1;
"
```
With:
```
style="
  background: none;
  border: none;
  cursor: pointer;
  color: {actionLabel === '+' ? '#22c55e' : '#f87171'};
  display: flex;
  align-items: center;
  padding: 0 4px;
  line-height: 1;
"
```

This changes Plus (+) icons to green (#22c55e) and Minus (−) icons to red (#f87171), reinforcing the stage=green / unstage=red visual language throughout.
  </action>
  <verify>
    <automated>grep -c "#22c55e" src/components/StagingPanel.svelte | grep -q '1' && grep -c "#f87171" src/components/StagingPanel.svelte | grep -q '2' && grep -c "#22c55e" src/components/FileRow.svelte | grep -q '[1-9]' && grep -c "#f87171" src/components/FileRow.svelte | grep -q '[1-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - StagingPanel.svelte Stage All button contains `background: #22c55e` and `color: white`
    - StagingPanel.svelte Unstage All button contains `background: #f87171` and `color: white`
    - StagingPanel.svelte Discard All button contains `background: #f87171` and `color: white`
    - All three buttons have `border-radius: 4px` and `padding: 2px 8px`
    - FileRow.svelte action button color is `{actionLabel === '+' ? '#22c55e' : '#f87171'}`
    - FileRow.svelte no longer contains `color: var(--color-accent)` in the action button
    - `npm test` passes
  </acceptance_criteria>
  <done>Stage All button is green, Unstage All and Discard All buttons are red, all with white text and rounded corners. Plus icon is green, Minus icon is red in file rows.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 50/50 equal-height file list layout with independent scroll</name>
  <files>src/components/StagingPanel.svelte</files>
  <read_first>
    - src/components/StagingPanel.svelte (current scroll wrapper at line 181, section structure lines 181-321)
  </read_first>
  <action>
Replace the single scrollable wrapper and its two child sections (lines 181-321) with a flex container that gives each section equal height and independent scrolling.

**Replace the entire block from line 181 (`<!-- Scrollable file sections wrapper -->`) through line 321 (closing `</div>` of the staged section's parent `</div>`) with:**

```html
<!-- File sections flex container (50/50 split when both expanded) -->
<div style="flex: 1; display: flex; flex-direction: column; overflow: hidden; min-height: 0;">
  <!-- Unstaged Files section -->
  <div style="
    {unstaged_expanded && staged_expanded ? 'flex: 1;' : unstaged_expanded ? 'flex: 1;' : ''}
    display: flex;
    flex-direction: column;
    overflow: hidden;
    min-height: 0;
  ">
    <div
      role="button"
      tabindex="0"
      onclick={() => (unstaged_expanded = !unstaged_expanded)}
      onkeydown={(e) => { if (e.key === 'Enter' || e.key === ' ') unstaged_expanded = !unstaged_expanded; }}
      style="
        height: 28px;
        border-bottom: 1px solid var(--color-border);
        padding: 0 8px;
        display: flex;
        align-items: center;
        cursor: pointer;
        user-select: none;
        flex-shrink: 0;
      "
    >
      <span style="color: var(--color-text-muted); display: inline-flex; align-items: center; margin-right: 4px;">
        {#if unstaged_expanded}<ChevronDown size={12} />{:else}<ChevronRight size={12} />{/if}
      </span>
      <span style="color: var(--color-text); font-size: 12px; font-weight: 500; flex: 1;">
        Unstaged Files ({(status?.unstaged.length ?? 0) + (status?.conflicted.length ?? 0)})
      </span>
      {#if (status?.unstaged.length ?? 0) > 0 || (status?.conflicted.length ?? 0) > 0}
        <button
          onclick={(e) => { e.stopPropagation(); handleDiscardAll(); }}
          style="
            background: #f87171;
            color: white;
            font-size: 11px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            padding: 2px 8px;
            white-space: nowrap;
          "
          aria-label="Discard all changes"
        >
          Discard All
        </button>
        <button
          onclick={(e) => { e.stopPropagation(); stageAll(); }}
          style="
            background: #22c55e;
            color: white;
            font-size: 11px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            padding: 2px 8px;
            white-space: nowrap;
            margin-left: 4px;
          "
          aria-label="Stage all changes"
        >
          Stage All Changes
        </button>
      {/if}
    </div>

    {#if unstaged_expanded}
      <div style="flex: 1; overflow-y: auto; min-height: 0;" role="list">
        {#each status?.unstaged ?? [] as f (f.path)}
          <FileRow
            file={f}
            actionLabel="+"
            isLoading={loadingFiles.has(f.path)}
            onaction={() => stageFile(f.path)}
            onclick={() => onfileselect?.(f.path, 'unstaged')}
            oncontextmenu={(e) => showFileContextMenu(e, f.path, f.status)}
          />
        {/each}
        {#each status?.conflicted ?? [] as f (f.path)}
          <FileRow
            file={f}
            actionLabel="+"
            isLoading={loadingFiles.has(f.path)}
            onaction={() => stageFile(f.path)}
            onclick={() => onfileselect?.(f.path, 'unstaged')}
            oncontextmenu={(e) => showFileContextMenu(e, f.path, f.status)}
          />
        {/each}
      </div>
    {/if}
  </div>

  <!-- Staged Files section -->
  <div style="
    {staged_expanded && unstaged_expanded ? 'flex: 1;' : staged_expanded ? 'flex: 1;' : ''}
    display: flex;
    flex-direction: column;
    overflow: hidden;
    min-height: 0;
  ">
    <div
      role="button"
      tabindex="0"
      onclick={() => (staged_expanded = !staged_expanded)}
      onkeydown={(e) => { if (e.key === 'Enter' || e.key === ' ') staged_expanded = !staged_expanded; }}
      style="
        height: 28px;
        border-bottom: 1px solid var(--color-border);
        padding: 0 8px;
        display: flex;
        align-items: center;
        cursor: pointer;
        user-select: none;
        flex-shrink: 0;
      "
    >
      <span style="color: var(--color-text-muted); display: inline-flex; align-items: center; margin-right: 4px;">
        {#if staged_expanded}<ChevronDown size={12} />{:else}<ChevronRight size={12} />{/if}
      </span>
      <span style="color: var(--color-text); font-size: 12px; font-weight: 500; flex: 1;">
        Staged Files ({status?.staged.length ?? 0})
      </span>
      {#if (status?.staged.length ?? 0) > 0}
        <button
          onclick={(e) => { e.stopPropagation(); unstageAll(); }}
          style="
            background: #f87171;
            color: white;
            font-size: 11px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            padding: 2px 8px;
            white-space: nowrap;
          "
          aria-label="Unstage all"
        >
          Unstage All
        </button>
      {/if}
    </div>

    {#if staged_expanded}
      <div style="flex: 1; overflow-y: auto; min-height: 0;" role="list">
        {#each status?.staged ?? [] as f (f.path)}
          <FileRow
            file={f}
            actionLabel="−"
            isLoading={loadingFiles.has(f.path)}
            onaction={() => unstageFile(f.path)}
            onclick={() => onfileselect?.(f.path, 'staged')}
          />
        {/each}
      </div>
    {/if}
  </div>
</div>
```

**Key structural changes:**
1. The outer `<div style="flex: 1; overflow-y: auto; min-height: 0;">` (old line 181) becomes `<div style="flex: 1; display: flex; flex-direction: column; overflow: hidden; min-height: 0;">` — a flex column container, NOT scrollable itself
2. Each section gets `flex: 1` when expanded (making them equal height), and its own `overflow-y: auto` scroll container for the file list
3. Section headers get `flex-shrink: 0` so they always stay visible
4. When one section is collapsed (no `flex: 1`), the other section with `flex: 1` takes all space
5. The `{#if expanded}` blocks wrap ONLY the scrollable file list div, not the header — headers always visible
6. Added `margin-left: 4px` between Discard All and Stage All buttons for spacing
  </action>
  <verify>
    <automated>grep -c "overflow-y: auto" src/components/StagingPanel.svelte | grep -q '2' && grep -c "flex-shrink: 0" src/components/StagingPanel.svelte | grep -q '[2-9]' && grep -c "flex: 1; overflow-y: auto; min-height: 0;" src/components/StagingPanel.svelte | grep -q '2' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - StagingPanel.svelte has exactly 2 `overflow-y: auto` elements (one per file list section), NOT on the outer wrapper
    - StagingPanel.svelte outer wrapper uses `display: flex; flex-direction: column; overflow: hidden` (not `overflow-y: auto`)
    - Both section headers have `flex-shrink: 0`
    - Unstaged section div has conditional `flex: 1` based on `unstaged_expanded && staged_expanded` or `unstaged_expanded`
    - Staged section div has conditional `flex: 1` based on `staged_expanded && unstaged_expanded` or `staged_expanded`
    - Each file list `<div role="list">` is wrapped in its own `<div style="flex: 1; overflow-y: auto; min-height: 0;">`
    - `npm test` passes
  </acceptance_criteria>
  <done>Unstaged and staged file lists each get 50% of available space with independent scrolling. Collapsing one section gives the other 100%. Headers always visible.</done>
</task>

</tasks>

<verification>
- `npm test` passes with no regressions
- StagingPanel.svelte has green Stage All, red Unstage All, red Discard All buttons
- FileRow.svelte has green Plus and red Minus icons
- StagingPanel.svelte has two independent scroll containers (not one shared wrapper)
</verification>

<success_criteria>
- STAGE-03: Stage All Changes button is visually green (#22c55e) with white text
- STAGE-04: Unstage All button is visually red (#f87171) with white text
- STAGE-05: Both file lists render at equal height (50/50) when both expanded, with independent scroll
- Discard All button is also red for visual consistency
- Plus/Minus file row icons are green/red respectively
</success_criteria>

<output>
After completion, create `.planning/phases/29-staging-commit-ux/29-02-SUMMARY.md`
</output>
